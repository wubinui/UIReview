(() => {
  if (window.__uireviewLoaded) return;
  window.__uireviewLoaded = true;
  // Load quietly. The inspector is opt-in: users explicitly open it from the
  // extension button or shortcut, and closing it restores a clean page.
  const state = { enabled: false, mode: null, toolbarCollapsed: false, toolMenu: "", hovered: null, selected: null, showPanel: true, panelOpen: true, closedElement: null, settingsOpen: false, feishuStatus: null, feishuChecking: false, pointer: { x: 0, y: 0 }, hitStack: [], hitIndex: 0, pinnedPanels: [], nextPinId: 1, selectorOpen: false, selectorQuery: "", selectorMatches: [], selectorIndex: 0, selectorError: "", selectorTimer: 0, detectedPseudoStates: [], forcedPseudoState: "", pickedColor: null, eyedropperError: "", captureSelecting: false, captureBusy: false, captureRect: null, captureDrag: null, captureReturnToFeedback: false, feedbackAnchorRect: null, feedbackOpen: false, feedbackEntering: false, feedbackClosing: false, feedbackSeen: false, feedbackEditing: false, destinationOpen: false, feedbackBusy: false, feedbackAdding: false, screenshotPreparing: false, screenshotDataUrl: "", feedbackQuestion: "", feedbackNote: "", feedbackDocumentUrl: "", feedbackMessage: "", feedbackStatusType: "", feedbackResultUrl: "", annotationTool: "pen", annotationStart: null, annotationDrawing: false, annotationBaseDataUrl: "", annotationHistory: [], annotationDraft: null, documents: [], selectedDocumentId: "", panelPosition: null, hintVisible: false, toolbar: { left: 16, top: 16 } };
  const host = document.createElement("div"); host.id = "uireview-overlay-host"; host.style.cssText = "position:fixed;inset:0;z-index:2147483647;pointer-events:none;";
  const shadow = host.attachShadow({ mode: "open" }); document.documentElement.appendChild(host);
  shadow.innerHTML = `<style>
  :host{--neutral-0:#fff;--neutral-100:#f5f5f5;--neutral-200:#e5e5e5;--neutral-300:#d4d4d4;--neutral-400:#a3a3a3;--neutral-500:#737373;--neutral-900:#171717;--blue-100:#d3e0ff;--blue-400:#5f89fe;--blue-500:#335cff;--red-400:#ff7675;--red-500:#fb3748;--red-700:#ba182d;--orange-100:#ffe6db;--orange-600:#df6e35;--orange-700:#b95826;--violet-100:#e0deff;--violet-700:#5b36b8;--green-500:#1fc16b;--spacing-1:4px;--spacing-2:8px;--spacing-3:12px;--spacing-4:16px;--radius-sm:4px;--radius-md:8px;--radius-lg:10px;--radius-xl:12px;--radius-full:999px;--shadow-float:0 4px 8px -1px rgba(0,0,0,.04),0 1px 2px rgba(0,0,0,.05),0 0 0 1px rgba(0,0,0,.07);font-family:Inter,ui-sans-serif,system-ui,-apple-system,sans-serif;color:var(--neutral-900)}*{box-sizing:border-box}[hidden]{display:none!important}#app.capture-active>:not(.capture-overlay):not(.submit-toast){display:none!important}
  .toolbar{pointer-events:auto;position:fixed;z-index:30;display:flex;align-items:center;gap:8px;padding:8px;border-radius:14px;background:#fff;box-shadow:0 0 0 1px rgba(0,0,0,.07),0 1px 2px rgba(0,0,0,.05),0 4px 8px -1px rgba(0,0,0,.04);cursor:move;user-select:none;touch-action:none;transform-origin:left center;animation:toolbar-in 120ms ease-out}.toolbar button{width:32px;height:32px;padding:6px;border-radius:6px}.toolbar button:hover{background:#f2f2f2}.toolbar button.active{background:#171717;color:#fff}.toolbar button.active svg{stroke:#fff!important}.toolbar>button svg,.tool-group>button:not(.tool-arrow) svg{width:20px;height:20px}.tool-group{display:flex;align-items:center;gap:0}.toolbar .tool-arrow{width:16px;height:32px;padding:11px 3px;border-radius:4px}.tool-arrow svg{width:10px;height:10px}.toolbar .tool-arrow.active{background:#ebebeb;color:#171717}.toolbar .tool-arrow.active svg{stroke:#171717!important}.toolbar .sep{height:20px;margin:0;width:1px}.toolbar.dragging{cursor:grabbing;will-change:transform}.toolbar-logo{pointer-events:auto;position:fixed;z-index:30;width:48px;height:48px;padding:0;border:0;border-radius:12px;background:#171717;box-shadow:0 0 0 1px rgba(0,0,0,.07),0 1px 2px rgba(0,0,0,.08),0 6px 14px rgba(0,0,0,.08);cursor:pointer;overflow:hidden;touch-action:none;user-select:none;transform-origin:center;animation:toolbar-in 120ms ease-out;contain:layout style}.toolbar-logo:hover{box-shadow:0 0 0 1px rgba(0,0,0,.08),0 2px 4px rgba(0,0,0,.08),0 8px 18px rgba(0,0,0,.12)}.toolbar-logo.drag-ready,.toolbar-logo.dragging{cursor:grabbing;will-change:transform}.toolbar-logo img{display:block;width:48px;height:48px;pointer-events:none}.tool-menu{pointer-events:auto;position:fixed;z-index:32;min-width:196px;padding:6px;border-radius:10px;background:#fff;box-shadow:0 0 0 1px rgba(0,0,0,.08),0 10px 28px rgba(0,0,0,.14);animation:toolbar-in 100ms ease-out}.tool-menu button{display:flex;align-items:center;justify-content:flex-start;gap:8px;width:100%;height:32px;padding:0 9px;border-radius:6px;color:#171717;font-size:11px;white-space:nowrap}.tool-menu button:hover{background:#f2f2f2}.tool-menu button svg{width:16px;height:16px}.tool-menu .menu-shortcut{margin-left:auto;color:#999;font:10px ui-monospace,SFMono-Regular,monospace}@keyframes toolbar-in{from{opacity:0;transform:scale(.96)}to{opacity:1;transform:scale(1)}}
  button{border:0;background:transparent;color:#666;width:32px;height:32px;border-radius:8px;cursor:pointer;font-size:14px;display:grid;place-items:center}button:hover{background:#f1f1f1;color:#111}button.active{background:#335cff;color:#fff}button.active svg{stroke:#fff!important}button:focus,button:focus-visible{outline:0;box-shadow:none}.sep{width:1px;background:#e7e7e7;margin:4px 2px}.tool-tip{position:fixed;z-index:40;pointer-events:none;display:flex;align-items:center;gap:8px;padding:6px 8px;border-radius:7px;background:#171717;color:#fff;font-size:11px;line-height:16px;white-space:nowrap;box-shadow:0 5px 16px rgba(0,0,0,.2);opacity:0;transform:translateY(-2px);transition:opacity 100ms ease,transform 100ms ease}.tool-tip.visible{opacity:1;transform:translateY(0)}.tool-shortcut{display:inline-flex;align-items:center;justify-content:center;min-width:18px;height:18px;padding:0 5px;border:1px solid rgba(255,255,255,.22);border-radius:4px;background:rgba(255,255,255,.12);color:#ddd;font:10px ui-monospace,SFMono-Regular,monospace}
  .box{position:fixed;left:0;top:0;pointer-events:none;will-change:transform,width,height}.label{position:fixed;left:0;top:0;pointer-events:none;white-space:nowrap;will-change:transform}.pixel-ruler{position:fixed;z-index:10;pointer-events:none}.pixel-ruler.top{left:28px;top:0;width:calc(100vw - 28px);height:28px}.pixel-ruler.left{left:0;top:28px;width:28px;height:calc(100vh - 28px)}.ruler-corner{position:fixed;z-index:11;left:0;top:0;width:28px;height:28px;pointer-events:none;background:var(--neutral-100);border-right:1px solid var(--neutral-300);border-bottom:1px solid var(--neutral-300)}.measure{position:fixed;z-index:1;pointer-events:none}.measure-label{position:fixed;z-index:2;pointer-events:none;white-space:nowrap}
  .box-model-layer,.layout-children-layer,.selector-highlight-layer{position:fixed;inset:0;pointer-events:none}.box-model-zone{position:fixed;pointer-events:none}.box-model-value{position:fixed;white-space:nowrap}.flex-axis{position:fixed;z-index:5;display:flex;align-items:center;justify-content:center;color:var(--blue-500);font:700 14px/1 ui-monospace,SFMono-Regular,monospace;filter:drop-shadow(0 1px 1px var(--neutral-0));pointer-events:none}.layout-child{position:fixed;pointer-events:none}.selector-highlight{position:fixed;border:1px solid rgba(51,92,255,.7);background:rgba(51,92,255,.08);pointer-events:none}.selector-highlight.active{border:2px solid var(--red-500);background:rgba(251,55,72,.1)}
  .pseudo-row{display:flex;flex-wrap:wrap;gap:5px}.pseudo-chip{width:auto;height:24px;padding:0 var(--spacing-2);border-radius:6px;background:var(--neutral-100);color:#555;font:10px ui-monospace,SFMono-Regular,monospace}.pseudo-chip.active{background:var(--blue-500);color:var(--neutral-0)}.settings{position:fixed;width:252px;padding:var(--spacing-2);border:1px solid var(--neutral-200);border-radius:var(--radius-lg);background:var(--neutral-0);box-shadow:0 12px 32px rgba(0,0,0,.14);font-size:12px;pointer-events:auto}.settings h3{margin:var(--spacing-1) var(--spacing-1) var(--spacing-2);font-size:13px}.setting{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:var(--spacing-2) var(--spacing-1);border-top:1px solid #f0f0f0;cursor:pointer}.setting input{accent-color:var(--blue-500)}.feishu-setting{display:flex;align-items:center;gap:var(--spacing-2);padding:9px var(--spacing-1);border-top:1px solid #f0f0f0}.feishu-dot{width:7px;height:7px;flex:none;border-radius:50%;background:#9ca3af}.feishu-dot.ready{background:var(--green-500)}.feishu-dot.warning{background:#f59e0b}.feishu-dot.offline{background:var(--red-500)}.feishu-text{min-width:0;flex:1;color:#555;font-size:11px;line-height:15px}.feishu-open{width:auto;height:28px;padding:0 9px;background:#f1f1f1;color:var(--neutral-900);font-size:11px}.hint{position:fixed;bottom:var(--spacing-4);left:var(--spacing-4);padding:6px 9px;border-radius:6px;background:rgba(23,23,23,.9);color:var(--neutral-0);font-size:11px;pointer-events:none}.selector-finder{position:fixed;z-index:31;width:320px;padding:var(--spacing-2);border:1px solid rgba(0,0,0,.1);border-radius:var(--radius-xl);background:var(--neutral-0);box-shadow:0 12px 32px rgba(0,0,0,.2);pointer-events:auto}.selector-input{width:100%;height:34px;padding:0 10px;border:1px solid #ddd;border-radius:var(--radius-md);outline:0;font:12px ui-monospace,SFMono-Regular,monospace}.selector-input:focus{border-color:var(--blue-500);box-shadow:0 0 0 2px rgba(51,92,255,.13)}.selector-footer{display:flex;align-items:center;gap:var(--spacing-1);margin-top:6px}.selector-status{min-width:0;flex:1;padding-left:2px;color:var(--neutral-500);font-size:10px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.selector-status.error{color:var(--red-500)}.selector-nav{width:28px;height:26px;border-radius:6px;background:var(--neutral-100)}.color-panel{position:fixed;width:224px;padding:10px;border:1px solid rgba(0,0,0,.1);border-radius:var(--radius-xl);background:var(--neutral-0);box-shadow:0 10px 30px rgba(0,0,0,.14);pointer-events:auto}.color-preview{height:72px;margin-bottom:var(--spacing-2);border:1px solid rgba(0,0,0,.1);border-radius:var(--radius-md)}.color-copy{display:flex;align-items:center;justify-content:space-between;width:100%;height:32px;padding:0 9px;border-radius:7px;background:var(--neutral-100);color:var(--neutral-900);font:11px ui-monospace,SFMono-Regular,monospace}.color-copy:hover{background:#ededed}.color-copy .copy-note{color:#8a8a8a;font:10px Inter,ui-sans-serif,system-ui}.color-rgb{display:flex;align-items:center;justify-content:space-between;height:30px;padding:0 9px;color:var(--neutral-500);font:11px ui-monospace,SFMono-Regular,monospace}.color-error{padding:7px 5px;color:var(--red-500);font-size:11px;line-height:16px}
  .capture-overlay{position:fixed;inset:0;z-index:200;pointer-events:auto;cursor:crosshair;user-select:none;touch-action:none}.capture-shade{position:fixed;background:rgba(0,0,0,.48);pointer-events:none}.capture-selection{position:fixed;left:0;top:0;border:1px solid #335cff;outline:1px solid rgba(255,255,255,.75);pointer-events:auto;cursor:move;will-change:transform,width,height}.capture-selection:empty{pointer-events:none}.capture-handle{position:absolute;width:10px;height:10px;border:1px solid #335cff;border-radius:2px;background:#fff;box-shadow:0 1px 3px #0003}.capture-handle[data-handle="nw"]{left:-6px;top:-6px;cursor:nwse-resize}.capture-handle[data-handle="n"]{left:50%;top:-6px;transform:translateX(-50%);cursor:ns-resize}.capture-handle[data-handle="ne"]{right:-6px;top:-6px;cursor:nesw-resize}.capture-handle[data-handle="e"]{right:-6px;top:50%;transform:translateY(-50%);cursor:ew-resize}.capture-handle[data-handle="se"]{right:-6px;bottom:-6px;cursor:nwse-resize}.capture-handle[data-handle="s"]{left:50%;bottom:-6px;transform:translateX(-50%);cursor:ns-resize}.capture-handle[data-handle="sw"]{left:-6px;bottom:-6px;cursor:nesw-resize}.capture-handle[data-handle="w"]{left:-6px;top:50%;transform:translateY(-50%);cursor:ew-resize}.capture-size{position:absolute;left:0;top:-27px;height:21px;padding:0 7px;border-radius:5px;background:#171717;color:#fff;font:11px/21px ui-monospace,SFMono-Regular,monospace;white-space:nowrap;pointer-events:none}.capture-instructions{position:fixed;left:50%;top:18px;transform:translateX(-50%);padding:8px 11px;border-radius:8px;background:#171717;color:#fff;box-shadow:0 6px 20px #0005;font-size:12px;white-space:nowrap;pointer-events:none}.capture-controls{position:fixed;display:flex;gap:6px;padding:5px;border-radius:10px;background:#fff;box-shadow:0 10px 30px #0004;pointer-events:auto;cursor:default}.capture-controls button{width:auto;height:30px;padding:0 11px}.capture-controls .capture-confirm{background:#171717;color:#fff}.capture-controls .capture-confirm:hover{background:#333;color:#fff}.capture-controls button:disabled{opacity:.55;cursor:wait}
  .feedback-backdrop{position:fixed;inset:0;z-index:100;background:rgba(0,0,0,.16);pointer-events:auto}.feedback-dialog{position:fixed;display:flex;flex-direction:column;gap:12px;width:336px;overflow:visible;outline:0;font-family:"PingFang SC",Inter,ui-sans-serif,system-ui,-apple-system,sans-serif;font-size:12px;pointer-events:auto}.feedback-card{display:flex;flex-direction:column;gap:12px;width:100%;padding:16px;border-radius:14px;background:#fff;box-shadow:0 8px 28px rgba(0,0,0,.18)}.feedback-head{display:flex;align-items:center;gap:12px;height:20px}.feedback-head strong{min-width:0;flex:1;color:#000;font-size:12px;font-weight:500;line-height:18px}.feedback-close{display:flex;align-items:center;justify-content:center;width:20px;height:20px;padding:4px;border-radius:999px}.feedback-close svg{display:block;width:12px;height:12px}.feedback-close:hover{background:#f7f7f7}.feedback-inputs{display:flex;flex-direction:column;gap:8px;cursor:text}.feedback-inputs textarea,.feedback-inputs input{display:block;width:100%;padding:0;border:0;border-radius:0;background:#fff;color:#171717;font:13px/20px "PingFang SC",Inter,ui-sans-serif,system-ui;outline:0}.feedback-inputs:not(.is-editing) textarea,.feedback-inputs:not(.is-editing) input{pointer-events:none}.feedback-inputs textarea{min-height:120px;resize:none}.feedback-inputs input{height:20px}.feedback-inputs textarea::placeholder,.feedback-inputs input::placeholder{color:#6f6f6f;opacity:1}.feedback-connected-document{display:flex;align-items:center;gap:7px;min-width:0;color:#5f5f5f;font-size:11px;line-height:16px}.feedback-connected-document:before{content:"";width:6px;height:6px;flex:none;border-radius:50%;background:#1fc16b;box-shadow:0 0 0 2px rgba(31,193,107,.12)}.feedback-connected-document span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.feedback-actions{display:flex;align-items:center;gap:8px;padding-top:4px}.feedback-destination-toggle{display:flex;align-items:center;justify-content:center;flex:none;width:36px;height:36px;padding:10px;border-radius:10px;background:#f7f7f7}.feedback-destination-toggle svg{display:block;width:16px;height:16px}.feedback-destination-toggle:hover,.feedback-destination-toggle.active{background:#ededed}.feedback-destination-toggle.active svg{stroke:#171717!important}.feedback-send{display:flex;align-items:center;justify-content:center;flex:1;width:auto;height:36px;padding:0 6px;border-radius:10px;background:#171717;color:#fff;font:500 12px/18px "PingFang SC",Inter,ui-sans-serif,system-ui}.feedback-send:hover{background:#333;color:#fff}.feedback-send:disabled{opacity:.5;cursor:wait}.feedback-status{color:#777;font-size:11px;line-height:16px}.feedback-status:empty{display:none}.feedback-status.error{color:#fb3748}.feedback-status.success{color:#1fc16b}.feedback-destination-fields{display:flex;flex-direction:column;gap:10px}.feedback-select-wrap{position:relative}.feedback-doc,.feedback-doc-url{display:block;width:100%;height:36px;padding:0 12px;border:0;border-radius:10px;background:#f7f7f7;color:#171717;font:12px/18px "PingFang SC",Inter,ui-sans-serif,system-ui;outline:0}.feedback-doc{padding-right:36px;appearance:none;-webkit-appearance:none}.feedback-select-icon{position:absolute;right:12px;top:12px;width:12px;height:12px;pointer-events:none}.feedback-select-icon svg{display:block;width:12px;height:12px}.feedback-doc-url::placeholder{color:#6f6f6f;opacity:1}.feedback-doc:focus,.feedback-doc-url:focus{box-shadow:0 0 0 2px rgba(51,92,255,.18)}.feedback-shot-column{position:fixed;display:flex;flex-direction:column;gap:8px;pointer-events:auto}.feedback-shot-wrap{position:relative;flex:1;min-height:0;border:0;border-radius:8px;background:#f5f5f5;box-shadow:0 12px 36px rgba(0,0,0,.22);overflow:hidden}.feedback-shot-placeholder{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;width:100%;height:100%;color:#737373;font-size:11px}.feedback-shot-placeholder:before{content:"";width:18px;height:18px;border:2px solid #d4d4d4;border-top-color:#335cff;border-radius:50%;animation:submit-spin .7s linear infinite}.feedback-canvas{display:block;width:100%;height:100%;object-fit:contain;cursor:crosshair;touch-action:none}.feedback-annotate-bar{display:flex;align-items:center;gap:6px;min-height:38px;padding:4px;border:.5px solid rgba(0,0,0,.08);border-radius:10px;background:#fff;box-shadow:0 8px 24px rgba(0,0,0,.18)}.feedback-annotate-bar button{width:30px;height:30px;border:0;background:#fff;color:#666}.feedback-annotate-bar button.active{background:#171717;color:#fff}.feedback-annotate-bar button:disabled{opacity:.32;cursor:not-allowed}.feedback-annotate-bar .annotation-divider{width:1px;height:18px;margin:0 1px;background:#e7e7e7}.feedback-annotate-bar .annotation-help{flex:1;padding:0 6px;color:#999;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.feedback-retake{position:absolute;right:8px;bottom:8px;width:auto;height:30px;padding:0 10px;background:rgba(23,23,23,.9);color:#fff;box-shadow:0 4px 12px #0003}.feedback-retake:hover{background:#171717;color:#fff}.feedback-backdrop.stacked{display:flex;flex-direction:column;gap:12px;overflow:auto;padding:28px 12px}.feedback-backdrop.stacked .feedback-shot-column,.feedback-backdrop.stacked .feedback-dialog{position:relative!important;left:auto!important;top:auto!important;width:min(560px,100%)!important;margin:0 auto}.feedback-backdrop.stacked .feedback-shot-column{order:0;height:min(55vh,420px)!important;flex:none}.feedback-backdrop.stacked .feedback-dialog{order:1;overflow:visible;flex:none}.submit-toast{position:fixed;z-index:180;right:16px;bottom:16px;display:flex;align-items:center;gap:9px;max-width:min(380px,calc(100vw - 32px));min-height:40px;padding:10px 12px;border:1px solid rgba(0,0,0,.08);border-radius:10px;background:#fff;color:#171717;box-shadow:0 12px 36px #0003;font-size:12px;line-height:18px;pointer-events:none}.submit-toast.loading:before{content:"";width:14px;height:14px;flex:none;border:2px solid #d4d4d4;border-top-color:#335cff;border-radius:50%;animation:submit-spin .7s linear infinite}.submit-toast.success:before,.submit-toast.error:before{display:grid;place-items:center;width:16px;height:16px;flex:none;border-radius:50%;color:#fff;font-size:11px;font-weight:700}.submit-toast.success:before{content:"✓";background:#1fc16b}.submit-toast.error:before{content:"!";background:#fb3748}.submit-toast.error{color:#fb3748}@keyframes submit-spin{to{transform:rotate(360deg)}}
  .feedback-backdrop{background:transparent}.feedback-backdrop:before{content:"";position:fixed;inset:0;background:rgba(0,0,0,.16);pointer-events:none}.feedback-destination-card{transform-origin:top center;animation:destination-open 180ms cubic-bezier(.23,1,.32,1)}.feedback-destination-card.closing{pointer-events:none;animation:destination-close 150ms cubic-bezier(.23,1,.32,1) forwards}.feedback-backdrop.entering:before{animation:feedback-fade-in 160ms cubic-bezier(.23,1,.32,1) both}.feedback-backdrop.entering .feedback-shot-wrap{animation:feedback-content-in 220ms cubic-bezier(.23,1,.32,1) both}.feedback-backdrop.entering .feedback-question-card{animation:feedback-content-in 220ms 35ms cubic-bezier(.23,1,.32,1) both}.feedback-backdrop.entering .feedback-destination-card{animation:feedback-content-in 220ms 65ms cubic-bezier(.23,1,.32,1) both}.feedback-backdrop.entering .feedback-annotate-bar{animation:feedback-toolbar-in 190ms 55ms cubic-bezier(.23,1,.32,1) both}.feedback-backdrop.closing{pointer-events:none}.feedback-backdrop.closing:before{animation:feedback-fade-out 160ms cubic-bezier(.23,1,.32,1) forwards}.feedback-backdrop.closing .feedback-shot-column,.feedback-backdrop.closing .feedback-dialog{animation:feedback-content-out 150ms cubic-bezier(.23,1,.32,1) forwards}@keyframes destination-open{from{opacity:0;transform:translateY(-8px) scale(.985)}to{opacity:1;transform:translateY(0) scale(1)}}@keyframes destination-close{from{opacity:1;transform:translateY(0) scale(1)}to{opacity:0;transform:translateY(-8px) scale(.985)}}@keyframes feedback-fade-in{from{opacity:0}to{opacity:1}}@keyframes feedback-fade-out{from{opacity:1}to{opacity:0}}@keyframes feedback-content-in{from{opacity:0;transform:translateY(8px) scale(.985)}to{opacity:1;transform:translateY(0) scale(1)}}@keyframes feedback-toolbar-in{from{opacity:0;transform:translateY(6px) scale(.98)}to{opacity:1;transform:translateY(0) scale(1)}}@keyframes feedback-content-out{from{opacity:1;transform:translateY(0) scale(1)}to{opacity:0;transform:translateY(4px) scale(.985)}}@keyframes feedback-reduced-in{from{opacity:0}to{opacity:1}}@keyframes feedback-reduced-out{from{opacity:1}to{opacity:0}}@media (prefers-reduced-motion:reduce){.feedback-destination-card,.feedback-backdrop.entering .feedback-shot-wrap,.feedback-backdrop.entering .feedback-question-card,.feedback-backdrop.entering .feedback-destination-card,.feedback-backdrop.entering .feedback-annotate-bar{animation:feedback-reduced-in 100ms 0ms cubic-bezier(.23,1,.32,1) both}.feedback-destination-card.closing,.feedback-backdrop.closing .feedback-shot-column,.feedback-backdrop.closing .feedback-dialog{animation:feedback-reduced-out 100ms cubic-bezier(.23,1,.32,1) forwards}}
  /* Inspector panel — Figma node 63:741 */
  .panel{position:fixed;overflow:auto}
  .panel{width:240px;max-height:calc(100vh - 24px);padding:var(--spacing-3) 14px;border:0;border-radius:var(--radius-xl);background:var(--neutral-0);box-shadow:var(--shadow-float);font-size:12px;line-height:18px;cursor:default;pointer-events:none}.panel.position-fixed,.panel.pinned{cursor:move;pointer-events:auto}.panel.position-fixed.dragging,.panel.pinned.dragging{cursor:grabbing}.panel.pinned{z-index:22}.panel h3{position:absolute;top:5px;right:5px;z-index:2;margin:0;cursor:default}.panel-close{width:20px;height:20px;padding:0;border-radius:5px;color:var(--neutral-500);font-size:14px;line-height:12px;opacity:0;transition:opacity 100ms ease}.panel:hover .panel-close,.panel:focus-within .panel-close{opacity:1}.panel-content{display:flex;flex-direction:column;gap:var(--spacing-4)}.panel-block{display:flex;flex-direction:column;gap:6px}.panel-group{display:flex;flex-direction:column;gap:var(--spacing-1)}.section-title{margin:0;padding:0;border:0;color:var(--neutral-400);font:500 12px/18px 'PingFang SC',Inter,ui-sans-serif,system-ui;text-transform:none;letter-spacing:0}.panel-row{display:flex;align-items:center;gap:6px;min-height:18px;white-space:nowrap}.panel-row .key{flex:none;color:var(--neutral-900);font:300 12px/18px 'Geist Mono',ui-monospace,SFMono-Regular,monospace;text-transform:none}.panel-row .value{min-width:0;overflow:hidden;color:var(--violet-700);font:300 12px/18px 'Geist Mono',ui-monospace,SFMono-Regular,monospace;text-overflow:ellipsis;white-space:nowrap}.panel-row .value.red{color:var(--red-700)}.panel-row .value.violet{color:var(--violet-700)}.panel-row .value.swatch-value{display:flex;align-items:center;gap:var(--spacing-1);color:var(--violet-700)}.panel-row .swatch{width:12px;height:12px;border:0;border-radius:var(--radius-sm);flex:none}.panel-row .swatch.has-border{border:1px solid var(--neutral-200)}.panel-row .swatch-text{min-width:0;overflow:hidden;text-overflow:ellipsis}.pseudo-row{margin-top:0}
  /* Inspection overlays — Figma node 80:1098 */
  .box{border:1px solid var(--blue-500);background:transparent}.hover-box.has-padding{border-color:transparent}.selected-box{border-color:var(--red-400)}.label{display:flex;align-items:center;gap:var(--spacing-2);height:16px;padding:0 2px;border-radius:0;background:var(--blue-500);color:var(--neutral-0);font:400 11px/16px 'Geist Mono',ui-monospace,SFMono-Regular,monospace}.label .label-size{color:var(--blue-100)}.box-model-zone.margin,.box-model-zone.padding{border:0;background:var(--orange-100)}.box-model-zone.gap{border:0;background:var(--violet-100)}.box-model-value{z-index:7;padding:0 2px;transform:translate(-50%,-50%);background:var(--orange-700);color:var(--neutral-0);font:400 11px/16px 'Geist Mono',ui-monospace,SFMono-Regular,monospace;text-shadow:none}.box-model-value.gap{background:var(--violet-700)}.layout-child{border:1px dashed var(--blue-400);background:transparent}.measure.distance-measure{border:0;background:var(--blue-500);color:var(--blue-500)}.measure.distance-measure.h{height:1px;transform:none}.measure.distance-measure.v{width:1px;transform:none}.measure.distance-measure:before,.measure.distance-measure:after{display:none}.measure-label.distance-label{height:16px;padding:0 2px;border-radius:0;background:var(--blue-500);color:var(--neutral-0);font:400 11px/16px 'Geist Mono',ui-monospace,SFMono-Regular,monospace}.selection-guide-layer{position:fixed;inset:0;pointer-events:none}.selection-guide{position:fixed;border-color:var(--neutral-300);border-style:dashed;opacity:.9}.selection-guide.h{height:0;border-width:1px 0 0}.selection-guide.v{width:0;border-width:0 0 0 1px}
  </style><div id="app"></div>`;
  const app = shadow.querySelector("#app"); let pending = false; const textTargetCache = new WeakMap();
  let rectCache=new WeakMap(),styleCache=new WeakMap();
  const ui = {};
  const ensureNode = (key,className) => { if(!ui[key]){ui[key]=document.createElement('div');ui[key].className=className;ui[key].hidden=true;app.appendChild(ui[key]);}return ui[key]; };
  const showNode = (node,visible) => { node.hidden=!visible; };
  const setBox = (node,rect) => { node.style.transform=`translate3d(${rect.left}px,${rect.top}px,0)`;node.style.width=`${rect.width}px`;node.style.height=`${rect.height}px`; };
  const gapLayer=ensureNode('gapLayer','measure-layer');gapLayer.hidden=false;
  const clearGeometryCache=()=>{rectCache=new WeakMap();styleCache=new WeakMap();};
  const schedule = () => { if (pending) return; pending = true; requestAnimationFrame(() => { pending = false;clearGeometryCache();render(); }); };
  const rectFor = el => {let rect=rectCache.get(el);if(rect)return rect;const r=el.getBoundingClientRect();rect={left:r.left,top:r.top,right:r.right,bottom:r.bottom,width:r.width,height:r.height};rectCache.set(el,rect);return rect;};
  const styleFor = el => {let style=styleCache.get(el);if(!style){style=getComputedStyle(el);styleCache.set(el,style);}return style;};
  const info = el => {const s=styleFor(el);return {element:el,tag:el.tagName.toLowerCase(),rect:rectFor(el),style:{display:s.display,position:s.position,color:s.color,backgroundColor:s.backgroundColor,fontFamily:s.fontFamily,fontSize:s.fontSize,fontWeight:s.fontWeight,fontStyle:s.fontStyle,lineHeight:s.lineHeight,letterSpacing:s.letterSpacing,textAlign:s.textAlign,textTransform:s.textTransform,textDecoration:s.textDecorationLine,margin:s.margin,padding:s.padding,paddingTop:s.paddingTop,paddingRight:s.paddingRight,paddingBottom:s.paddingBottom,paddingLeft:s.paddingLeft,boxSizing:s.boxSizing,borderRadius:s.borderRadius,boxShadow:s.boxShadow,opacity:s.opacity,gap:s.gap,rowGap:s.rowGap,columnGap:s.columnGap,flexDirection:s.flexDirection}}; };
  const textTarget = (el) => {const cached=textTargetCache.get(el);if(cached?.isConnected)return cached;let n=el; while(n&&n!==document.body){const s=styleFor(n); if(n.childElementCount===0||/^(p|h[1-6]|span|a|button|label|li|blockquote|code|pre|em|strong)$/i.test(n.tagName)||s.display==='inline'){textTargetCache.set(el,n);return n;} n=n.parentElement;}textTargetCache.set(el,el);return el; };
  // Lucide icons are inlined so Manifest V3 never depends on remote scripts or
  // local per-icon files. Paths below use Lucide's 24px viewBox geometry.
  const lucide = {
    inspect:'<path d="M4.037 4.688a.495.495 0 0 1 .651-.651l16 6.5a.5.5 0 0 1-.063.947l-6.124 1.58a2 2 0 0 0-1.438 1.435l-1.579 6.126a.5.5 0 0 1-.947.063z"/>',
    type:'<polyline points="4 7 4 4 20 4 20 7"/><line x1="9" y1="20" x2="15" y2="20"/><line x1="12" y1="4" x2="12" y2="20"/>',
    pipette:'<path d="m2 22 1-1h3l9-9"/><path d="M3 21v-3l9-9"/><path d="m15 6 3.4-3.4a2.1 2.1 0 1 1 3 3L18 9l.4.4a2.1 2.1 0 1 1-3 3l-3.8-3.8a2.1 2.1 0 1 1 3-3Z"/>',
    ruler:'<path d="M21.3 15.3a2.4 2.4 0 0 1 0 3.4l-2.6 2.6a2.4 2.4 0 0 1-3.4 0L2.7 8.7a2.41 2.41 0 0 1 0-3.4l2.6-2.6a2.41 2.41 0 0 1 3.4 0Z"/><path d="m14.5 12.5 2-2"/><path d="m11.5 9.5 2-2"/><path d="m8.5 6.5 2-2"/><path d="m17.5 15.5 2-2"/>',
    'panels-top-left':'<rect width="18" height="18" x="3" y="3" rx="2"/><path d="M3 9h18"/><path d="M9 21V9"/>',
    menu:'<path d="M4 5h16"/><path d="M4 12h16"/><path d="M4 19h16"/>',
    crop:'<path d="M6 2v14a2 2 0 0 0 2 2h14"/><path d="M18 22V8a2 2 0 0 0-2-2H2"/>',
    close:'<line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>',
    pen:'<path d="m4 20 4.5-1L19 8.5a2.12 2.12 0 0 0-3-3L5.5 16z"/><path d="m14.5 6.5 3 3"/>',
    'arrow-up-right':'<path d="M7 17 17 7"/><path d="M7 7h10v10"/>',
    square:'<rect width="18" height="18" x="3" y="3" rx="2"/>',
    circle:'<circle cx="12" cy="12" r="10"/>',
    undo:'<path d="M9 14 4 9l5-5"/><path d="M4 9h10.5a5.5 5.5 0 0 1 0 11H11"/>',
    search:'<circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/>',
    'chevron-left':'<path d="m15 18-6-6 6-6"/>',
    'chevron-right':'<path d="m9 18 6-6-6-6"/>',
    pin:'<path d="M12 17v5"/><path d="M5 17h14"/><path d="M15 3.5a1 1 0 0 0-1-1h-4a1 1 0 0 0-1 1V9L6 12v2h12v-2l-3-3z"/>',
    'scroll-text':'<path d="M15 12h-5"/><path d="M15 8h-5"/><path d="M19 17V5a2 2 0 0 0-2-2H4"/><path d="M8 21h12a2 2 0 0 0 2-2v-1a1 1 0 0 0-1-1H11a1 1 0 0 0-1 1v1a2 2 0 1 1-4 0V5a2 2 0 1 0-4 0v2a1 1 0 0 0 1 1h3"/>',
    'chevron-down':'<path d="m6 9 6 6 6-6"/>',
    'log-out':'<path d="m16 17 5-5-5-5"/><path d="M21 12H9"/><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/>'
  };
  const icon = name => `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#171717" stroke-width="1.25" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${lucide[name]}</svg>`;
  let tipTimer=0;
  const hideToolTip = () => {clearTimeout(tipTimer);ui.toolTip?.classList.remove('visible');};
  const showToolTip = (button,label,shortcut='') => {clearTimeout(tipTimer);tipTimer=setTimeout(()=>{const tip=ensureNode('toolTip','tool-tip');tip.innerHTML=`<span>${label}</span>${shortcut?`<kbd class="tool-shortcut">${shortcut}</kbd>`:''}`;showNode(tip,true);const r=button.getBoundingClientRect(),w=tip.offsetWidth;let left=Math.max(8,Math.min(innerWidth-w-8,r.left+r.width/2-w/2)),top=r.bottom+8;if(top+tip.offsetHeight>innerHeight-8)top=Math.max(8,r.top-tip.offsetHeight-8);tip.style.left=`${left}px`;tip.style.top=`${top}px`;requestAnimationFrame(()=>tip.classList.add('visible'));},180);};
  const btn = (label,iconName,tipLabel,shortcut,fn) => {const b=document.createElement('button');b.setAttribute('aria-label',label);b.innerHTML=icon(iconName);b.onclick=fn;b.addEventListener('mouseenter',()=>showToolTip(b,tipLabel,shortcut));b.addEventListener('mouseleave',hideToolTip);b.addEventListener('focus',()=>showToolTip(b,tipLabel,shortcut));b.addEventListener('blur',hideToolTip);return b;};
  const clearSelector = () => {clearTimeout(state.selectorTimer);state.selectorMatches=[];state.selectorIndex=0;state.selectorError='';};
  const clearForcedPseudo = () => {if(state.selected?.element)state.selected.element.removeAttribute('data-uireview-force');document.getElementById('uireview-forced-pseudo')?.remove();state.forcedPseudoState='';state.detectedPseudoStates=[];};
  let hintTimer=0;
  const hideModeHint = () => {clearTimeout(hintTimer);state.hintVisible=false;};
  const showModeHint = () => {clearTimeout(hintTimer);state.hintVisible=true;hintTimer=setTimeout(()=>{state.hintVisible=false;render();},3000);};
  const exitMode = () => { clearForcedPseudo();clearSelector();hideModeHint();state.mode=null;state.toolMenu='';state.hovered=null;state.selected=null;state.hitStack=[];state.hitIndex=0;state.pinnedPanels=[];state.panelPosition=null;state.selectorOpen=false;state.panelOpen=false;state.settingsOpen=false; };
  const hexToRgb = hex => {const value=hex.replace('#','');return {r:parseInt(value.slice(0,2),16),g:parseInt(value.slice(2,4),16),b:parseInt(value.slice(4,6),16)};};
  const cssColorToHex = color => {const match=String(color).match(/[\d.]+/g);if(!match||match.length<3)return String(color);const [r,g,b,a='1']=match.map(Number);if(a===0)return 'transparent';return `#${[r,g,b].map(value=>clamp(Math.round(value),0,255).toString(16).padStart(2,'0')).join('').toUpperCase()}`;};
  const feishuRequest = async (action, body) => {let result;try{result=await chrome.runtime.sendMessage({type:'feishu-api-request',action,body});}catch{throw new Error('UIReview 后台服务暂时不可用，请重新加载扩展。');}if(!result?.ok)throw new Error(result?.error||'飞书请求失败');return result.data;};
  let documentsLoadPromise=null,documentsLoaded=false;
  const loadDocuments = async () => {const saved=await chrome.runtime.sendMessage({type:'document-state'});if(!saved?.ok)throw new Error(saved?.error||'Unable to load saved documents');state.documents=saved.documents||[];state.selectedDocumentId=saved.selectedDocumentId||state.documents[0]?.id||'';documentsLoaded=true;};
  const preloadDocuments = () => documentsLoaded?Promise.resolve():documentsLoadPromise||(documentsLoadPromise=loadDocuments().finally(()=>{documentsLoadPromise=null;}));
  const saveDocumentState = async () => {const result=await chrome.runtime.sendMessage({type:'save-document-state',documents:state.documents,selectedDocumentId:state.selectedDocumentId});if(!result?.ok)throw new Error(result?.error||'Unable to save document selection');};
  let captureTaskId=0;
  const resetFeedback = () => {captureTaskId++;state.captureSelecting=false;state.captureBusy=false;state.captureRect=null;state.captureDrag=null;state.captureReturnToFeedback=false;state.feedbackAnchorRect=null;state.feedbackOpen=false;state.feedbackEntering=false;state.feedbackClosing=false;state.feedbackEditing=false;state.destinationOpen=false;state.feedbackBusy=false;state.feedbackAdding=false;state.screenshotPreparing=false;state.screenshotDataUrl='';state.feedbackQuestion='';state.feedbackNote='';state.feedbackDocumentUrl='';state.feedbackMessage='';state.feedbackStatusType='';state.feedbackResultUrl='';state.annotationTool='pen';state.annotationStart=null;state.annotationDrawing=false;state.annotationBaseDataUrl='';state.annotationHistory=[];state.annotationDraft=null;};
  let feedbackMotionTimer=0;
  const closeFeedbackAnimated = () => {if(state.feedbackClosing)return;const root=ui.feedback;if(!state.feedbackOpen||!root||root.hidden){resetFeedback();render();return;}state.feedbackClosing=true;root.classList.remove('entering');root.classList.add('closing');clearTimeout(feedbackMotionTimer);feedbackMotionTimer=setTimeout(()=>{resetFeedback();root.classList.remove('closing');render();},170);};
  let submitToastTimer=0;
  const setSubmitStatus = (status,message,timeout=0) => {clearTimeout(submitToastTimer);state.submitStatus=status;state.submitMessage=message;render();if(timeout)submitToastTimer=setTimeout(()=>{state.submitStatus='';state.submitMessage='';render();},timeout);};
  const submitFeedbackInBackground = payload => {setSubmitStatus('loading','Sending feedback to Feishu…');feishuRequest('submitFeedback',payload).then(()=>setSubmitStatus('success','Feedback sent successfully',3200)).catch(error=>setSubmitStatus('error',`Send failed: ${error.message}`,8000));};
  const clamp = (value,min,max) => Math.min(max,Math.max(min,value));
  const normalizeRect = (x1,y1,x2,y2) => ({left:Math.min(x1,x2),top:Math.min(y1,y2),width:Math.abs(x2-x1),height:Math.abs(y2-y1)});
  const beginRegionCapture = () => {captureTaskId++;state.captureReturnToFeedback=state.feedbackOpen&&!!state.screenshotDataUrl;state.feedbackOpen=false;state.feedbackMessage='';state.feedbackStatusType='';state.feedbackResultUrl='';state.screenshotPreparing=false;state.captureRect=null;state.captureDrag=null;state.captureBusy=false;state.captureSelecting=true;preloadDocuments().catch(()=>{});render();};
  const cancelRegionCapture = () => {state.captureSelecting=false;state.captureBusy=false;state.captureRect=null;state.captureDrag=null;if(state.captureReturnToFeedback){state.feedbackOpen=true;state.feedbackEntering=true;}state.captureReturnToFeedback=false;render();};
  const cropScreenshot = (dataUrl,rect,viewportWidth,viewportHeight) => new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>{const scaleX=image.naturalWidth/viewportWidth,scaleY=image.naturalHeight/viewportHeight,x=clamp(Math.round(rect.left*scaleX),0,image.naturalWidth-1),y=clamp(Math.round(rect.top*scaleY),0,image.naturalHeight-1),width=clamp(Math.round(rect.width*scaleX),1,image.naturalWidth-x),height=clamp(Math.round(rect.height*scaleY),1,image.naturalHeight-y),canvas=document.createElement('canvas');canvas.width=width;canvas.height=height;const context=canvas.getContext('2d');if(!context){reject(new Error('Unable to prepare the screenshot'));return;}context.drawImage(image,x,y,width,height,0,0,width,height);resolve(canvas.toDataURL('image/png'));};image.onerror=()=>reject(new Error('Unable to read the captured screenshot'));image.src=dataUrl;});
  const loadImage = dataUrl => new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>resolve(image);image.onerror=()=>reject(new Error('Unable to load screenshot'));image.src=dataUrl;});
  const drawArrow = (ctx,x1,y1,x2,y2,size=18) => {ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.stroke();const angle=Math.atan2(y2-y1,x2-x1);ctx.beginPath();ctx.moveTo(x2,y2);ctx.lineTo(x2-size*Math.cos(angle-.45),y2-size*Math.sin(angle-.45));ctx.lineTo(x2-size*Math.cos(angle+.45),y2-size*Math.sin(angle+.45));ctx.closePath();ctx.fill();};
  const annotationPoint = (canvas,e) => {const r=canvas.getBoundingClientRect(),sx=canvas.width/r.width,sy=canvas.height/r.height;return {x:clamp((e.clientX-r.left)*sx,0,canvas.width),y:clamp((e.clientY-r.top)*sy,0,canvas.height)};};
  const setupAnnotations = async (canvas) => {
    const baseDataUrl=state.annotationBaseDataUrl||state.screenshotDataUrl;
    if(!state.annotationBaseDataUrl)state.annotationBaseDataUrl=baseDataUrl;
    const image=await loadImage(baseDataUrl);
    if(!canvas.isConnected)return;
    canvas.width=image.naturalWidth;canvas.height=image.naturalHeight;
    const ctx=canvas.getContext('2d'),strokeWidth=Math.max(4,canvas.width/220),arrowSize=Math.max(16,strokeWidth*4.5);
    const style=()=>{ctx.strokeStyle='#fb3748';ctx.fillStyle='#fb3748';ctx.lineWidth=strokeWidth;ctx.lineCap='round';ctx.lineJoin='round';};
    const drawCommand=command=>{if(!command)return;style();const start=command.start,end=command.end;if(command.tool==='pen'){if(command.points.length<2)return;ctx.beginPath();ctx.moveTo(command.points[0].x,command.points[0].y);for(const point of command.points.slice(1))ctx.lineTo(point.x,point.y);ctx.stroke();return;}if(!start||!end)return;if(command.tool==='arrow'){drawArrow(ctx,start.x,start.y,end.x,end.y,arrowSize);return;}const left=Math.min(start.x,end.x),top=Math.min(start.y,end.y),width=Math.abs(end.x-start.x),height=Math.abs(end.y-start.y);if(command.tool==='rectangle'){ctx.strokeRect(left,top,width,height);return;}if(command.tool==='ellipse'){ctx.beginPath();ctx.ellipse(left+width/2,top+height/2,width/2,height/2,0,0,Math.PI*2);ctx.stroke();}};
    const redraw=(draft=null,save=false)=>{ctx.clearRect(0,0,canvas.width,canvas.height);ctx.drawImage(image,0,0);for(const command of state.annotationHistory)drawCommand(command);drawCommand(draft);if(save)state.screenshotDataUrl=canvas.toDataURL('image/png');};
    const finish=e=>{if(!state.annotationDrawing)return;const point=annotationPoint(canvas,e),draft=state.annotationDraft;if(draft?.tool==='pen')draft.points.push(point);else if(draft)draft.end=point;state.annotationDrawing=false;state.annotationStart=null;state.annotationDraft=null;const moved=draft?.tool==='pen'?draft.points.length>1:draft&&Math.hypot(draft.end.x-draft.start.x,draft.end.y-draft.start.y)>2;if(moved)state.annotationHistory.push(draft);redraw(null,true);};
    redraw(null,state.annotationHistory.length>0);
    canvas.onpointerdown=e=>{if(state.feedbackBusy||e.button!==0)return;const point=annotationPoint(canvas,e);state.annotationDrawing=true;state.annotationStart=point;state.annotationDraft=state.annotationTool==='pen'?{tool:'pen',points:[point]}:{tool:state.annotationTool,start:point,end:point};canvas.setPointerCapture?.(e.pointerId);redraw(state.annotationDraft);};
    canvas.onpointermove=e=>{if(!state.annotationDrawing||!state.annotationDraft)return;const point=annotationPoint(canvas,e);if(state.annotationDraft.tool==='pen')state.annotationDraft.points.push(point);else state.annotationDraft.end=point;redraw(state.annotationDraft);};
    canvas.onpointerup=e=>{finish(e);render();};
    canvas.onpointercancel=()=>{state.annotationDrawing=false;state.annotationStart=null;state.annotationDraft=null;redraw();};
  };
  const undoAnnotation = () => {if(!state.annotationHistory.length)return false;state.annotationHistory.pop();state.annotationDrawing=false;state.annotationStart=null;state.annotationDraft=null;return true;};
  const confirmRegionCapture = async () => {const rect=state.captureRect;if(!rect||rect.width<8||rect.height<8||state.captureBusy)return;const taskId=captureTaskId,viewportWidth=innerWidth,viewportHeight=innerHeight;state.feedbackAnchorRect={...rect};state.captureBusy=true;paintCaptureOverlay();host.style.visibility='hidden';try{await new Promise(resolve=>requestAnimationFrame(()=>setTimeout(resolve,0)));const result=await chrome.runtime.sendMessage({type:'capture-visible-tab'});if(taskId!==captureTaskId)return;if(!result?.ok)throw new Error(result?.error||'Unable to capture this tab');state.captureSelecting=false;state.captureRect=null;state.captureDrag=null;state.captureReturnToFeedback=false;state.captureBusy=false;state.feedbackOpen=true;state.feedbackEntering=true;state.screenshotPreparing=true;state.screenshotDataUrl='';state.annotationBaseDataUrl='';state.annotationHistory=[];state.annotationDraft=null;state.annotationDrawing=false;const hasDestination=state.documents.some(item=>item.id===state.selectedDocumentId);state.destinationOpen=!state.feedbackSeen||!hasDestination;state.feedbackSeen=true;host.style.visibility='';render();const documentPromise=(documentsLoadPromise||preloadDocuments()).then(()=>{if(taskId!==captureTaskId||!state.feedbackOpen)return;const destinationAvailable=state.documents.some(item=>item.id===state.selectedDocumentId);state.destinationOpen=!destinationAvailable||state.destinationOpen;render();}).catch(error=>{if(taskId!==captureTaskId||!state.feedbackOpen)return;state.destinationOpen=true;state.feedbackMessage=error.message;state.feedbackStatusType='error';render();});const screenshot=await cropScreenshot(result.dataUrl,rect,viewportWidth,viewportHeight);if(taskId!==captureTaskId)return;state.screenshotDataUrl=screenshot;state.annotationBaseDataUrl=screenshot;state.screenshotPreparing=false;render();await documentPromise;}catch(error){if(taskId!==captureTaskId)return;state.captureSelecting=false;state.captureRect=null;state.captureDrag=null;state.captureReturnToFeedback=false;state.feedbackOpen=true;state.feedbackEntering=true;state.destinationOpen=true;state.screenshotPreparing=false;state.feedbackMessage=error.message;state.feedbackStatusType='error';}finally{if(taskId===captureTaskId){state.captureBusy=false;host.style.visibility='';render();}}};
  const saveDocument = async url => {const document=await feishuRequest('validateDocument',{documentUrl:url});state.documents=[document,...state.documents.filter(item=>item.id!==document.id)].slice(0,20);state.selectedDocumentId=document.id;await saveDocumentState();return document;};
  const pickColor = async () => {state.mode='eyedropper';state.hovered=null;state.selected=null;state.panelOpen=false;state.settingsOpen=false;state.eyedropperError='';render();if(!window.EyeDropper){state.eyedropperError='当前浏览器不支持原生吸管工具';render();return;}try{const result=await new EyeDropper().open();state.pickedColor=result.sRGBHex.toUpperCase();state.eyedropperError='';}catch(error){if(error?.name!=='AbortError')state.eyedropperError='取色失败，请重新尝试';}render();};
  const activateMode = mode => {state.toolbarCollapsed=false;state.toolMenu='';if(mode==='eyedropper'){clearForcedPseudo();showModeHint();pickColor();return;}if(state.mode===mode)exitMode();else{clearForcedPseudo();state.mode=mode;state.selected=null;state.hovered=null;state.hitStack=[];state.hitIndex=0;state.pinnedPanels=[];state.panelPosition=null;state.panelOpen=!['xray','guides'].includes(mode);state.closedElement=null;showModeHint();}render();};
  const setToolbarPosition=(left,top,node,width=node.offsetWidth,height=node.offsetHeight)=>{state.toolbar.left=clamp(left,0,Math.max(0,innerWidth-width));state.toolbar.top=clamp(top,0,Math.max(0,innerHeight-height));node.style.left=`${state.toolbar.left}px`;node.style.top=`${state.toolbar.top}px`;};
  const makeToolbarDraggable=node=>{node.addEventListener('pointerdown',e=>{if(e.target.closest('button'))return;e.preventDefault();hideToolTip();state.toolMenu='';if(ui.toolMenu)showNode(ui.toolMenu,false);const sx=e.clientX,sy=e.clientY,sl=state.toolbar.left,st=state.toolbar.top,width=node.offsetWidth,height=node.offsetHeight;let frame=0,latest=null,dx=0,dy=0;node.style.animation='none';node.classList.add('dragging');const paint=point=>{dx=clamp(point.clientX-sx,-sl,innerWidth-width-sl);dy=clamp(point.clientY-sy,-st,innerHeight-height-st);node.style.transform=`translate3d(${dx}px,${dy}px,0)`;};const move=m=>{latest={clientX:m.clientX,clientY:m.clientY};if(frame)return;frame=requestAnimationFrame(()=>{frame=0;const point=latest;latest=null;if(point)paint(point);});};const up=m=>{if(frame){cancelAnimationFrame(frame);frame=0;}if(latest||m)paint(latest||m);latest=null;state.toolbar.left=clamp(sl+dx,0,Math.max(0,innerWidth-width));state.toolbar.top=clamp(st+dy,0,Math.max(0,innerHeight-height));node.style.left=`${state.toolbar.left}px`;node.style.top=`${state.toolbar.top}px`;node.style.transform='';node.classList.remove('dragging');removeEventListener('pointermove',move);removeEventListener('pointerup',up);removeEventListener('pointercancel',up);};addEventListener('pointermove',move,{passive:true});addEventListener('pointerup',up);addEventListener('pointercancel',up);});};
  const arrowButton=(name,label,fn)=>{const b=document.createElement('button');b.type='button';b.className=`tool-arrow${state.toolMenu===name?' active':''}`;b.dataset.menu=name;b.setAttribute('aria-label',label);b.setAttribute('aria-expanded',String(state.toolMenu===name));b.innerHTML=icon('chevron-down');b.onclick=e=>{e.stopPropagation();hideToolTip();fn();};b.addEventListener('mouseenter',()=>showToolTip(b,label));b.addEventListener('mouseleave',hideToolTip);return b;};
  function toolMenu(){const menu=ensureNode('toolMenu','tool-menu'),visible=state.enabled&&!state.toolbarCollapsed&&!!state.toolMenu;showNode(menu,visible);if(!visible){menu.dataset.menu='';menu.replaceChildren();return;}if(menu.dataset.menu!==state.toolMenu){menu.dataset.menu=state.toolMenu;menu.replaceChildren();const add=(label,shortcut,action,iconName)=>{const button=document.createElement('button');button.type='button';if(iconName)button.innerHTML=icon(iconName);const text=document.createElement('span');text.textContent=label;const key=document.createElement('span');key.className='menu-shortcut';key.textContent=shortcut;button.append(text,key);button.onclick=()=>{state.toolMenu='';action();};menu.appendChild(button);};if(state.toolMenu==='inspect'){add('Inspect elements','I',()=>activateMode('inspect'));add('Find by CSS selector','F',()=>{state.selectorOpen=true;if(state.mode!=='inspect')activateMode('inspect');state.selectorOpen=true;render();});}else if(state.toolMenu==='capture'){add('Capture region','C',beginRegionCapture);}else if(state.toolMenu==='main'){add('Inspector settings','',()=>{state.settingsOpen=!state.settingsOpen;if(state.settingsOpen){state.feishuStatus=null;checkFeishu();}render();},'menu');add('Exit UIReview','',()=>{state.enabled=false;resetFeedback();exitMode();render();},'log-out');}}const trigger=app.querySelector('.tool-arrow[data-menu="'+state.toolMenu+'"]'),anchor=trigger?.closest('.tool-group')?.getBoundingClientRect()||app.querySelector('[data-menu-trigger="main"]')?.getBoundingClientRect()||app.querySelector('[data-settings]')?.getBoundingClientRect(),edge=8,gap=8;const menuWidth=menu.offsetWidth,menuHeight=menu.offsetHeight;const preferredLeft=anchor?.left??state.toolbar.left;const left=clamp(preferredLeft,edge,Math.max(edge,innerWidth-menuWidth-edge));const below=(anchor?.bottom??state.toolbar.top+48)+gap;const above=(anchor?.top??state.toolbar.top)-menuHeight-gap;const top=below+menuHeight<=innerHeight-edge?below:above>=edge?above:clamp((anchor?.top??innerHeight/2)-menuHeight/2,edge,Math.max(edge,innerHeight-menuHeight-edge));menu.style.left=`${left}px`;menu.style.top=`${top}px`;}
  const makeCollapsedLogoInteractive=logo=>{let gesture=null;const expand=()=>{state.toolbarCollapsed=false;state.toolMenu='';render();};const offset=(g,point)=>({x:clamp(point.clientX-g.sx,-g.sl,innerWidth-g.width-g.sl),y:clamp(point.clientY-g.sy,-g.st,innerHeight-g.height-g.st)});const paint=(g,point)=>{const next=offset(g,point);g.dx=next.x;g.dy=next.y;logo.style.transform=`translate3d(${next.x}px,${next.y}px,0) scale(.96)`;};const finish=e=>{const g=gesture;if(!g||e.pointerId!==g.pointerId)return;clearTimeout(g.timer);if(g.frame)cancelAnimationFrame(g.frame);if(g.longPress&&g.latest)paint(g,g.latest);if(g.longPress){state.toolbar.left=clamp(g.sl+g.dx,0,Math.max(0,innerWidth-g.width));state.toolbar.top=clamp(g.st+g.dy,0,Math.max(0,innerHeight-g.height));logo.style.left=`${state.toolbar.left}px`;logo.style.top=`${state.toolbar.top}px`;}logo.style.transform='';logo.classList.remove('drag-ready','dragging');gesture=null;try{logo.releasePointerCapture(e.pointerId);}catch{}const distance=Math.hypot(e.clientX-g.sx,e.clientY-g.sy);if(!g.longPress&&distance<8)expand();};logo.addEventListener('pointerdown',e=>{if(e.button!==0)return;e.preventDefault();hideToolTip();const g={pointerId:e.pointerId,sx:e.clientX,sy:e.clientY,sl:state.toolbar.left,st:state.toolbar.top,width:logo.offsetWidth,height:logo.offsetHeight,longPress:false,frame:0,latest:null,dx:0,dy:0};gesture=g;logo.setPointerCapture?.(e.pointerId);g.timer=setTimeout(()=>{if(gesture!==g)return;g.longPress=true;logo.classList.add('drag-ready');logo.style.transform='translate3d(0,0,0) scale(.96)';},220);});logo.addEventListener('pointermove',e=>{const g=gesture;if(!g||e.pointerId!==g.pointerId||!g.longPress)return;g.latest={clientX:e.clientX,clientY:e.clientY};logo.classList.remove('drag-ready');logo.classList.add('dragging');if(g.frame)return;g.frame=requestAnimationFrame(()=>{g.frame=0;const point=g.latest;g.latest=null;if(point&&gesture===g)paint(g,point);});});logo.addEventListener('pointerup',finish);logo.addEventListener('pointercancel',finish);logo.addEventListener('keydown',e=>{if(e.key!=='Enter'&&e.key!==' ')return;e.preventDefault();expand();});};
  function toolbar(){let bar=app.querySelector('.toolbar'),logo=app.querySelector('.toolbar-logo');if(state.toolbarCollapsed){hideToolTip();bar?.remove();bar=null;if(!logo){logo=document.createElement('button');logo.type='button';logo.className='toolbar-logo';logo.setAttribute('aria-label','Expand UIReview. Long press and drag to move.');const image=document.createElement('img');image.src=chrome.runtime.getURL('assets/icon.svg');image.alt='UIReview';logo.appendChild(image);makeCollapsedLogoInteractive(logo);app.appendChild(logo);}setToolbarPosition(state.toolbar.left,state.toolbar.top,logo);return;}logo?.remove();if(!bar){bar=document.createElement('div');bar.className='toolbar';const group=(mode,iconName,title,shortcut,menuName)=>{const wrap=document.createElement('div');wrap.className='tool-group';const button=btn(title,iconName,title,shortcut,()=>{hideToolTip();activateMode(mode);});button.dataset.mode=mode;button.dataset.title=title;wrap.appendChild(button);if(menuName)wrap.appendChild(arrowButton(menuName,`${title} menu`,()=>{state.toolMenu=state.toolMenu===menuName?'':menuName;render();}));bar.appendChild(wrap);};group('inspect','inspect','Inspect','I','inspect');group('typography','type','Typography','T');group('guides','ruler','Rulers','R');group('xray','panels-top-left','X-ray','X');group('eyedropper','pipette','Eyedropper','P');const captureWrap=document.createElement('div');captureWrap.className='tool-group';captureWrap.appendChild(btn('Capture feedback','crop','Screenshot Feedback','C',()=>{hideToolTip();state.toolMenu='';beginRegionCapture();}));captureWrap.appendChild(arrowButton('capture','Screenshot menu',()=>{state.toolMenu=state.toolMenu==='capture'?'':'capture';render();}));bar.appendChild(captureWrap);const sep=document.createElement('span');sep.className='sep';bar.appendChild(sep);
    const settings=btn('Menu','menu','Menu','M',()=>{hideToolTip();state.settingsOpen=false;state.toolMenu=state.toolMenu==='main'?'':'main';render();});settings.dataset.settings='true';settings.dataset.menuTrigger='main';bar.appendChild(settings);
    makeToolbarDraggable(bar);app.appendChild(bar);}for(const button of bar.querySelectorAll('[data-mode]')){const active=state.mode===button.dataset.mode;button.classList.toggle('active',active);button.setAttribute('aria-label',active?`Close ${button.dataset.title}`:button.dataset.title);}
  for(const arrow of bar.querySelectorAll('.tool-arrow')){const active=state.toolMenu===arrow.dataset.menu;arrow.classList.toggle('active',active);arrow.setAttribute('aria-expanded',String(active));}
  bar.querySelector('[data-settings]')?.classList.toggle('active',state.toolMenu==='main'||state.settingsOpen);setToolbarPosition(state.toolbar.left,state.toolbar.top,bar);}
  function selectorFinder(){const finder=ensureNode('selectorFinder','selector-finder');showNode(finder,state.enabled&&state.selectorOpen);if(finder.hidden)return;finder.style.left=`${clamp(state.toolbar.left,8,Math.max(8,innerWidth-328))}px`;finder.style.top=`${clamp(state.toolbar.top+48,8,Math.max(8,innerHeight-96))}px`;if(!finder.firstChild){finder.innerHTML='<input class="selector-input" type="text" spellcheck="false" placeholder="CSS selector, e.g. .button.primary" aria-label="CSS selector"><div class="selector-footer"><span class="selector-status"></span><button class="selector-nav previous" aria-label="Previous match"></button><button class="selector-nav next" aria-label="Next match"></button></div>';finder.querySelector('.previous').innerHTML=icon('chevron-left');finder.querySelector('.next').innerHTML=icon('chevron-right');const input=finder.querySelector('input');input.addEventListener('input',()=>{state.selectorQuery=input.value;clearTimeout(state.selectorTimer);state.selectorTimer=setTimeout(()=>runSelectorQuery(input.value),180);});input.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();runSelectorQuery(input.value);}});finder.querySelector('.previous').onclick=()=>focusSelectorMatch(state.selectorIndex-1);finder.querySelector('.next').onclick=()=>focusSelectorMatch(state.selectorIndex+1);requestAnimationFrame(()=>input.focus());}const input=finder.querySelector('input');if(input!==shadow.activeElement&&input.value!==state.selectorQuery)input.value=state.selectorQuery;const status=finder.querySelector('.selector-status');status.className=`selector-status${state.selectorError?' error':''}`;status.textContent=state.selectorError||(state.selectorMatches.length?`${state.selectorIndex+1} of ${state.selectorMatches.length}`:state.selectorQuery?'No matches':'Native CSS selector search');finder.querySelectorAll('.selector-nav').forEach(button=>button.disabled=!state.selectorMatches.length);}
  function addMeasure(x,y,length,axis,value){if(value<0||value>1200)return;const key=['measure',axis,Math.round(x),Math.round(y),Math.round(length),Math.round(value)].join(':');if([...gapLayer.children].some(node=>node.dataset.measureKey===key))return;const line=document.createElement('div');line.className=`measure distance-measure ${axis}`;line.dataset.measureKey=key;line.style.left=`${x}px`;line.style.top=`${y}px`;if(axis==='h')line.style.width=`${length}px`;else line.style.height=`${length}px`;gapLayer.appendChild(line);const label=document.createElement('div');label.className='measure-label distance-label';label.dataset.measureKey=key;label.textContent=`${Math.round(value)} px`;if(axis==='h'){label.style.left=`${Math.max(2,x+length/2-label.textContent.length*3.3)}px`;label.style.top=`${y-20}px`;}else{label.style.left=`${Math.min(innerWidth-52,x+7)}px`;label.style.top=`${Math.max(2,y+length/2-8)}px`;}gapLayer.appendChild(label);}
  function visibleRect(el){if(!el?.isConnected||el===document.body||el===document.documentElement||host.contains(el))return null;const r=rectFor(el);if(r.width<2||r.height<2||r.right<=0||r.bottom<=0||r.left>=innerWidth||r.top>=innerHeight)return null;return r;}
  const hitTest = (x,y,keepIndex=false) => {
    const previous=state.hitStack[state.hitIndex];
    const stack=document.elementsFromPoint(x,y).filter(el=>el instanceof Element&&!host.contains(el)&&el!==document.body&&el!==document.documentElement);
    state.hitStack=stack;
    state.hitIndex=keepIndex&&previous&&stack.includes(previous)?stack.indexOf(previous):Math.min(state.hitIndex,Math.max(0,stack.length-1));
    if(!keepIndex)state.hitIndex=0;
    return stack[state.hitIndex]||null;
  };
  const describeElement = el => {if(!el)return '';const tag=el.tagName.toLowerCase(),id=el.id?`#${el.id}`:'',classes=[...el.classList].filter(name=>!name.startsWith('uireview')).slice(0,2).map(name=>`.${name}`).join('');const extra=el.classList.length>2?'…':'';return `${tag}${id}${classes}${extra}`;};
  const setHoveredElement = target => {if(!target){state.hovered=null;return;}if(target===state.hovered?.element)return;state.panelOpen=state.showPanel;state.closedElement=null;state.hovered=info(target);if(state.mode==='typography'&&target===state.closedElement)state.panelOpen=false;};
  const visibleChild = element => [...(element?.children||[])].find(child=>visibleRect(child))||null;
  function deselectElement(element=state.selected?.element){clearForcedPseudo();state.selected=null;state.hovered=element?.isConnected?info(element):null;gapLayer.replaceChildren();state.panelOpen=state.showPanel;state.closedElement=null;render();}
  let pseudoDetectionId=0;
  function selectElement(element){if(!element?.isConnected)return;if(state.selected?.element===element){deselectElement(element);return;}clearForcedPseudo();state.selected=info(element);state.hovered=null;gapLayer.replaceChildren();state.panelOpen=state.showPanel;state.closedElement=null;state.detectedPseudoStates=[];const detectionId=++pseudoDetectionId;render();const detect=()=>{if(detectionId!==pseudoDetectionId||state.selected?.element!==element)return;state.detectedPseudoStates=detectPseudoStates(element);render();};if(typeof requestIdleCallback==='function')requestIdleCallback(detect,{timeout:250});else setTimeout(detect,0);}
  function navigateHierarchy(direction){const current=state.selected?.element||state.hovered?.element;if(!current)return;let target=direction==='parent'?current.parentElement:visibleChild(current);while(target&&(target===document.body||target===document.documentElement||host.contains(target)))target=direction==='parent'?target.parentElement:visibleChild(target);if(target)selectElement(target);}
  function paintBoxModel(element){
    const layer=ensureNode('boxModelLayer','box-model-layer');layer.replaceChildren();showNode(layer,state.mode==='inspect'&&!!element?.isConnected);if(layer.hidden)return;
    const r=rectFor(element),s=styleFor(element),number=name=>parseFloat(s[name])||0;
    const margin={top:number('marginTop'),right:number('marginRight'),bottom:number('marginBottom'),left:number('marginLeft')},padding={top:number('paddingTop'),right:number('paddingRight'),bottom:number('paddingBottom'),left:number('paddingLeft')},border={top:number('borderTopWidth'),right:number('borderRightWidth'),bottom:number('borderBottomWidth'),left:number('borderLeftWidth')},painted=new Set();
    const add=(kind,side,left,top,width,height,value)=>{const rounded=Math.round(value),key=[kind,side,Math.round(left),Math.round(top),Math.round(width),Math.round(height),rounded].join(':');if(value<=0||width<=0||height<=0||painted.has(key))return;painted.add(key);const zone=document.createElement('div');zone.className=`box-model-zone ${kind}`;Object.assign(zone.style,{left:`${left}px`,top:`${top}px`,width:`${width}px`,height:`${height}px`});layer.appendChild(zone);const label=document.createElement('span');label.className=`box-model-value${kind==='gap'?' gap':''}`;label.textContent=String(rounded);label.style.left=`${clamp(left+width/2,10,innerWidth-10)}px`;label.style.top=`${clamp(top+height/2,10,innerHeight-10)}px`;layer.appendChild(label);};
    add('margin','top',r.left-margin.left,r.top-margin.top,r.width+margin.left+margin.right,margin.top,margin.top);add('margin','right',r.right,r.top,margin.right,r.height,margin.right);add('margin','bottom',r.left-margin.left,r.bottom,r.width+margin.left+margin.right,margin.bottom,margin.bottom);add('margin','left',r.left-margin.left,r.top,margin.left,r.height,margin.left);
    const il=r.left+border.left,it=r.top+border.top,iw=Math.max(0,r.width-border.left-border.right),ih=Math.max(0,r.height-border.top-border.bottom),content={left:il+padding.left,top:it+padding.top,right:r.right-border.right-padding.right,bottom:r.bottom-border.bottom-padding.bottom};
    add('padding','top',il,it,iw,padding.top,padding.top);add('padding','right',r.right-border.right-padding.right,it+padding.top,padding.right,Math.max(0,ih-padding.top-padding.bottom),padding.right);add('padding','bottom',il,r.bottom-border.bottom-padding.bottom,iw,padding.bottom,padding.bottom);add('padding','left',il,it+padding.top,padding.left,Math.max(0,ih-padding.top-padding.bottom),padding.left);
    const children=[...element.children].slice(0,80).map(child=>visibleRect(child)).filter(Boolean),isFlex=s.display.includes('flex'),isGrid=s.display.includes('grid');
    if((isFlex||isGrid)&&children.length>1){
      const candidates=[],addHorizontal=(a,b)=>{const left=a.right,right=b.left,top=Math.max(content.top,a.top,b.top),bottom=Math.min(content.bottom,a.bottom,b.bottom);if(right>left&&bottom>top)candidates.push({axis:'column',left,top,width:right-left,height:bottom-top,value:right-left});},addVertical=(a,b)=>{const top=a.bottom,bottom=b.top,left=Math.max(content.left,a.left,b.left),right=Math.min(content.right,a.right,b.right);if(bottom>top&&right>left)candidates.push({axis:'row',left,top,width:right-left,height:bottom-top,value:bottom-top});};
      if(isFlex){const horizontal=s.flexDirection.startsWith('row'),ordered=[...children].sort(horizontal?(a,b)=>a.left-b.left:(a,b)=>a.top-b.top);for(let i=0;i<ordered.length-1;i++)horizontal?addHorizontal(ordered[i],ordered[i+1]):addVertical(ordered[i],ordered[i+1]);}
      else{
        const seen=new Set();
        for(const a of children.slice(0,48)){
          const right=children.filter(b=>b!==a&&b.left>=a.right-0.5&&Math.min(a.bottom,b.bottom)>Math.max(a.top,b.top)).sort((x,y)=>x.left-y.left)[0];
          const below=children.filter(b=>b!==a&&b.top>=a.bottom-0.5&&Math.min(a.right,b.right)>Math.max(a.left,b.left)).sort((x,y)=>x.top-y.top)[0];
          for(const [kind,b] of [['h',right],['v',below]]){if(!b)continue;const key=`${kind}:${Math.round(a.left)}:${Math.round(a.top)}:${Math.round(b.left)}:${Math.round(b.top)}`;if(seen.has(key))continue;seen.add(key);kind==='h'?addHorizontal(a,b):addVertical(a,b);}
        }
      }
      for(const gap of candidates.slice(0,24))add('gap',gap.axis,gap.left,gap.top,gap.width,gap.height,gap.value);
    }
    if(s.display.includes('flex')){const axis=document.createElement('span');axis.className='flex-axis';const reverse=s.flexDirection.endsWith('reverse'),horizontal=s.flexDirection.startsWith('row');axis.textContent=horizontal?(reverse?'←':'→'):(reverse?'↑':'↓');axis.style.left=`${r.left+r.width/2-7}px`;axis.style.top=`${r.top+r.height/2-7}px`;layer.appendChild(axis);}
  }
  function paintLayoutChildren(element){const layer=ensureNode('layoutChildrenLayer','layout-children-layer');layer.replaceChildren();showNode(layer,state.mode==='inspect'&&!!element?.isConnected);if(layer.hidden)return;const style=styleFor(element);if(!style.display.includes('flex')&&!style.display.includes('grid'))return;const fragment=document.createDocumentFragment();for(const child of [...element.children].slice(0,80)){const rect=visibleRect(child);if(!rect)continue;const box=document.createElement('div');box.className='layout-child';setBox(box,rect);fragment.appendChild(box);}layer.appendChild(fragment);}
  function renderSelectionGuides(element){const layer=ensureNode('selectionGuideLayer','selection-guide-layer');layer.replaceChildren();showNode(layer,state.mode==='inspect'&&!!element?.isConnected);if(layer.hidden)return;const r=rectFor(element),add=(axis,left,top,length)=>{if(length<=0)return;const guide=document.createElement('div');guide.className=`selection-guide ${axis}`;guide.style.left=`${left}px`;guide.style.top=`${top}px`;if(axis==='h')guide.style.width=`${length}px`;else guide.style.height=`${length}px`;layer.appendChild(guide);};add('h',0,r.top,Math.max(0,r.left));add('h',r.right,r.top,Math.max(0,innerWidth-r.right));add('h',0,r.bottom,Math.max(0,r.left));add('h',r.right,r.bottom,Math.max(0,innerWidth-r.right));add('v',r.left,0,Math.max(0,r.top));add('v',r.left,r.bottom,Math.max(0,innerHeight-r.bottom));add('v',r.right,0,Math.max(0,r.top));add('v',r.right,r.bottom,Math.max(0,innerHeight-r.bottom));}
  function walkStyleRules(callback){const walk=rules=>{for(const rule of [...rules]){if(rule.type===CSSRule.STYLE_RULE)callback(rule);else if(rule.cssRules)try{walk(rule.cssRules);}catch{}}};for(const sheet of [...document.styleSheets])try{walk(sheet.cssRules);}catch{}}
  function detectPseudoStates(element){const found=new Set();walkStyleRules(rule=>{for(const pseudo of ['hover','focus','active']){if(!rule.selectorText.includes(`:${pseudo}`))continue;for(const selector of rule.selectorText.split(',')){if(!selector.includes(`:${pseudo}`))continue;const base=selector.replace(/:(hover|focus|active|visited)\b/g,'');try{if(element.matches(base.trim()))found.add(pseudo);}catch{}}}});return [...found];}
  function forcePseudoState(pseudo){const element=state.selected?.element;if(!element)return;clearForcedPseudo();state.detectedPseudoStates=detectPseudoStates(element);if(!pseudo){render();return;}const attribute=`[data-uireview-force="${pseudo}"]`,rules=[];element.setAttribute('data-uireview-force',pseudo);walkStyleRules(rule=>{for(const selector of rule.selectorText.split(',')){if(!selector.includes(`:${pseudo}`))continue;const replaced=selector.replace(new RegExp(`:${pseudo}\\b`,'g'),attribute);try{if(element.matches(replaced))rules.push(`${replaced}{${rule.style.cssText}}`);}catch{}}});if(rules.length){const style=document.createElement('style');style.id='uireview-forced-pseudo';style.textContent=rules.join('\n');document.documentElement.appendChild(style);state.forcedPseudoState=pseudo;}render();}
  function renderSelectorHighlights(){const layer=ensureNode('selectorHighlightLayer','selector-highlight-layer');layer.replaceChildren();showNode(layer,state.enabled&&state.selectorOpen);if(layer.hidden)return;state.selectorMatches.forEach((element,index)=>{const r=visibleRect(element);if(!r)return;const box=document.createElement('div');box.className=`selector-highlight${index===state.selectorIndex?' active':''}`;setBox(box,r);layer.appendChild(box);});}
  function runSelectorQuery(query=state.selectorQuery){state.selectorQuery=query.trim();state.selectorError='';state.selectorMatches=[];state.selectorIndex=0;if(!state.selectorQuery){render();return;}try{state.selectorMatches=[...document.querySelectorAll(state.selectorQuery)].filter(el=>!host.contains(el));if(state.selectorMatches.length)focusSelectorMatch(0,false);}catch(error){state.selectorError='Invalid CSS selector';}render();}
  function focusSelectorMatch(index,smooth=true){if(!state.selectorMatches.length)return;state.selectorIndex=(index+state.selectorMatches.length)%state.selectorMatches.length;const element=state.selectorMatches[state.selectorIndex];element.scrollIntoView({behavior:smooth?'smooth':'auto',block:'center',inline:'center'});selectElement(element);}
  // Compare the locked selection with the live hover target, including
  // partially overlapping tracks and elements from unrelated DOM branches.
  function redline(aElement,bElement){
    if(!aElement?.isConnected||!bElement?.isConnected||aElement===bElement)return;
    const a=visibleRect(aElement),b=visibleRect(bElement);if(!a||!b)return;
    const horizontal=b.left>=a.right?{x:a.right,n:b.left-a.right}:a.left>=b.right?{x:b.right,n:a.left-b.right}:null;
    const vertical=b.top>=a.bottom?{y:a.bottom,n:b.top-a.bottom}:a.top>=b.bottom?{y:b.bottom,n:a.top-b.bottom}:null;
    if(horizontal&&horizontal.n>0){const overlapTop=Math.max(a.top,b.top),overlapBottom=Math.min(a.bottom,b.bottom),y=overlapBottom>overlapTop?(overlapTop+overlapBottom)/2:clamp((a.top+a.bottom+b.top+b.bottom)/4,8,innerHeight-8);addMeasure(horizontal.x,y,horizontal.n,'h',horizontal.n);}
    if(vertical&&vertical.n>0){const overlapLeft=Math.max(a.left,b.left),overlapRight=Math.min(a.right,b.right),x=overlapRight>overlapLeft?(overlapLeft+overlapRight)/2:clamp((a.left+a.right+b.left+b.right)/4,8,innerWidth-8);addMeasure(x,vertical.y,vertical.n,'v',vertical.n);}
    if(!horizontal&&!vertical){
      const dx=Math.max(a.left-b.right,b.left-a.right,0),dy=Math.max(a.top-b.bottom,b.top-a.bottom,0);
      if(dx>0){const x=b.left>=a.right?a.right:b.right,y=Math.max(a.top,b.top)+Math.min(a.height,b.height)/2;addMeasure(x,y,dx,'h',dx);}
      if(dy>0){const y=b.top>=a.bottom?a.bottom:b.bottom,x=Math.max(a.left,b.left)+Math.min(a.width,b.width)/2;addMeasure(x,y,dy,'v',dy);}
    }
  };
  const checkFeishu = async () => {if(state.feishuChecking)return;state.feishuChecking=true;try{state.feishuStatus=await chrome.runtime.sendMessage({type:'feishu-status'});}catch{state.feishuStatus={configured:false};}finally{state.feishuChecking=false;render();}};
  function settingsPanel(){const s=ensureNode('settings','settings');showNode(s,state.settingsOpen);if(!state.settingsOpen)return;s.replaceChildren();const title=document.createElement('h3');title.textContent='Inspector settings';const label=document.createElement('label');label.className='setting';label.innerHTML='<span>Show style panel</span><input type="checkbox" />';const input=label.querySelector('input');input.checked=state.showPanel;input.onchange=()=>{state.showPanel=input.checked;state.panelOpen=state.showPanel&&['inspect','typography'].includes(state.mode);if(!state.showPanel)state.panelPosition=null;render();};const feishu=document.createElement('div');feishu.className='feishu-setting';const dot=document.createElement('i');const status=state.feishuStatus;dot.className=`feishu-dot ${status?.configured?'ready':'warning'}`;const text=document.createElement('span');text.className='feishu-text';text.textContent=state.feishuChecking?'Checking Feishu…':status?.configured?`Feishu connected · ${status.appId}`:'Connect your Feishu app';const open=document.createElement('button');open.type='button';open.className='feishu-open';open.textContent='Configure';open.onclick=()=>chrome.runtime.sendMessage({type:'open-feishu-settings'});feishu.append(dot,text,open);s.append(title,label,feishu);const trigger=app.querySelector('[data-menu-trigger="main"]')?.getBoundingClientRect(),edge=8,gap=8,width=s.offsetWidth,height=s.offsetHeight,left=clamp(trigger?.left??state.toolbar.left,edge,Math.max(edge,innerWidth-width-edge)),below=(trigger?.bottom??state.toolbar.top+48)+gap,above=(trigger?.top??state.toolbar.top)-height-gap,top=below+height<=innerHeight-edge?below:above>=edge?above:clamp(above,edge,Math.max(edge,innerHeight-height-edge));s.style.left=`${left}px`;s.style.top=`${top}px`;if(!state.feishuStatus&&!state.feishuChecking)checkFeishu();}
  function submitToast(){const toast=ensureNode('submitToast','submit-toast');showNode(toast,!!state.submitStatus);if(!state.submitStatus)return;toast.className=`submit-toast ${state.submitStatus}`;toast.textContent=state.submitMessage;}
  function paintCaptureOverlay(){
    const root=ensureNode('captureOverlay','capture-overlay');showNode(root,state.captureSelecting);app.classList.toggle('capture-active',state.captureSelecting);if(!state.captureSelecting)return;
    if(!root.firstChild){
      root.innerHTML='<div class="capture-shade" data-shade="top"></div><div class="capture-shade" data-shade="left"></div><div class="capture-shade" data-shade="right"></div><div class="capture-shade" data-shade="bottom"></div><div class="capture-selection"><span class="capture-size"></span><i class="capture-handle" data-handle="nw"></i><i class="capture-handle" data-handle="n"></i><i class="capture-handle" data-handle="ne"></i><i class="capture-handle" data-handle="e"></i><i class="capture-handle" data-handle="se"></i><i class="capture-handle" data-handle="s"></i><i class="capture-handle" data-handle="sw"></i><i class="capture-handle" data-handle="w"></i></div><div class="capture-instructions">Drag to select an area · release to capture · Esc to cancel</div><div class="capture-controls"><button type="button" class="capture-cancel">Cancel</button></div>';
      root.querySelector('.capture-cancel').onclick=e=>{e.stopPropagation();cancelRegionCapture();};
      const confirmButton=root.querySelector('.capture-confirm');if(confirmButton)confirmButton.onclick=e=>{e.stopPropagation();confirmRegionCapture();};
      root.addEventListener('pointerdown',e=>{
        if(e.button!==0||e.target.closest('.capture-controls'))return;e.preventDefault();e.stopPropagation();
        const handle=e.target.closest('.capture-handle')?.dataset.handle,selection=e.target.closest('.capture-selection'),startX=e.clientX,startY=e.clientY,rect=state.captureRect?{...state.captureRect}:null;
        state.captureDrag={kind:handle?'resize':selection&&rect?'move':'draw',handle:handle||'',startX,startY,rect};
        if(state.captureDrag.kind==='draw')state.captureRect={left:startX,top:startY,width:0,height:0};
        root.setPointerCapture?.(e.pointerId);paintCaptureOverlay();
      });
      root.addEventListener('pointermove',e=>{
        const drag=state.captureDrag;if(!drag)return;e.preventDefault();
        if(drag.kind==='draw'){
          const next=normalizeRect(drag.startX,drag.startY,clamp(e.clientX,0,innerWidth),clamp(e.clientY,0,innerHeight));state.captureRect=next;
        }else if(drag.kind==='move'){
          state.captureRect={...drag.rect,left:clamp(drag.rect.left+e.clientX-drag.startX,0,innerWidth-drag.rect.width),top:clamp(drag.rect.top+e.clientY-drag.startY,0,innerHeight-drag.rect.height)};
        }else{
          let left=drag.rect.left,top=drag.rect.top,right=left+drag.rect.width,bottom=top+drag.rect.height;const h=drag.handle;
          if(h.includes('w'))left=clamp(e.clientX,0,right-8);if(h.includes('e'))right=clamp(e.clientX,left+8,innerWidth);if(h.includes('n'))top=clamp(e.clientY,0,bottom-8);if(h.includes('s'))bottom=clamp(e.clientY,top+8,innerHeight);state.captureRect={left,top,width:right-left,height:bottom-top};
        }
        paintCaptureOverlay();
      });
      const endDrag=e=>{const drag=state.captureDrag;if(!drag)return;state.captureDrag=null;try{root.releasePointerCapture?.(e.pointerId);}catch{}if(drag.kind==='draw'&&state.captureRect?.width>=8&&state.captureRect?.height>=8){confirmRegionCapture();return;}paintCaptureOverlay();};
      root.addEventListener('pointerup',endDrag);root.addEventListener('pointercancel',endDrag);
    }
    const rect=state.captureRect,selection=root.querySelector('.capture-selection'),controls=root.querySelector('.capture-controls'),confirm=root.querySelector('.capture-confirm'),shades=[...root.querySelectorAll('.capture-shade')];
    if(!rect){selection.hidden=true;if(controls)controls.hidden=true;Object.assign(shades[0].style,{left:'0px',top:'0px',width:`${innerWidth}px`,height:`${innerHeight}px`});for(let i=1;i<shades.length;i++)shades[i].style.cssText='display:none';return;}
    selection.hidden=false;if(controls)controls.hidden=false;setBox(selection,rect);selection.querySelector('.capture-size').textContent=`${Math.round(rect.width)} × ${Math.round(rect.height)}`;selection.querySelector('.capture-size').style.top=rect.top<34?'6px':'-27px';
    const right=rect.left+rect.width,bottom=rect.top+rect.height,areas=[[0,0,innerWidth,rect.top],[0,rect.top,rect.left,rect.height],[right,rect.top,Math.max(0,innerWidth-right),rect.height],[0,bottom,innerWidth,Math.max(0,innerHeight-bottom)]];
    shades.forEach((shade,index)=>{const [left,top,width,height]=areas[index];shade.style.cssText=`left:${left}px;top:${top}px;width:${width}px;height:${height}px`;});
    const controlWidth=88,controlHeight=40;let controlLeft=clamp(rect.left+rect.width-controlWidth,8,innerWidth-controlWidth-8),controlTop=bottom+10;if(controlTop+controlHeight>innerHeight-8)controlTop=Math.max(8,rect.top-controlHeight-10);if(controls){controls.style.left=`${controlLeft}px`;controls.style.top=`${controlTop}px`;if(confirm){confirm.disabled=state.captureBusy||rect.width<8||rect.height<8;confirm.textContent=state.captureBusy?'Capturing…':'Capture';}}
  }
  function feedbackDialog(){
    const root=ensureNode('feedback','feedback-backdrop');showNode(root,state.feedbackOpen);if(!state.feedbackOpen)return;root.classList.remove('stacked');root.classList.toggle('entering',state.feedbackEntering);root.classList.toggle('closing',state.feedbackClosing);
    root.replaceChildren();
    const dialog=document.createElement('div');dialog.className='feedback-dialog';dialog.tabIndex=-1;dialog.setAttribute('role','dialog');dialog.setAttribute('aria-modal','true');dialog.setAttribute('aria-label','Send feedback to Feishu');root.appendChild(dialog);
    const cardHead=(label,action,ariaLabel)=>{const head=document.createElement('div');head.className='feedback-head';const title=document.createElement('strong');title.textContent=label;const close=document.createElement('button');close.className='feedback-close';close.type='button';close.setAttribute('aria-label',ariaLabel);close.innerHTML=icon('close');close.onclick=action;head.append(title,close);return {head,close};};
    const close=()=>{closeFeedbackAnimated();};
    const closeDestination=()=>{const card=dialog.querySelector('.feedback-destination-card');if(!card){state.destinationOpen=false;render();return;}card.classList.add('closing');let finished=false;const finish=()=>{if(finished)return;finished=true;state.destinationOpen=false;render();};card.addEventListener('animationend',finish,{once:true});setTimeout(finish,220);};
    const questionCard=document.createElement('section');questionCard.className='feedback-card feedback-question-card';const questionHead=cardHead('Question',close,'Close feedback');questionCard.appendChild(questionHead.head);
    const inputs=document.createElement('div');inputs.className=`feedback-inputs${state.feedbackEditing?' is-editing':''}`;const question=document.createElement('textarea');question.placeholder='Describe the issue...';question.value=state.feedbackQuestion;question.disabled=state.feedbackBusy;question.setAttribute('aria-label','Question');const note=document.createElement('input');note.type='text';note.placeholder='Optional note...';note.value=state.feedbackNote;note.disabled=state.feedbackBusy;note.setAttribute('aria-label','Optional note');inputs.append(question,note);questionCard.appendChild(inputs);
    const selectedDocument=state.documents?.find(item=>item.id===state.selectedDocumentId);if(selectedDocument){const connected=document.createElement('div');connected.className='feedback-connected-document';connected.title=selectedDocument.name||'Connected document';const name=document.createElement('span');name.textContent=selectedDocument.name||'Connected document';connected.appendChild(name);questionCard.appendChild(connected);}
    const status=document.createElement('div');status.className=`feedback-status${state.feedbackStatusType?` ${state.feedbackStatusType}`:''}`;status.textContent=state.feedbackMessage;questionCard.appendChild(status);
    const actions=document.createElement('div');actions.className='feedback-actions';const destinationToggle=document.createElement('button');destinationToggle.className=`feedback-destination-toggle${state.destinationOpen?' active':''}`;destinationToggle.type='button';destinationToggle.setAttribute('aria-label','Destination document');destinationToggle.setAttribute('aria-expanded',String(state.destinationOpen));destinationToggle.innerHTML=icon('scroll-text');const send=document.createElement('button');send.className='feedback-send';send.type='button';send.disabled=state.feedbackBusy||state.feedbackAdding||state.screenshotPreparing||!!state.feedbackResultUrl;send.textContent=state.feedbackBusy?'Sending…':state.screenshotPreparing?'Preparing…':'Send';actions.append(destinationToggle,send);questionCard.appendChild(actions);dialog.appendChild(questionCard);
    let select=null,docUrl=null;
    if(state.destinationOpen){const destinationCard=document.createElement('section');destinationCard.className='feedback-card feedback-destination-card';const destinationHead=cardHead('Destination document',closeDestination,'Close destination document');destinationCard.appendChild(destinationHead.head);const fields=document.createElement('div');fields.className='feedback-destination-fields';const selectWrap=document.createElement('div');selectWrap.className='feedback-select-wrap';select=document.createElement('select');select.className='feedback-doc';select.setAttribute('aria-label','Select a document');const empty=document.createElement('option');empty.value='';empty.textContent='Select a document';select.appendChild(empty);for(const saved of state.documents){const option=document.createElement('option');option.value=saved.id;option.textContent=saved.name||'Untitled Feishu document';option.selected=saved.id===state.selectedDocumentId;select.appendChild(option);}select.disabled=state.feedbackBusy||state.feedbackAdding;const chevron=document.createElement('span');chevron.className='feedback-select-icon';chevron.innerHTML=icon('chevron-down');selectWrap.append(select,chevron);docUrl=document.createElement('input');docUrl.className='feedback-doc-url';docUrl.type='url';docUrl.placeholder=state.feedbackAdding?'Adding document...':'Paste the document link';docUrl.value=state.feedbackDocumentUrl;docUrl.disabled=state.feedbackBusy||state.feedbackAdding;docUrl.setAttribute('aria-label','Paste the document link');fields.append(selectWrap,docUrl);destinationCard.appendChild(fields);dialog.appendChild(destinationCard);}
    if(state.screenshotDataUrl||state.screenshotPreparing){const shotColumn=document.createElement('div');shotColumn.className='feedback-shot-column';const shotWrap=document.createElement('div');shotWrap.className='feedback-shot-wrap';if(state.screenshotDataUrl){const canvas=document.createElement('canvas');canvas.className='feedback-canvas';canvas.setAttribute('aria-label','Screenshot annotation canvas');const retake=document.createElement('button');retake.className='feedback-retake';retake.type='button';retake.textContent='Retake';retake.disabled=state.feedbackBusy;retake.onclick=()=>beginRegionCapture();shotWrap.append(canvas,retake);shotColumn.appendChild(shotWrap);const annotateBar=document.createElement('div');annotateBar.className='feedback-annotate-bar';const toolButton=(tool,iconName,label)=>{const button=document.createElement('button');button.type='button';button.innerHTML=icon(iconName);button.title=label;button.setAttribute('aria-label',label);button.classList.toggle('active',state.annotationTool===tool);button.onclick=()=>{state.annotationTool=tool;state.annotationDrawing=false;state.annotationDraft=null;render();};return button;};const pen=toolButton('pen','pen','Pen');const arrow=toolButton('arrow','arrow-up-right','Arrow');const rectangle=toolButton('rectangle','square','Rectangle');const ellipse=toolButton('ellipse','circle','Ellipse');const divider=document.createElement('span');divider.className='annotation-divider';const undo=document.createElement('button');undo.type='button';undo.innerHTML=icon('undo');undo.title='Undo (Ctrl/Cmd + Z)';undo.setAttribute('aria-label','Undo last annotation');undo.disabled=!state.annotationHistory.length||state.feedbackBusy;undo.onclick=()=>{if(undoAnnotation())render();};annotateBar.append(pen,arrow,rectangle,ellipse,divider,undo);const help=document.createElement('span');help.className='annotation-help';help.textContent=state.annotationHistory.length?`${state.annotationHistory.length} annotation${state.annotationHistory.length===1?'':'s'}`:'Drag to annotate';annotateBar.appendChild(help);shotColumn.appendChild(annotateBar);root.appendChild(shotColumn);setupAnnotations(canvas).catch(error=>{state.feedbackMessage=error.message;state.feedbackStatusType='error';render();});}else{const placeholder=document.createElement('div');placeholder.className='feedback-shot-placeholder';placeholder.setAttribute('role','status');placeholder.setAttribute('aria-label','Preparing screenshot');const label=document.createElement('span');label.textContent='Preparing screenshot…';placeholder.appendChild(label);shotWrap.appendChild(placeholder);shotColumn.appendChild(shotWrap);root.appendChild(shotColumn);}}else{state.feedbackMessage='Screenshot unavailable. Close this dialog and try again.';state.feedbackStatusType='error';status.textContent=state.feedbackMessage;status.className='feedback-status error';}
    const shotColumn=root.querySelector('.feedback-shot-column');
    if(shotColumn){const anchor=state.feedbackAnchorRect||{left:innerWidth/2-180,top:innerHeight/2-140,width:360,height:280};const gap=12,formWidth=336,edge=12,toolbarHeight=46,formHeight=state.destinationOpen?394:230;const maxShotWidth=Math.min(560,innerWidth-edge*2),maxShotHeight=Math.min(420,Math.max(120,innerHeight-toolbarHeight-edge*2));const aspect=Math.max(.1,(anchor.width||360)/(anchor.height||280));let shotWidth=Math.min(maxShotWidth,Math.max(180,anchor.width||360)),shotHeight=shotWidth/aspect;if(shotHeight>maxShotHeight){shotHeight=maxShotHeight;shotWidth=Math.min(maxShotWidth,shotHeight*aspect);}if(shotHeight<120){shotHeight=120;shotWidth=Math.min(maxShotWidth,shotHeight*aspect);}const totalWidth=shotWidth+gap+formWidth;const canSitBeside=totalWidth<=innerWidth-edge*2&&formHeight<=innerHeight-edge*2;if(canSitBeside){const groupLeft=clamp(anchor.left,edge,innerWidth-totalWidth-edge);const shotTop=clamp(anchor.top,edge,Math.max(edge,innerHeight-shotHeight-toolbarHeight-edge));const dialogTop=clamp(anchor.top,edge,Math.max(edge,innerHeight-formHeight-edge));shotColumn.style.left=`${groupLeft}px`;shotColumn.style.top=`${shotTop}px`;shotColumn.style.width=`${shotWidth}px`;shotColumn.style.height=`${shotHeight+toolbarHeight}px`;dialog.style.left=`${groupLeft+shotWidth+gap}px`;dialog.style.top=`${dialogTop}px`;}else{root.classList.add('stacked');dialog.style.left='';dialog.style.top='';shotColumn.style.left='';shotColumn.style.top='';shotColumn.style.width='';shotColumn.style.height='';}}
    root.onclick=e=>{if(e.target===root&&!state.feedbackBusy)close();};root.onkeydown=e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='z'&&!e.shiftKey&&!(e.target instanceof HTMLInputElement)&&!(e.target instanceof HTMLTextAreaElement)){e.preventDefault();e.stopPropagation();if(undoAnnotation())render();return;}if(e.key==='Escape'){e.preventDefault();e.stopPropagation();close();}};inputs.onclick=()=>{if(!state.feedbackEditing){state.feedbackEditing=true;inputs.classList.add('is-editing');requestAnimationFrame(()=>question.focus());}};question.onfocus=note.onfocus=()=>{state.feedbackEditing=true;inputs.classList.add('is-editing');};question.oninput=()=>{state.feedbackQuestion=question.value;};note.oninput=()=>{state.feedbackNote=note.value;};destinationToggle.onclick=()=>{state.feedbackQuestion=question.value;state.feedbackNote=note.value;state.feedbackMessage='';state.feedbackStatusType='';if(state.destinationOpen)closeDestination();else{state.destinationOpen=true;render();}};
    if(select&&docUrl){select.onchange=()=>{state.selectedDocumentId=select.value;state.feedbackMessage='';state.feedbackStatusType='';saveDocumentState().catch(error=>{state.feedbackMessage=error.message;state.feedbackStatusType='error';render();});render();};docUrl.oninput=()=>{state.feedbackDocumentUrl=docUrl.value;};const addDocument=async()=>{state.feedbackDocumentUrl=docUrl.value.trim();if(!state.feedbackDocumentUrl||state.feedbackAdding)return;state.feedbackAdding=true;state.feedbackMessage='';state.feedbackStatusType='';render();try{const saved=await saveDocument(state.feedbackDocumentUrl);state.feedbackDocumentUrl='';state.feedbackMessage=`Selected: ${saved.name}`;state.feedbackStatusType='success';}catch(error){state.feedbackMessage=error.message;state.feedbackStatusType='error';}finally{state.feedbackAdding=false;render();}};docUrl.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();addDocument();}};docUrl.onpaste=()=>setTimeout(addDocument,0);}
    send.onclick=()=>{state.feedbackQuestion=question.value;state.feedbackNote=note.value;const destination=state.documents.find(item=>item.id===state.selectedDocumentId);if(!state.feedbackQuestion.trim()){state.feedbackEditing=true;state.feedbackMessage='Enter a question before sending.';state.feedbackStatusType='error';render();return;}if(!destination){state.destinationOpen=true;state.feedbackMessage='Select or paste a Feishu document link.';state.feedbackStatusType='error';render();return;}if(!state.screenshotDataUrl){state.feedbackMessage='Capture a screenshot first.';state.feedbackStatusType='error';render();return;}const payload={question:state.feedbackQuestion.trim(),note:state.feedbackNote.trim(),documentId:destination.id,documentUrl:destination.url,pageTitle:window.document.title||'Untitled page',pageUrl:location.href,viewport:`${innerWidth} × ${innerHeight}`,screenshotDataUrl:state.screenshotDataUrl};resetFeedback();submitFeedbackInBackground(payload);beginRegionCapture();};
    dialog.onkeydown=e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='z'&&!e.shiftKey&&!(e.target instanceof HTMLInputElement)&&!(e.target instanceof HTMLTextAreaElement)){e.preventDefault();e.stopPropagation();if(undoAnnotation())render();return;}if(e.key==='Escape'){e.preventDefault();e.stopPropagation();close();return;}if(e.key==='Enter'&&!e.shiftKey&&e.target!==docUrl&&e.target!==select){e.preventDefault();e.stopPropagation();send.click();}};if(state.feedbackEntering){clearTimeout(feedbackMotionTimer);feedbackMotionTimer=setTimeout(()=>{state.feedbackEntering=false;if(root.isConnected)root.classList.remove('entering');},280);}if(!state.feedbackEditing)requestAnimationFrame(()=>dialog.focus({preventScroll:true}));
  }
  function colorPanel(){const p=ensureNode('colorPanel','color-panel'),visible=state.mode==='eyedropper'&&(state.pickedColor||state.eyedropperError);showNode(p,!!visible);if(!visible)return;p.style.left=`${Math.min(innerWidth-236,Math.max(8,state.toolbar.left))}px`;p.style.top=`${Math.min(innerHeight-170,state.toolbar.top+52)}px`;const key=`${state.pickedColor}:${state.eyedropperError}`;if(p.dataset.key===key)return;p.dataset.key=key;if(state.eyedropperError){p.innerHTML=`<div class="color-error">${state.eyedropperError}</div>`;return;}const hex=state.pickedColor,{r,g,b}=hexToRgb(hex);p.innerHTML=`<div class="color-preview" style="background:${hex}"></div><button class="color-copy" type="button" aria-label="Copy ${hex}"><span>${hex}</span><span class="copy-note">点击复制</span></button><div class="color-rgb"><span>RGB</span><span>rgb(${r}, ${g}, ${b})</span></div>`;const copy=p.querySelector('.color-copy'),note=p.querySelector('.copy-note');copy.onclick=async e=>{e.stopPropagation();try{await navigator.clipboard.writeText(hex);note.textContent='已复制';setTimeout(()=>{if(note.isConnected)note.textContent='点击复制';},1200);}catch{note.textContent='复制失败';}};}
  const rulerCanvas = (key,className) => {if(!ui[key]){ui[key]=document.createElement('canvas');ui[key].className=className;ui[key].hidden=true;app.appendChild(ui[key]);}return ui[key];};
  const prepareRuler = (canvas,width,height) => {const dpr=Math.max(1,devicePixelRatio||1),pixelWidth=Math.round(width*dpr),pixelHeight=Math.round(height*dpr);if(canvas.width!==pixelWidth||canvas.height!==pixelHeight){canvas.width=pixelWidth;canvas.height=pixelHeight;canvas.style.width=`${width}px`;canvas.style.height=`${height}px`;}const ctx=canvas.getContext('2d');ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,width,height);ctx.fillStyle='#fafafa';ctx.fillRect(0,0,width,height);ctx.strokeStyle='#b8b8b8';ctx.fillStyle='#999';ctx.lineWidth=1;ctx.font='10px Inter,ui-sans-serif,system-ui';return ctx;};
  function renderRulers(){const visible=state.enabled&&state.mode==='guides',top=rulerCanvas('rulerTop','pixel-ruler top'),left=rulerCanvas('rulerLeft','pixel-ruler left'),corner=ensureNode('rulerCorner','ruler-corner');showNode(top,visible);showNode(left,visible);showNode(corner,visible);if(!visible)return;const topWidth=Math.max(1,innerWidth-28),leftHeight=Math.max(1,innerHeight-28),step=10,major=100;let ctx=prepareRuler(top,topWidth,28);ctx.beginPath();ctx.moveTo(0,27.5);ctx.lineTo(topWidth,27.5);const firstX=Math.ceil((scrollX+28)/step)*step;for(let value=firstX;value<=scrollX+innerWidth;value+=step){const x=Math.round(value-scrollX-28)+.5,isMajor=value%major===0,isMid=value%50===0;ctx.moveTo(x,28);ctx.lineTo(x,28-(isMajor?9:isMid?6:4));if(isMajor){const label=String(value),width=ctx.measureText(label).width;ctx.fillText(label,x-width/2,11);}}ctx.stroke();ctx=prepareRuler(left,28,leftHeight);ctx.beginPath();ctx.moveTo(27.5,0);ctx.lineTo(27.5,leftHeight);const firstY=Math.ceil((scrollY+28)/step)*step;for(let value=firstY;value<=scrollY+innerHeight;value+=step){const y=Math.round(value-scrollY-28)+.5,isMajor=value%major===0,isMid=value%50===0;ctx.moveTo(28,y);ctx.lineTo(28-(isMajor?9:isMid?6:4),y);if(isMajor){const label=String(value),width=ctx.measureText(label).width;ctx.save();ctx.translate(10,y+width/2);ctx.rotate(-Math.PI/2);ctx.fillText(label,0,0);ctx.restore();}}ctx.stroke();}
  function syncXrayClass(){if(document.body)document.body.classList.toggle('xray-mode',state.enabled&&state.mode==='xray');}
  const panelRow=(group,keyText,valueText,{tone='violet',color=''}={})=>{const line=document.createElement('div'),key=document.createElement('span'),value=document.createElement('span');line.className='panel-row';key.className='key';key.textContent=`${keyText}:`;value.className=`value ${color?'swatch-value':tone}`;value.title=String(valueText);if(color){const swatch=document.createElement('i'),text=document.createElement('span');swatch.className='swatch';swatch.classList.toggle('has-border',cssColorToHex(color)==='#FFFFFF');swatch.style.background=color;text.className='swatch-text';text.textContent=String(valueText);value.append(swatch,text);}else value.textContent=String(valueText);line.append(key,value);group.appendChild(line);};
  const panelBlock=(content,title,rows)=>{const block=document.createElement('section'),heading=document.createElement('div'),group=document.createElement('div');block.className='panel-block';heading.className='section-title';heading.textContent=title;group.className='panel-group';for(const [key,value,options] of rows)panelRow(group,key,value,options);block.append(heading,group);content.appendChild(block);return block;};
  function fillInspectorPanel(p,selected,{pinned=false,onClose}={}){const t=state.mode==='typography',fresh=selected.style?selected:info(selected.element),{rect,style}=fresh;p.replaceChildren();const head=document.createElement('h3'),close=document.createElement('button'),content=document.createElement('div');close.className='panel-close';close.type='button';close.setAttribute('aria-label',pinned?'Close pinned details':'Close details');close.textContent='×';close.onclick=e=>{e.stopPropagation();onClose?.();};head.appendChild(close);content.className='panel-content';p.append(head,content);const gap=style.gap==='normal'?'0px':style.gap,background=cssColorToHex(style.backgroundColor),textColor=cssColorToHex(style.color);if(!t){panelBlock(content,'Border-box',[['width',`${Math.round(rect.width)}px`,{tone:'red'}],['height',`${Math.round(rect.height)}px`],['display',style.display],['gap',gap],['padding',style.padding,{tone:'red'}],['box-sizing',style.boxSizing]]);panelBlock(content,'Style',[['roder-radius',style.borderRadius],['barkground',background,{color:style.backgroundColor}],['box-shadow',style.boxShadow||'none']]);}const typographyRows=t?[['color',textColor,{color:style.color}]]:[];typographyRows.push(['font-family',style.fontFamily,{tone:'red'}],['font-size',style.fontSize,{tone:'red'}],['font-weight',style.fontWeight],['line-height',style.lineHeight],['text-align',style.textAlign]);panelBlock(content,'Typography',typographyRows);if(!t&&!pinned&&selected.element===state.selected?.element&&state.detectedPseudoStates.length){const block=panelBlock(content,'State',[]),group=block.querySelector('.panel-group');for(const pseudo of state.detectedPseudoStates){const button=document.createElement('button');button.className=`pseudo-chip${state.forcedPseudoState===pseudo?' active':''}`;button.textContent=`:${pseudo}`;button.onclick=e=>{e.stopPropagation();forcePseudoState(state.forcedPseudoState===pseudo?'':pseudo);};group.appendChild(button);}}return {head,rect};}
  const makeDraggable=(node,head,getPosition,setPosition)=>{head.onpointerdown=e=>{if(e.target.closest('button'))return;e.preventDefault();const start=getPosition(),sx=e.clientX,sy=e.clientY,width=node.offsetWidth,height=node.offsetHeight,maxLeft=Math.max(8,innerWidth-width-8),maxTop=Math.max(8,innerHeight-height-8);let frame=0,latest=null,dx=0,dy=0;node.style.left=`${start.left}px`;node.style.top=`${start.top}px`;node.style.transform='';node.style.willChange='transform';node.classList.add('dragging');const paint=point=>{dx=clamp(start.left+point.clientX-sx,8,maxLeft)-start.left;dy=clamp(start.top+point.clientY-sy,8,maxTop)-start.top;node.style.transform=`translate3d(${dx}px,${dy}px,0)`;};const move=m=>{latest={clientX:m.clientX,clientY:m.clientY};if(frame)return;frame=requestAnimationFrame(()=>{frame=0;const point=latest;latest=null;if(point)paint(point);});};const up=m=>{if(frame){cancelAnimationFrame(frame);frame=0;}if(latest||m)paint(latest||m);latest=null;const next={left:start.left+dx,top:start.top+dy};node.style.left=`${next.left}px`;node.style.top=`${next.top}px`;node.style.transform='';node.style.willChange='';node.classList.remove('dragging');setPosition(next);removeEventListener('pointermove',move);removeEventListener('pointerup',up);removeEventListener('pointercancel',up);};addEventListener('pointermove',move,{passive:true});addEventListener('pointerup',up);addEventListener('pointercancel',up);};};
  const positionLivePanel=(p,x,y)=>{p.style.left='0';p.style.top='0';p.style.transform=`translate3d(${x}px,${y}px,0)`;};
  const moveLivePanel=()=>{if(state.panelPosition)return;const p=ui.panel;if(!p||p.hidden)return;const width=240,height=p._uiReviewHeight||300,edge=8,right=state.pointer.x+16,left=state.pointer.x-width-16,x=right+width<=innerWidth-edge?right:left>=edge?left:Math.max(edge,innerWidth-width-edge),y=clamp(state.pointer.y+16,edge,Math.max(edge,innerHeight-height-edge));positionLivePanel(p,x,y);};
  function panel(){const selected=state.hovered||state.selected,p=ensureNode('panel','panel');if(!selected?.element?.isConnected||!state.panelOpen||!state.showPanel){showNode(p,false);return;}showNode(p,true);const rect=selected.rect,panelW=240,edge=8;let x,y;if(state.panelPosition){x=state.panelPosition.left;y=state.panelPosition.top;}else{const pointerX=state.pointer.x||rect.right,pointerY=state.pointer.y||rect.top,right=pointerX+16,left=pointerX-panelW-16;x=right+panelW<=innerWidth-edge?right:left>=edge?left:Math.max(edge,innerWidth-panelW-edge);y=clamp(pointerY+16,edge,Math.max(edge,innerHeight-320));}x=clamp(x,edge,Math.max(edge,innerWidth-panelW-edge));y=clamp(y,edge,Math.max(edge,innerHeight-300-edge));const fixed=!!state.panelPosition;p.classList.toggle('position-fixed',fixed);if(fixed){p.style.transform='';p.style.left=`${x}px`;p.style.top=`${y}px`;makeDraggable(p,p,()=>state.panelPosition,position=>{state.panelPosition=position;});}else{p.onpointerdown=null;p.classList.remove('dragging');positionLivePanel(p,x,y);}const panelKey=`${state.mode}:${state.forcedPseudoState}:${state.detectedPseudoStates.join(',')}`;if(p._uiReviewElement===selected.element&&p.dataset.panelKey===panelKey)return;p._uiReviewElement=selected.element;p.dataset.panelKey=panelKey;fillInspectorPanel(p,selected,{onClose:()=>{state.panelOpen=false;state.closedElement=selected.element;render();}});p._uiReviewHeight=p.offsetHeight;}
  function togglePanelPosition(){const p=ui.panel,selected=state.hovered||state.selected;if(!state.showPanel||!p||p.hidden||!selected?.element?.isConnected)return;if(state.panelPosition)state.panelPosition=null;else{const rect=p.getBoundingClientRect();state.panelPosition={left:rect.left,top:rect.top};}render();}
  function pinCurrentPanel(){const selected=state.hovered?.element?.isConnected?state.hovered:state.selected;if(!selected?.element||state.pinnedPanels.some(pin=>pin.element===selected.element))return;const offset=state.pinnedPanels.length*18;state.pinnedPanels.push({id:state.nextPinId++,element:selected.element,position:{left:clamp((state.panelPosition?.left??state.pointer.x+16)+offset,8,Math.max(8,innerWidth-248)),top:clamp((state.panelPosition?.top??state.pointer.y+16)+offset,8,Math.max(8,innerHeight-320))}});render();}
  function renderPinnedPanels(){state.pinnedPanels=state.pinnedPanels.filter(pin=>pin.element?.isConnected);const existing=new Map([...app.querySelectorAll('.panel.pinned')].map(node=>[Number(node.dataset.pinId),node]));for(const pin of state.pinnedPanels){let p=existing.get(pin.id);if(!p){p=document.createElement('div');p.className='panel pinned';p.dataset.pinId=String(pin.id);app.appendChild(p);}existing.delete(pin.id);p.style.left=`${pin.position.left}px`;p.style.top=`${pin.position.top}px`;fillInspectorPanel(p,{element:pin.element},{pinned:true,onClose:()=>{state.pinnedPanels=state.pinnedPanels.filter(item=>item.id!==pin.id);render();}});makeDraggable(p,p,()=>pin.position,position=>{pin.position=position;});}for(const node of existing.values())node.remove();}
  function render(){clearGeometryCache();syncXrayClass();app.style.display=state.enabled?'':'none';if(!state.enabled){app.classList.remove('capture-active');gapLayer.replaceChildren();ensureNode('boxModelLayer','box-model-layer').replaceChildren();ensureNode('layoutChildrenLayer','layout-children-layer').replaceChildren();ensureNode('selectionGuideLayer','selection-guide-layer').replaceChildren();ensureNode('selectorHighlightLayer','selector-highlight-layer').replaceChildren();app.querySelectorAll('.panel.pinned').forEach(node=>node.remove());renderRulers();return;}toolbar();toolMenu();const hoverBox=ensureNode('hoverBox','box hover-box'),hoverLabel=ensureNode('hoverLabel','label'),selectedBox=ensureNode('selectedBox','box selected-box');const canHover=state.hovered?.element?.isConnected&&state.mode==='inspect';showNode(hoverBox,!!canHover);showNode(hoverLabel,!!canHover);if(canHover){state.hovered.rect=rectFor(state.hovered.element);const {rect}=state.hovered,hoverStyle=state.hovered.style,hasPadding=['paddingTop','paddingRight','paddingBottom','paddingLeft'].some(name=>(parseFloat(hoverStyle[name])||0)>0);setBox(hoverBox,rect);hoverBox.classList.toggle('has-padding',hasPadding);hoverLabel.style.transform=`translate3d(${clamp(rect.left,4,Math.max(4,innerWidth-240))}px,${Math.max(4,rect.top-18)}px,0)`;const name=describeElement(state.hovered.element),size=`${Math.round(rect.width)} x ${Math.round(rect.height)}`,key=`${name}|${size}`;if(hoverLabel.dataset.labelKey!==key){hoverLabel.dataset.labelKey=key;const elementName=document.createElement('span'),elementSize=document.createElement('span');elementName.textContent=name;elementSize.className='label-size';elementSize.textContent=size;hoverLabel.replaceChildren(elementName,elementSize);}}else hoverBox.classList.remove('has-padding');const canSelect=state.mode==='inspect'&&state.selected?.element?.isConnected;showNode(selectedBox,!!canSelect);if(canSelect){state.selected.rect=rectFor(state.selected.element);setBox(selectedBox,state.selected.rect);}renderSelectionGuides(canSelect?state.selected.element:null);paintBoxModel(state.hovered?.element);paintLayoutChildren(state.hovered?.element);gapLayer.replaceChildren();if(state.mode==='inspect'&&canSelect&&state.hovered?.element&&state.hovered.element!==state.selected.element)redline(state.selected.element,state.hovered.element);renderRulers();panel();renderPinnedPanels();selectorFinder();renderSelectorHighlights();colorPanel();settingsPanel();feedbackDialog();paintCaptureOverlay();submitToast();const h=ensureNode('hint','hint');showNode(h,state.hintVisible&&!state.toolbarCollapsed&&!!state.mode);const stackNote=state.hitStack.length>1?` · Tab ${state.hitIndex+1}/${state.hitStack.length}`:'';const panelNote=state.panelPosition?'Space resumes panel following':'Space fixes the style panel';const hint=state.mode==='typography'?`Text inspector active · hover text · ${panelNote}`:state.mode==='inspect'?`Hover to inspect · click to lock · ${panelNote} · hover another element to measure${stackNote} · Alt+Shift+↑/↓ navigates DOM`:state.mode==='guides'?'Pixel rulers enabled':state.mode==='eyedropper'?'Eyedropper active · click the tool to pick again':state.mode==='xray'?'X-ray mode enabled via body.xray-mode · press X to close':'';if(h.textContent!==hint)h.textContent=hint;}
  let pointerFrame=0,latestPointer=null;
  document.addEventListener('mousemove',e=>{if(!state.enabled||!['inspect','typography'].includes(state.mode)||host.contains(e.target))return;latestPointer={x:e.clientX,y:e.clientY,target:e.target};if(pointerFrame)return;pointerFrame=requestAnimationFrame(()=>{pointerFrame=0;const point=latestPointer;latestPointer=null;if(!point)return;clearGeometryCache();state.pointer={x:point.x,y:point.y};moveLivePanel();let target=point.target instanceof Element&&!host.contains(point.target)?point.target:null;if(state.mode==='typography'&&target)target=textTarget(target);if(state.mode==='inspect'&&target===state.selected?.element){if(state.hovered){state.hovered=null;schedule();}return;}if(!target){if(!state.hovered)return;setHoveredElement(null);schedule();return;}if(target===state.hovered?.element)return;hitTest(point.x,point.y);setHoveredElement(target);schedule();});},{passive:true});
  // Inspection mode is a modal interaction layer. Stop all page gestures before
  // their target handlers receive them; the shadow-host UI remains interactive.
  const blockPageEvent = e => { if(!state.enabled||!state.mode||state.mode==='eyedropper'||state.mode==='guides'||host.contains(e.target)) return; if(state.toolMenu){state.toolMenu='';render();}if(e.type==='click'&&state.mode==='inspect'){const target=state.hovered?.element||hitTest(e.clientX,e.clientY,true);if(target)selectElement(target);} e.preventDefault(); e.stopImmediatePropagation(); };
  ['pointerdown','pointerup','click','dblclick','auxclick','contextmenu','dragstart'].forEach(type=>document.addEventListener(type,blockPageEvent,{capture:true}));
  document.addEventListener('pointerdown',e=>{if(!state.enabled||!state.toolMenu||host.contains(e.target))return;state.toolMenu='';render();},{capture:true,passive:true});
  document.addEventListener('keydown',e=>{const k=e.key.toLowerCase(),origin=e.composedPath?.()[0]||e.target,editable=origin instanceof HTMLInputElement||origin instanceof HTMLTextAreaElement||origin?.isContentEditable;if(state.captureSelecting){if(k==='escape'){e.preventDefault();e.stopImmediatePropagation();cancelRegionCapture();}else if(k==='enter'){e.preventDefault();e.stopImmediatePropagation();confirmRegionCapture();}return;}if(k!=='escape'&&editable)return;if(!state.enabled)return;if(k===' '){if(['inspect','typography'].includes(state.mode)){e.preventDefault();e.stopImmediatePropagation();if(!e.repeat)togglePanelPosition();return;}if(host.contains(e.target)){e.preventDefault();e.stopImmediatePropagation();}return;}if(state.mode==='inspect'&&e.altKey&&e.shiftKey&&(k==='arrowup'||k==='arrowdown')){e.preventDefault();e.stopImmediatePropagation();navigateHierarchy(k==='arrowup'?'parent':'child');return;}if(state.mode==='inspect'&&k==='tab'){e.preventDefault();e.stopImmediatePropagation();if(state.hitStack.length){state.hitIndex=(state.hitIndex+(e.shiftKey?-1:1)+state.hitStack.length)%state.hitStack.length;setHoveredElement(state.hitStack[state.hitIndex]);render();}return;}const modes={i:'inspect',t:'typography',r:'guides',p:'eyedropper',x:'xray'};if(modes[k]){e.preventDefault();e.stopImmediatePropagation();activateMode(modes[k]);return;}if(k==='f'){e.preventDefault();e.stopImmediatePropagation();state.selectorOpen=!state.selectorOpen;if(!state.selectorOpen)clearSelector();render();return;}if(k==='c'){e.preventDefault();e.stopImmediatePropagation();beginRegionCapture();return;}if(k==='m'){e.preventDefault();e.stopImmediatePropagation();state.settingsOpen=false;state.toolMenu=state.toolMenu==='main'?'':'main';render();return;}if(k==='escape'){e.preventDefault();e.stopImmediatePropagation();if(state.feedbackOpen){closeFeedbackAnimated();return;}else if(state.selectorOpen){state.selectorOpen=false;clearSelector();}else if(state.toolMenu)state.toolMenu='';else if(state.settingsOpen)state.settingsOpen=false;else if(state.mode)exitMode();else if(!state.toolbarCollapsed){state.toolbarCollapsed=true;hideToolTip();}render();return;}if(state.mode&&!['eyedropper','guides'].includes(state.mode)){e.preventDefault();e.stopImmediatePropagation();}}, {capture:true});
  let geometryTimer=0;
  const geometryChanged=()=>{if(!state.enabled)return;const toolbarNode=app.querySelector('.toolbar,.toolbar-logo');if(toolbarNode)setToolbarPosition(state.toolbar.left,state.toolbar.top,toolbarNode);if(!state.mode){schedule();return;}if(state.hovered?.element?.isConnected)state.hovered.rect=rectFor(state.hovered.element);if(state.selected?.element?.isConnected)state.selected.rect=rectFor(state.selected.element);gapLayer.hidden=true;schedule();clearTimeout(geometryTimer);geometryTimer=setTimeout(()=>{gapLayer.hidden=false;schedule();},100);};
  addEventListener('scroll',geometryChanged,{capture:true,passive:true});
  addEventListener('resize',geometryChanged,{passive:true});
  chrome.runtime.onMessage.addListener(m=>{if(m.type==='toggle'){state.enabled=!state.enabled;if(!state.enabled){resetFeedback();exitMode();state.toolMenu='';}else{state.toolbarCollapsed=false;state.mode=null;state.toolMenu='';state.settingsOpen=false;}render();}});render();
})();
