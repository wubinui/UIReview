(() => {
  if (window.__uireviewLoaded) return;
  window.__uireviewLoaded = true;
  // Load quietly. The inspector is opt-in: users explicitly open it from the
  // extension button or shortcut, and closing it restores a clean page.
  const state = { enabled: false, mode: "inspect", hovered: null, selected: null, showSize: false, showPanel: true, panelOpen: true, closedElement: null, settingsOpen: false, feishuStatus: null, feishuChecking: false, alt: false, altCandidates: [], measureRefresh: false, pickedColor: null, eyedropperError: "", captureSelecting: false, captureBusy: false, captureRect: null, captureDrag: null, captureReturnToFeedback: false, feedbackAnchorRect: null, feedbackOpen: false, feedbackSeen: false, feedbackEditing: false, destinationOpen: false, feedbackBusy: false, feedbackAdding: false, screenshotDataUrl: "", feedbackQuestion: "", feedbackNote: "", feedbackDocumentUrl: "", feedbackMessage: "", feedbackStatusType: "", feedbackResultUrl: "", annotationTool: "pen", annotationStart: null, annotationDrawing: false, annotationBaseDataUrl: "", annotationHistory: [], annotationDraft: null, panelPosition: null, panelDrag: null, toolbar: { left: 16, top: 16 } };
  const host = document.createElement("div"); host.id = "uireview-overlay-host"; host.style.cssText = "position:fixed;inset:0;z-index:2147483647;pointer-events:none;";
  const shadow = host.attachShadow({ mode: "open" }); document.documentElement.appendChild(host);
  shadow.innerHTML = `<style>
  :host{font-family:Inter,ui-sans-serif,system-ui,-apple-system,sans-serif;color:#171717}*{box-sizing:border-box}[hidden]{display:none!important}#app.capture-active>:not(.capture-overlay):not(.submit-toast){display:none!important}
  .toolbar{pointer-events:auto;position:fixed;z-index:30;display:flex;gap:4px;padding:4px;border-radius:12px;background:rgba(255,255,255,.96);box-shadow:0 8px 30px #0002,0 0 1px 0 #00000038 inset;backdrop-filter:blur(10px);cursor:move;user-select:none}
  button{border:0;background:transparent;color:#666;width:32px;height:32px;border-radius:8px;cursor:pointer;font-size:14px;display:grid;place-items:center}button:hover{background:#f1f1f1;color:#111}button.active{background:#335cff;color:#fff}button.active svg{stroke:#fff!important}button:focus-visible{outline:2px solid #335cff}.sep{width:1px;background:#e7e7e7;margin:4px 2px}.tool-tip{position:fixed;z-index:40;pointer-events:none;display:flex;align-items:center;gap:8px;padding:6px 8px;border-radius:7px;background:#171717;color:#fff;font-size:11px;line-height:16px;white-space:nowrap;box-shadow:0 5px 16px rgba(0,0,0,.2);opacity:0;transform:translateY(-2px);transition:opacity 100ms ease,transform 100ms ease}.tool-tip.visible{opacity:1;transform:translateY(0)}.tool-shortcut{display:inline-flex;align-items:center;justify-content:center;min-width:18px;height:18px;padding:0 5px;border:1px solid rgba(255,255,255,.22);border-radius:4px;background:rgba(255,255,255,.12);color:#ddd;font:10px ui-monospace,SFMono-Regular,monospace}
  .box{position:fixed;left:0;top:0;border:1.5px solid #335cff;background:#335cff1a;pointer-events:none;will-change:transform,width,height}.selected-box{border:2px solid #fb3748;background:#fb37480d}.text-box{border-color:#a855f7;background:#a855f71a}.label{position:fixed;left:0;top:0;background:#171717;color:#fff;font:12px ui-monospace,monospace;padding:4px 6px;border-radius:4px;pointer-events:none;white-space:nowrap;will-change:transform}.pixel-ruler{position:fixed;z-index:10;pointer-events:none}.pixel-ruler.top{left:28px;top:0;width:calc(100vw - 28px);height:28px}.pixel-ruler.left{left:0;top:28px;width:28px;height:calc(100vh - 28px)}.ruler-corner{position:fixed;z-index:11;left:0;top:0;width:28px;height:28px;pointer-events:none;background:#f7f7f7;border-right:1px solid #d9d9d9;border-bottom:1px solid #d9d9d9}.xray-box{position:fixed;border:1px dashed #f97316;background:#f9731608;pointer-events:none}.xray-label{position:fixed;color:#c2410c;background:#fff7ed;border:1px solid #fed7aa;border-radius:3px;padding:1px 4px;font:10px ui-monospace,monospace;pointer-events:none;white-space:nowrap}.measure{position:fixed;pointer-events:none;background:#fb3748;color:#fb3748;z-index:1}.size-measure{background:#335cff;color:#335cff;z-index:3}.measure.h,.size-measure.h{height:1px}.measure.v,.size-measure.v{width:1px}.measure.h:before,.measure.h:after,.size-measure.h:before,.size-measure.h:after{content:"";position:absolute;top:-4px;height:9px;width:1px;background:currentColor}.measure.h:before,.size-measure.h:before{left:0}.measure.h:after,.size-measure.h:after{right:0}.measure.v:before,.measure.v:after,.size-measure.v:before,.size-measure.v:after{content:"";position:absolute;left:-4px;width:9px;height:1px;background:currentColor}.measure.v:before,.size-measure.v:before{top:0}.measure.v:after,.size-measure.v:after{bottom:0}.measure-label{position:fixed;z-index:2;pointer-events:none;background:#fb3748;color:#fff;font:11px ui-monospace,monospace;line-height:18px;height:18px;padding:0 5px;border-radius:4px;white-space:nowrap}.size-measure-label{background:#335cff;z-index:4;font-size:12px;font-weight:500}
  .panel{position:fixed;width:272px;max-height:calc(100vh - 24px);overflow:auto;padding:12px;border:.5px solid rgba(0,0,0,.08);border-radius:12px;background:#fff;box-shadow:0 4px 16px rgba(0,0,0,.03),0 4px 10px rgba(0,0,0,.03),0 2px 4px rgba(0,0,0,.02);font-size:11px;line-height:16px;pointer-events:auto}.panel h3{display:flex;align-items:center;gap:12px;font-size:11px;line-height:16px;font-weight:500;margin:0 0 10px;cursor:move;user-select:none}.panel-title{display:flex;align-items:center;gap:12px;min-width:0;flex:1}.panel-tag{display:inline-flex;align-items:center;justify-content:center;padding:2px 6px;border-radius:6px;background:#171717;color:#fff;font-size:11px;line-height:16px;white-space:nowrap}.panel-kind{color:#999;font-size:11px;white-space:nowrap}.panel-close{flex:none;width:12px;height:12px;padding:0;font-size:14px;line-height:12px;color:#999;cursor:pointer}.section-title{color:#999;font-size:10px;line-height:14px;text-transform:uppercase;letter-spacing:.04em;margin:10px 0 2px;padding-top:6px;border-top:1px solid rgba(0,0,0,.06)}.size-center-label{position:fixed;z-index:5;pointer-events:none;transform:translate(-50%,-50%);padding:1px 4px;border-radius:3px;background:#335cff;color:#fff;font:9px/14px ui-monospace,SFMono-Regular,monospace;white-space:nowrap;box-shadow:0 1px 2px #0003}.settings{position:fixed;width:252px;padding:8px;border:1px solid #e5e5e5;border-radius:10px;background:#fff;box-shadow:0 12px 32px #00000024;font-size:12px;pointer-events:auto}.settings h3{font-size:13px;margin:4px 4px 8px}.setting{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:8px 4px;border-top:1px solid #f0f0f0;cursor:pointer}.setting input{accent-color:#335cff}.feishu-setting{display:flex;align-items:center;gap:8px;padding:9px 4px;border-top:1px solid #f0f0f0}.feishu-dot{width:7px;height:7px;flex:none;border-radius:50%;background:#9ca3af}.feishu-dot.ready{background:#1fc16b}.feishu-dot.warning{background:#f59e0b}.feishu-dot.offline{background:#fb3748}.feishu-text{min-width:0;flex:1;color:#555;font-size:11px;line-height:15px}.feishu-open{width:auto;height:28px;padding:0 9px;background:#f1f1f1;color:#171717;font-size:11px}.badge{color:#7e22ce;background:#f3e8ff;padding:2px 5px;border-radius:4px;font:11px ui-monospace,monospace}.row{display:grid;grid-template-columns:96px minmax(0,1fr);gap:8px;align-items:center;border-top:0;padding:0;min-height:20px}.key{color:#999;text-transform:capitalize;white-space:nowrap}.value{font-family:ui-monospace,SFMono-Regular,monospace;overflow-wrap:anywhere;color:#171717}.color{display:flex;align-items:center;gap:5px}.swatch{width:12px;height:12px;border:1px solid #0003;border-radius:3px;flex:none}.hint{position:fixed;bottom:16px;left:16px;padding:6px 9px;border-radius:6px;background:#171717e6;color:#fff;font-size:11px;pointer-events:none}.color-panel{position:fixed;width:224px;padding:10px;border:.5px solid rgba(0,0,0,.1);border-radius:12px;background:#fff;box-shadow:0 10px 30px rgba(0,0,0,.14);pointer-events:auto}.color-preview{height:72px;margin-bottom:8px;border:1px solid rgba(0,0,0,.1);border-radius:8px}.color-copy{width:100%;height:32px;padding:0 9px;display:flex;align-items:center;justify-content:space-between;border-radius:7px;background:#f5f5f5;color:#171717;font:11px ui-monospace,SFMono-Regular,monospace}.color-copy:hover{background:#ededed}.color-copy .copy-note{color:#8a8a8a;font:10px Inter,ui-sans-serif,system-ui}.color-rgb{display:flex;align-items:center;justify-content:space-between;height:30px;padding:0 9px;color:#777;font:11px ui-monospace,SFMono-Regular,monospace}.color-error{padding:7px 5px;color:#fb3748;font-size:11px;line-height:16px}
  .capture-overlay{position:fixed;inset:0;z-index:200;pointer-events:auto;cursor:crosshair;user-select:none;touch-action:none}.capture-shade{position:fixed;background:rgba(0,0,0,.48);pointer-events:none}.capture-selection{position:fixed;left:0;top:0;border:1px solid #335cff;outline:1px solid rgba(255,255,255,.75);pointer-events:auto;cursor:move;will-change:transform,width,height}.capture-selection:empty{pointer-events:none}.capture-handle{position:absolute;width:10px;height:10px;border:1px solid #335cff;border-radius:2px;background:#fff;box-shadow:0 1px 3px #0003}.capture-handle[data-handle="nw"]{left:-6px;top:-6px;cursor:nwse-resize}.capture-handle[data-handle="n"]{left:50%;top:-6px;transform:translateX(-50%);cursor:ns-resize}.capture-handle[data-handle="ne"]{right:-6px;top:-6px;cursor:nesw-resize}.capture-handle[data-handle="e"]{right:-6px;top:50%;transform:translateY(-50%);cursor:ew-resize}.capture-handle[data-handle="se"]{right:-6px;bottom:-6px;cursor:nwse-resize}.capture-handle[data-handle="s"]{left:50%;bottom:-6px;transform:translateX(-50%);cursor:ns-resize}.capture-handle[data-handle="sw"]{left:-6px;bottom:-6px;cursor:nesw-resize}.capture-handle[data-handle="w"]{left:-6px;top:50%;transform:translateY(-50%);cursor:ew-resize}.capture-size{position:absolute;left:0;top:-27px;height:21px;padding:0 7px;border-radius:5px;background:#171717;color:#fff;font:11px/21px ui-monospace,SFMono-Regular,monospace;white-space:nowrap;pointer-events:none}.capture-instructions{position:fixed;left:50%;top:18px;transform:translateX(-50%);padding:8px 11px;border-radius:8px;background:#171717;color:#fff;box-shadow:0 6px 20px #0005;font-size:12px;white-space:nowrap;pointer-events:none}.capture-controls{position:fixed;display:flex;gap:6px;padding:5px;border-radius:10px;background:#fff;box-shadow:0 10px 30px #0004;pointer-events:auto;cursor:default}.capture-controls button{width:auto;height:30px;padding:0 11px}.capture-controls .capture-confirm{background:#171717;color:#fff}.capture-controls .capture-confirm:hover{background:#333;color:#fff}.capture-controls button:disabled{opacity:.55;cursor:wait}
  .feedback-backdrop{position:fixed;inset:0;z-index:100;background:rgba(0,0,0,.16);pointer-events:auto}.feedback-dialog{position:fixed;display:flex;flex-direction:column;gap:12px;width:336px;overflow:visible;outline:0;font-family:"PingFang SC",Inter,ui-sans-serif,system-ui,-apple-system,sans-serif;font-size:12px;pointer-events:auto}.feedback-card{display:flex;flex-direction:column;gap:12px;width:100%;padding:16px;border-radius:14px;background:#fff;box-shadow:0 8px 28px rgba(0,0,0,.18)}.feedback-head{display:flex;align-items:center;gap:12px;height:20px}.feedback-head strong{min-width:0;flex:1;color:#000;font-size:12px;font-weight:500;line-height:18px}.feedback-close{display:flex;align-items:center;justify-content:center;width:20px;height:20px;padding:4px;border-radius:999px}.feedback-close svg{display:block;width:12px;height:12px}.feedback-close:hover{background:#f7f7f7}.feedback-inputs{display:flex;flex-direction:column;gap:8px;cursor:text}.feedback-inputs textarea,.feedback-inputs input{display:block;width:100%;padding:0;border:0;border-radius:0;background:#fff;color:#171717;font:13px/20px "PingFang SC",Inter,ui-sans-serif,system-ui;outline:0}.feedback-inputs:not(.is-editing) textarea,.feedback-inputs:not(.is-editing) input{pointer-events:none}.feedback-inputs textarea{min-height:120px;resize:none}.feedback-inputs input{height:20px}.feedback-inputs textarea::placeholder,.feedback-inputs input::placeholder{color:#6f6f6f;opacity:1}.feedback-connected-document{display:flex;align-items:center;gap:7px;min-width:0;color:#5f5f5f;font-size:11px;line-height:16px}.feedback-connected-document:before{content:"";width:6px;height:6px;flex:none;border-radius:50%;background:#1fc16b;box-shadow:0 0 0 2px rgba(31,193,107,.12)}.feedback-connected-document span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.feedback-actions{display:flex;align-items:center;gap:8px;padding-top:4px}.feedback-destination-toggle{display:flex;align-items:center;justify-content:center;flex:none;width:36px;height:36px;padding:10px;border-radius:10px;background:#f7f7f7}.feedback-destination-toggle svg{display:block;width:16px;height:16px}.feedback-destination-toggle:hover,.feedback-destination-toggle.active{background:#ededed}.feedback-destination-toggle.active svg{stroke:#000!important}.feedback-send{display:flex;align-items:center;justify-content:center;flex:1;width:auto;height:36px;padding:0 6px;border-radius:10px;background:#171717;color:#fff;font:500 12px/18px "PingFang SC",Inter,ui-sans-serif,system-ui}.feedback-send:hover{background:#333;color:#fff}.feedback-send:disabled{opacity:.5;cursor:wait}.feedback-status{color:#777;font-size:11px;line-height:16px}.feedback-status:empty{display:none}.feedback-status.error{color:#fb3748}.feedback-status.success{color:#1fc16b}.feedback-destination-fields{display:flex;flex-direction:column;gap:10px}.feedback-select-wrap{position:relative}.feedback-doc,.feedback-doc-url{display:block;width:100%;height:36px;padding:0 12px;border:0;border-radius:10px;background:#f7f7f7;color:#171717;font:12px/18px "PingFang SC",Inter,ui-sans-serif,system-ui;outline:0}.feedback-doc{padding-right:36px;appearance:none;-webkit-appearance:none}.feedback-select-icon{position:absolute;right:12px;top:12px;width:12px;height:12px;pointer-events:none}.feedback-select-icon svg{display:block;width:12px;height:12px}.feedback-doc-url::placeholder{color:#6f6f6f;opacity:1}.feedback-doc:focus,.feedback-doc-url:focus{box-shadow:0 0 0 2px rgba(51,92,255,.18)}.feedback-shot-column{position:fixed;display:flex;flex-direction:column;gap:8px;pointer-events:auto}.feedback-shot-wrap{position:relative;flex:1;min-height:0;border:0;border-radius:8px;background:#f5f5f5;box-shadow:0 12px 36px rgba(0,0,0,.22);overflow:hidden}.feedback-canvas{display:block;width:100%;height:100%;object-fit:contain;cursor:crosshair;touch-action:none}.feedback-annotate-bar{display:flex;align-items:center;gap:6px;min-height:38px;padding:4px;border:.5px solid rgba(0,0,0,.08);border-radius:10px;background:#fff;box-shadow:0 8px 24px rgba(0,0,0,.18)}.feedback-annotate-bar button{width:30px;height:30px;border:0;background:#fff;color:#666}.feedback-annotate-bar button.active{background:#171717;color:#fff}.feedback-annotate-bar button:disabled{opacity:.32;cursor:not-allowed}.feedback-annotate-bar .annotation-divider{width:1px;height:18px;margin:0 1px;background:#e7e7e7}.feedback-annotate-bar .annotation-help{flex:1;padding:0 6px;color:#999;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.feedback-retake{position:absolute;right:8px;bottom:8px;width:auto;height:30px;padding:0 10px;background:rgba(23,23,23,.9);color:#fff;box-shadow:0 4px 12px #0003}.feedback-retake:hover{background:#171717;color:#fff}.feedback-backdrop.stacked{display:flex;flex-direction:column;gap:12px;overflow:auto;padding:28px 12px}.feedback-backdrop.stacked .feedback-shot-column,.feedback-backdrop.stacked .feedback-dialog{position:relative!important;left:auto!important;top:auto!important;width:min(560px,100%)!important;margin:0 auto}.feedback-backdrop.stacked .feedback-shot-column{order:0;height:min(55vh,420px)!important;flex:none}.feedback-backdrop.stacked .feedback-dialog{order:1;overflow:visible;flex:none}.submit-toast{position:fixed;z-index:180;right:16px;bottom:16px;display:flex;align-items:center;gap:9px;max-width:min(380px,calc(100vw - 32px));min-height:40px;padding:10px 12px;border:1px solid rgba(0,0,0,.08);border-radius:10px;background:#fff;color:#171717;box-shadow:0 12px 36px #0003;font-size:12px;line-height:18px;pointer-events:none}.submit-toast.loading:before{content:"";width:14px;height:14px;flex:none;border:2px solid #d4d4d4;border-top-color:#335cff;border-radius:50%;animation:submit-spin .7s linear infinite}.submit-toast.success:before,.submit-toast.error:before{display:grid;place-items:center;width:16px;height:16px;flex:none;border-radius:50%;color:#fff;font-size:11px;font-weight:700}.submit-toast.success:before{content:"✓";background:#1fc16b}.submit-toast.error:before{content:"!";background:#fb3748}.submit-toast.error{color:#fb3748}@keyframes submit-spin{to{transform:rotate(360deg)}}
  </style><div id="app"></div>`;
  const app = shadow.querySelector("#app"); let toolbarMode = null; let pending = false; const tokenCache = new WeakMap(); const textTargetCache = new WeakMap(); let tokenRuleIndex = null;
  const ui = {};
  const ensureNode = (key,className) => { if(!ui[key]){ui[key]=document.createElement('div');ui[key].className=className;ui[key].hidden=true;app.appendChild(ui[key]);}return ui[key]; };
  const showNode = (node,visible) => { node.hidden=!visible; };
  const setBox = (node,rect) => { node.style.transform=`translate3d(${rect.left}px,${rect.top}px,0)`;node.style.width=`${rect.width}px`;node.style.height=`${rect.height}px`; };
  const gapLayer=ensureNode('gapLayer','measure-layer'),sizeLayer=ensureNode('sizeLayer','size-layer');gapLayer.hidden=false;sizeLayer.hidden=false;
  const schedule = () => { if (pending) return; pending = true; requestAnimationFrame(() => { pending = false; render(); }); };
  // Computed styles turn `var(--token)` into its final pixel/color value. To expose
  // the design-system token, inspect the author CSS rule that wins the cascade.
  const specificity = selector => { const clean=selector.replace(/:not\(([^)]*)\)/g,'$1'); return [(clean.match(/#[\w-]+/g)||[]).length,(clean.match(/\.[\w-]+|\[[^\]]+\]|:(?!:)[\w-]+(?:\([^)]*\))?/g)||[]).length,(clean.match(/(^|[\s>+~])([a-z][\w-]*|::[\w-]+)/gi)||[]).length]; };
  const compare = (a,b) => a.important-b.important || a.spec[0]-b.spec[0] || a.spec[1]-b.spec[1] || a.spec[2]-b.spec[2] || a.order-b.order;
  // Build the author-rule index once, then test a text target against it once.
  // Previously every token property recursively walked every stylesheet again.
  const rulesForTokens = () => { if(tokenRuleIndex)return tokenRuleIndex;const index=[];let order=0;const collect=rules=>{for(const rule of [...rules]){if(rule.type===CSSRule.STYLE_RULE){for(const raw of rule.selectorText.split(',')){const selector=raw.trim();if(selector)index.push({selector,style:rule.style,spec:specificity(selector),order:++order});}}else if(rule.cssRules){try{collect(rule.cssRules);}catch{}}}};for(const sheet of [...document.styleSheets]){try{collect(sheet.cssRules);}catch{}}return tokenRuleIndex=index;};
  const tokenValuesFor = (el, properties) => { const winners=new Map(),wanted=new Set(properties);for(const rule of rulesForTokens()){let hasValue=false;for(const property of wanted)if(rule.style.getPropertyValue(property)){hasValue=true;break;}if(!hasValue)continue;try{if(!el.matches(rule.selector))continue;}catch{continue;}for(const property of wanted){const value=rule.style.getPropertyValue(property);if(!value)continue;const candidate={value:value.trim(),important:rule.style.getPropertyPriority(property)==='important'?1:0,spec:rule.spec,order:rule.order},previous=winners.get(property);if(!previous||compare(previous,candidate)<0)winners.set(property,candidate);}}for(const property of wanted){const value=el.style.getPropertyValue(property);if(value)winners.set(property,{value:value.trim(),important:el.style.getPropertyPriority(property)==='important'?1:0,spec:[1000000,0,0],order:Number.MAX_SAFE_INTEGER});}return winners; };
  const tokenNames = value => [...new Set([...value.matchAll(/var\(\s*(--[\w-]+)/g)].map(match=>match[1]))];
  const tokenFor = (values, properties) => [...new Set(properties.flatMap(property=>tokenNames(values.get(property)?.value||'')))].join(' · ') || '—';
  const tokensFor = (el, textOnly=false) => { if(!el)return {size:'—',style:'—',color:'—',gap:'—',padding:'—'};const cached=tokenCache.get(el);if(cached?.[textOnly?'text':'all'])return cached[textOnly?'text':'all'];const textProperties=['font-size','line-height','font-family','font-weight','font-style','letter-spacing','color'],textValues=tokenValuesFor(el,textProperties),text={size:tokenFor(textValues,['font-size','line-height']),style:tokenFor(textValues,['font-family','font-weight','font-style','letter-spacing']),color:tokenFor(textValues,['color'])};let entry=cached||{};entry.text=text;if(textOnly){tokenCache.set(el,entry);return text;}const parent=el.parentElement,parentValues=parent?tokenValuesFor(parent,['gap','row-gap','column-gap','padding','padding-top','padding-right','padding-bottom','padding-left']):new Map();entry.all={...text,gap:tokenFor(parentValues,['gap','row-gap','column-gap']),padding:tokenFor(parentValues,['padding','padding-top','padding-right','padding-bottom','padding-left'])};tokenCache.set(el,entry);return entry.all; };
  // `withTokens` is false on mousemove: reading layout is cheap; walking every
  // stylesheet is not. Tokens are resolved only for selected/text-panel items.
  const rectFor = el => {const r=el.getBoundingClientRect();return {left:r.left,top:r.top,width:r.width,height:r.height};};
  const info = (el, withTokens=false, detailed=true) => { const base={element:el,tag:el.tagName.toLowerCase(),rect:rectFor(el)};if(!detailed)return base;const s=getComputedStyle(el); const parent=el.parentElement,ps=parent?getComputedStyle(parent):null; return {...base,style:{display:s.display,position:s.position,color:s.color,backgroundColor:s.backgroundColor,fontFamily:s.fontFamily,fontSize:s.fontSize,fontWeight:s.fontWeight,fontStyle:s.fontStyle,lineHeight:s.lineHeight,letterSpacing:s.letterSpacing,textAlign:s.textAlign,textTransform:s.textTransform,textDecoration:s.textDecorationLine,margin:s.margin,padding:s.padding,borderRadius:s.borderRadius,opacity:s.opacity,gap:s.gap,rowGap:s.rowGap,columnGap:s.columnGap,flexDirection:s.flexDirection},layout:parent?{display:ps.display,flexDirection:ps.flexDirection,gap:ps.gap,rowGap:ps.rowGap,columnGap:ps.columnGap,padding:ps.padding}:null,tokens:withTokens?tokensFor(el,state.mode==='typography'):null}; };
  const textTarget = (el) => {const cached=textTargetCache.get(el);if(cached?.isConnected)return cached;let n=el; while(n&&n!==document.body){const s=getComputedStyle(n); if(n.childElementCount===0||/^(p|h[1-6]|span|a|button|label|li|blockquote|code|pre|em|strong)$/i.test(n.tagName)||s.display==='inline'){textTargetCache.set(el,n);return n;} n=n.parentElement;}textTargetCache.set(el,el);return el; };
  // Lucide icons are inlined so Manifest V3 never depends on remote scripts or
  // local per-icon files. Paths below use Lucide's 24px viewBox geometry.
  const lucide = {
    inspect:'<path d="M4.037 4.688a.495.495 0 0 1 .651-.651l16 6.5a.5.5 0 0 1-.063.947l-6.124 1.58a2 2 0 0 0-1.438 1.435l-1.579 6.126a.5.5 0 0 1-.947.063z"/>',
    type:'<polyline points="4 7 4 4 20 4 20 7"/><line x1="9" y1="20" x2="15" y2="20"/><line x1="12" y1="4" x2="12" y2="20"/>',
    pipette:'<path d="m2 22 1-1h3l9-9"/><path d="M3 21v-3l9-9"/><path d="m15 6 3.4-3.4a2.1 2.1 0 1 1 3 3L18 9l.4.4a2.1 2.1 0 1 1-3 3l-3.8-3.8a2.1 2.1 0 1 1 3-3Z"/>',
    ruler:'<path d="M21.3 15.3a2.4 2.4 0 0 1 0 3.4l-2.6 2.6a2.4 2.4 0 0 1-3.4 0L2.7 8.7a2.41 2.41 0 0 1 0-3.4l2.6-2.6a2.41 2.41 0 0 1 3.4 0Z"/><path d="m14.5 12.5 2-2"/><path d="m11.5 9.5 2-2"/><path d="m8.5 6.5 2-2"/><path d="m17.5 15.5 2-2"/>',
    xray:'<path d="M5 21a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2"/><path d="M9 21h1"/><path d="M14 21h1"/>',
    settings:'<circle cx="12" cy="12" r="1"/><circle cx="19" cy="12" r="1"/><circle cx="5" cy="12" r="1"/>',
    crop:'<path d="M6 2v14a2 2 0 0 0 2 2h14"/><path d="M18 22V8a2 2 0 0 0-2-2H2"/>',
    close:'<line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>',
    pen:'<path d="m4 20 4.5-1L19 8.5a2.12 2.12 0 0 0-3-3L5.5 16z"/><path d="m14.5 6.5 3 3"/>',
    'arrow-up-right':'<path d="M7 17 17 7"/><path d="M7 7h10v10"/>',
    square:'<rect width="18" height="18" x="3" y="3" rx="2"/>',
    circle:'<circle cx="12" cy="12" r="10"/>',
    undo:'<path d="M9 14 4 9l5-5"/><path d="M4 9h10.5a5.5 5.5 0 0 1 0 11H11"/>',
    'scroll-text':'<path d="M15 12h-5"/><path d="M15 8h-5"/><path d="M19 17V5a2 2 0 0 0-2-2H4"/><path d="M8 21h12a2 2 0 0 0 2-2v-1a1 1 0 0 0-1-1H11a1 1 0 0 0-1 1v1a2 2 0 1 1-4 0V5a2 2 0 1 0-4 0v2a1 1 0 0 0 1 1h3"/>',
    'chevron-down':'<path d="m6 9 6 6 6-6"/>'
  };
  const icon = name => `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#000000" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${lucide[name]}</svg>`;
  let tipTimer=0;
  const hideToolTip = () => {clearTimeout(tipTimer);ui.toolTip?.classList.remove('visible');};
  const showToolTip = (button,label,shortcut='') => {clearTimeout(tipTimer);tipTimer=setTimeout(()=>{const tip=ensureNode('toolTip','tool-tip');tip.innerHTML=`<span>${label}</span>${shortcut?`<kbd class="tool-shortcut">${shortcut}</kbd>`:''}`;showNode(tip,true);const r=button.getBoundingClientRect(),w=tip.offsetWidth;let left=Math.max(8,Math.min(innerWidth-w-8,r.left+r.width/2-w/2)),top=r.bottom+8;if(top+tip.offsetHeight>innerHeight-8)top=Math.max(8,r.top-tip.offsetHeight-8);tip.style.left=`${left}px`;tip.style.top=`${top}px`;requestAnimationFrame(()=>tip.classList.add('visible'));},180);};
  const btn = (label,iconName,tipLabel,shortcut,fn) => {const b=document.createElement('button');b.setAttribute('aria-label',label);b.innerHTML=icon(iconName);b.onclick=fn;b.addEventListener('mouseenter',()=>showToolTip(b,tipLabel,shortcut));b.addEventListener('mouseleave',hideToolTip);b.addEventListener('focus',()=>showToolTip(b,tipLabel,shortcut));b.addEventListener('blur',hideToolTip);return b;};
  const exitMode = () => { state.mode=null; state.hovered=null; state.selected=null; state.guideLocked=false; state.alt=false; state.measureRefresh=false; state.panelOpen=false; state.settingsOpen=false; };
  const hexToRgb = hex => {const value=hex.replace('#','');return {r:parseInt(value.slice(0,2),16),g:parseInt(value.slice(2,4),16),b:parseInt(value.slice(4,6),16)};};
  const feishuRequest = async (action, body) => {let result;try{result=await chrome.runtime.sendMessage({type:'feishu-api-request',action,body});}catch{throw new Error('UIReview 后台服务暂时不可用，请重新加载扩展。');}if(!result?.ok)throw new Error(result?.error||'飞书请求失败');return result.data;};
  const loadDocuments = async () => {const saved=await chrome.runtime.sendMessage({type:'document-state'});if(!saved?.ok)throw new Error(saved?.error||'Unable to load saved documents');state.documents=saved.documents||[];state.selectedDocumentId=saved.selectedDocumentId||state.documents[0]?.id||'';};
  const saveDocumentState = async () => {const result=await chrome.runtime.sendMessage({type:'save-document-state',documents:state.documents,selectedDocumentId:state.selectedDocumentId});if(!result?.ok)throw new Error(result?.error||'Unable to save document selection');};
  const resetFeedback = () => {state.captureSelecting=false;state.captureBusy=false;state.captureRect=null;state.captureDrag=null;state.captureReturnToFeedback=false;state.feedbackAnchorRect=null;state.feedbackOpen=false;state.feedbackEditing=false;state.destinationOpen=false;state.feedbackBusy=false;state.feedbackAdding=false;state.screenshotDataUrl='';state.feedbackQuestion='';state.feedbackNote='';state.feedbackDocumentUrl='';state.feedbackMessage='';state.feedbackStatusType='';state.feedbackResultUrl='';state.annotationTool='pen';state.annotationStart=null;state.annotationDrawing=false;state.annotationBaseDataUrl='';state.annotationHistory=[];state.annotationDraft=null;};
  let submitToastTimer=0;
  const setSubmitStatus = (status,message,timeout=0) => {clearTimeout(submitToastTimer);state.submitStatus=status;state.submitMessage=message;render();if(timeout)submitToastTimer=setTimeout(()=>{state.submitStatus='';state.submitMessage='';render();},timeout);};
  const submitFeedbackInBackground = payload => {setSubmitStatus('loading','Sending feedback to Feishu…');feishuRequest('submitFeedback',payload).then(()=>setSubmitStatus('success','Feedback sent successfully',3200)).catch(error=>setSubmitStatus('error',`Send failed: ${error.message}`,8000));};
  const clamp = (value,min,max) => Math.min(max,Math.max(min,value));
  const normalizeRect = (x1,y1,x2,y2) => ({left:Math.min(x1,x2),top:Math.min(y1,y2),width:Math.abs(x2-x1),height:Math.abs(y2-y1)});
  const beginRegionCapture = () => {state.captureReturnToFeedback=state.feedbackOpen&&!!state.screenshotDataUrl;state.feedbackOpen=false;state.feedbackMessage='';state.feedbackStatusType='';state.feedbackResultUrl='';state.captureRect=null;state.captureDrag=null;state.captureBusy=false;state.captureSelecting=true;render();};
  const cancelRegionCapture = () => {state.captureSelecting=false;state.captureBusy=false;state.captureRect=null;state.captureDrag=null;if(state.captureReturnToFeedback)state.feedbackOpen=true;state.captureReturnToFeedback=false;render();};
  const cropScreenshot = (dataUrl,rect,viewportWidth,viewportHeight) => new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>{const scaleX=image.naturalWidth/viewportWidth,scaleY=image.naturalHeight/viewportHeight,x=clamp(Math.round(rect.left*scaleX),0,image.naturalWidth-1),y=clamp(Math.round(rect.top*scaleY),0,image.naturalHeight-1),width=clamp(Math.round(rect.width*scaleX),1,image.naturalWidth-x),height=clamp(Math.round(rect.height*scaleY),1,image.naturalHeight-y),canvas=document.createElement('canvas');canvas.width=width;canvas.height=height;const context=canvas.getContext('2d');if(!context){reject(new Error('Unable to prepare the screenshot'));return;}context.drawImage(image,x,y,width,height,0,0,width,height);resolve(canvas.toDataURL('image/png'));};image.onerror=()=>reject(new Error('Unable to read the captured screenshot'));image.src=dataUrl;});
  const loadImage = dataUrl => new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>resolve(image);image.onerror=()=>reject(new Error('Unable to load screenshot'));image.src=dataUrl;});
  const drawArrow = (ctx,x1,y1,x2,y2,size=18) => {ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.stroke();const angle=Math.atan2(y2-y1,x2-x1);ctx.beginPath();ctx.moveTo(x2,y2);ctx.lineTo(x2-size*Math.cos(angle-.45),y2-size*Math.sin(angle-.45));ctx.lineTo(x2-size*Math.cos(angle+.45),y2-size*Math.sin(angle+.45));ctx.closePath();ctx.fill();};
  const annotationPoint = (canvas,e) => {const r=canvas.getBoundingClientRect(),sx=canvas.width/r.width,sy=canvas.height/r.height;return {x:clamp((e.clientX-r.left)*sx,0,canvas.width),y:clamp((e.clientY-r.top)*sy,0,canvas.height)};};
  const setupAnnotations = async (canvas) => {
    const baseDataUrl=state.annotationBaseDataUrl||state.screenshotDataUrl;
    if(!state.annotationBaseDataUrl)state.annotationBaseDataUrl=baseDataUrl;
    const image=await loadImage(baseDataUrl);
    if(!canvas.isConnected)return;
    canvas.width=image.naturalWidth;canvas.height=image.naturalHeight;
    const ctx=canvas.getContext('2d'),strokeWidth=Math.max(4,canvas.width/220),arrowSize=Math.max(16,strokeWidth*4.5);
    const style=()=>{ctx.strokeStyle='#fb3748';ctx.fillStyle='#fb3748';ctx.lineWidth=strokeWidth;ctx.lineCap='round';ctx.lineJoin='round';};
    const drawCommand=command=>{if(!command)return;style();const start=command.start,end=command.end;if(command.tool==='pen'){if(command.points.length<2)return;ctx.beginPath();ctx.moveTo(command.points[0].x,command.points[0].y);for(const point of command.points.slice(1))ctx.lineTo(point.x,point.y);ctx.stroke();return;}if(!start||!end)return;if(command.tool==='arrow'){drawArrow(ctx,start.x,start.y,end.x,end.y,arrowSize);return;}const left=Math.min(start.x,end.x),top=Math.min(start.y,end.y),width=Math.abs(end.x-start.x),height=Math.abs(end.y-start.y);if(command.tool==='rectangle'){ctx.strokeRect(left,top,width,height);return;}if(command.tool==='ellipse'){ctx.beginPath();ctx.ellipse(left+width/2,top+height/2,width/2,height/2,0,0,Math.PI*2);ctx.stroke();}};
    const redraw=(draft=null,save=false)=>{ctx.clearRect(0,0,canvas.width,canvas.height);ctx.drawImage(image,0,0);for(const command of state.annotationHistory)drawCommand(command);drawCommand(draft);if(save)state.screenshotDataUrl=canvas.toDataURL('image/png');};
    const finish=e=>{if(!state.annotationDrawing)return;const point=annotationPoint(canvas,e),draft=state.annotationDraft;if(draft?.tool==='pen')draft.points.push(point);else if(draft)draft.end=point;state.annotationDrawing=false;state.annotationStart=null;state.annotationDraft=null;const moved=draft?.tool==='pen'?draft.points.length>1:draft&&Math.hypot(draft.end.x-draft.start.x,draft.end.y-draft.start.y)>2;if(moved)state.annotationHistory.push(draft);redraw(null,true);};
    redraw(null,true);
    canvas.onpointerdown=e=>{if(state.feedbackBusy||e.button!==0)return;const point=annotationPoint(canvas,e);state.annotationDrawing=true;state.annotationStart=point;state.annotationDraft=state.annotationTool==='pen'?{tool:'pen',points:[point]}:{tool:state.annotationTool,start:point,end:point};canvas.setPointerCapture?.(e.pointerId);redraw(state.annotationDraft);};
    canvas.onpointermove=e=>{if(!state.annotationDrawing||!state.annotationDraft)return;const point=annotationPoint(canvas,e);if(state.annotationDraft.tool==='pen')state.annotationDraft.points.push(point);else state.annotationDraft.end=point;redraw(state.annotationDraft);};
    canvas.onpointerup=e=>{finish(e);render();};
    canvas.onpointercancel=()=>{state.annotationDrawing=false;state.annotationStart=null;state.annotationDraft=null;redraw();};
  };
  const undoAnnotation = () => {if(!state.annotationHistory.length)return false;state.annotationHistory.pop();state.annotationDrawing=false;state.annotationStart=null;state.annotationDraft=null;return true;};
  const confirmRegionCapture = async () => {const rect=state.captureRect;if(!rect||rect.width<8||rect.height<8||state.captureBusy)return;const viewportWidth=innerWidth,viewportHeight=innerHeight;state.feedbackAnchorRect={...rect};state.captureBusy=true;paintCaptureOverlay();host.style.visibility='hidden';let result;try{await new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(()=>setTimeout(resolve,50))));result=await chrome.runtime.sendMessage({type:'capture-visible-tab'});if(!result?.ok)throw new Error(result?.error||'Unable to capture this tab');state.screenshotDataUrl=await cropScreenshot(result.dataUrl,rect,viewportWidth,viewportHeight);state.annotationBaseDataUrl=state.screenshotDataUrl;state.annotationHistory=[];state.annotationDraft=null;state.annotationDrawing=false;state.captureSelecting=false;state.captureRect=null;state.captureDrag=null;state.captureReturnToFeedback=false;state.feedbackOpen=true;await loadDocuments();const hasDestination=state.documents.some(item=>item.id===state.selectedDocumentId);state.destinationOpen=!state.feedbackSeen||!hasDestination;state.feedbackSeen=true;}catch(error){state.captureSelecting=false;state.captureRect=null;state.captureDrag=null;state.captureReturnToFeedback=false;state.feedbackOpen=true;state.destinationOpen=true;state.feedbackMessage=error.message;state.feedbackStatusType='error';}finally{state.captureBusy=false;host.style.visibility='';render();}};
  const saveDocument = async url => {const document=await feishuRequest('validateDocument',{documentUrl:url});state.documents=[document,...state.documents.filter(item=>item.id!==document.id)].slice(0,20);state.selectedDocumentId=document.id;await saveDocumentState();return document;};
  const pickColor = async () => {state.mode='eyedropper';state.hovered=null;state.selected=null;state.panelOpen=false;state.settingsOpen=false;state.eyedropperError='';render();if(!window.EyeDropper){state.eyedropperError='当前浏览器不支持原生吸管工具';render();return;}try{const result=await new EyeDropper().open();state.pickedColor=result.sRGBHex.toUpperCase();state.eyedropperError='';}catch(error){if(error?.name!=='AbortError')state.eyedropperError='取色失败，请重新尝试';}render();};
  const activateMode = mode => {if(mode==='eyedropper'){pickColor();return;}if(state.mode===mode)exitMode();else{state.mode=mode;state.selected=null;state.hovered=null;state.guideLocked=false;state.panelOpen=!['xray','guides'].includes(mode);state.closedElement=null;}render();};
  function toolbar(){if(toolbarMode===state.mode&&app.querySelector('.toolbar'))return;hideToolTip();app.querySelector('.toolbar')?.remove();const bar=document.createElement('div');bar.className='toolbar';bar.style.left=`${state.toolbar.left}px`;bar.style.top=`${state.toolbar.top}px`;[['inspect','inspect','Inspect','I'],['typography','type','Typography','T'],['guides','ruler','Rulers','R'],['eyedropper','pipette','Eyedropper','P'],['xray','xray','X-ray','X']].forEach(([m,i,t,key])=>{const active=state.mode===m;const action=m==='eyedropper'&&active?'Pick another color':active?`Close ${t}`:t;const b=btn(action,i,action,key,()=>{hideToolTip();activateMode(m);});if(active)b.classList.add('active');bar.appendChild(b);});bar.appendChild(btn('Capture feedback','crop','Screenshot Feedback','C',()=>{hideToolTip();beginRegionCapture();}));const sep=document.createElement('span');sep.className='sep';bar.appendChild(sep);const settings=btn('Inspector settings','settings','Settings','S',()=>{hideToolTip();state.settingsOpen=!state.settingsOpen;if(state.settingsOpen){state.feishuStatus=null;checkFeishu();}render()});if(state.settingsOpen)settings.classList.add('active');bar.appendChild(settings);bar.appendChild(btn('Close UIReview','close','Close UIReview','Esc',()=>{hideToolTip();resetFeedback();state.enabled=false;exitMode();render()}));bar.addEventListener('pointerdown',e=>{if(e.target.closest('button'))return;hideToolTip();const sx=e.clientX,sy=e.clientY,sl=state.toolbar.left,st=state.toolbar.top;const move=m=>{state.toolbar.left=Math.max(0,sl+m.clientX-sx);state.toolbar.top=Math.max(0,st+m.clientY-sy);bar.style.left=`${state.toolbar.left}px`;bar.style.top=`${state.toolbar.top}px`;};const up=()=>{removeEventListener('pointermove',move);removeEventListener('pointerup',up)};addEventListener('pointermove',move);addEventListener('pointerup',up);});app.appendChild(bar);toolbarMode=state.mode;}
  const row=(p,k,v,color=false)=>{const r=document.createElement('div');r.className='row';r.innerHTML=`<span class="key">${k}</span><span class="value">${color?`<span class="color"><i class="swatch" style="background:${v}"></i>${v}</span>`:v}</span>`;p.appendChild(r);};
  const section=(p,name)=>{const s=document.createElement('div');s.className='section-title';s.textContent=name;p.appendChild(s);};
  const overlap=(a1,a2,b1,b2)=>Math.max(0,Math.min(a2,b2)-Math.max(a1,b1));
  function nearestGaps(element){
    const source=element.getBoundingClientRect(),gaps={left:null,right:null,top:null,bottom:null};
    // Inspect visible peer elements. Small/empty wrappers are skipped so the
    // result reads like layout spacing instead of DOM implementation details.
    for(const node of document.body.querySelectorAll('*')){if(node===element||node.contains(element)||element.contains(node)||host.contains(node))continue;const r=node.getBoundingClientRect();if(r.width<4||r.height<4||r.right<0||r.bottom<0||r.left>innerWidth||r.top>innerHeight)continue;
      const vertical=overlap(source.top,source.bottom,r.top,r.bottom),horizontal=overlap(source.left,source.right,r.left,r.right);
      if(vertical>=Math.min(8,source.height*.25,r.height*.25)){if(r.right<=source.left){const d=source.left-r.right;if(!gaps.left||d<gaps.left.d)gaps.left={d,r};}if(r.left>=source.right){const d=r.left-source.right;if(!gaps.right||d<gaps.right.d)gaps.right={d,r};}}
      if(horizontal>=Math.min(8,source.width*.25,r.width*.25)){if(r.bottom<=source.top){const d=source.top-r.bottom;if(!gaps.top||d<gaps.top.d)gaps.top={d,r};}if(r.top>=source.bottom){const d=r.top-source.bottom;if(!gaps.bottom||d<gaps.bottom.d)gaps.bottom={d,r};}}
    } return {source,gaps};
  }
  function addMeasure(x,y,length,axis,value,kind='measure'){if(value<0||value>1200)return;const layer=kind==='size-measure'?sizeLayer:gapLayer;const line=document.createElement('div');line.className=`${kind} ${axis}`;line.style.left=`${x}px`;line.style.top=`${y}px`;if(axis==='h')line.style.width=`${length}px`;else line.style.height=`${length}px`;layer.appendChild(line);const label=document.createElement('div');label.className=`measure-label${kind==='size-measure'?' size-measure-label':''}`;label.textContent=`${Math.round(value)} px`;if(axis==='h'){label.style.left=`${Math.max(2,x+length/2-label.textContent.length*3.3)}px`;label.style.top=`${y-24}px`;}else{label.style.left=`${Math.min(innerWidth-52,x+7)}px`;label.style.top=`${Math.max(2,y+length/2-9)}px`;}layer.appendChild(label);}
  function renderSize(element=state.selected?.element){const center=ensureNode('sizeCenterLabel','size-center-label');if(state.mode!=='inspect'||!state.showSize||!element?.isConnected){showNode(center,false);return;}const r=element.getBoundingClientRect();showNode(center,true);center.textContent=`${Math.round(r.width)} × ${Math.round(r.height)}`;center.style.left=`${r.left+r.width/2}px`;center.style.top=`${r.top+r.height/2}px`;}
  function visibleRect(el){if(!el?.isConnected||el===document.body||el===document.documentElement||host.contains(el))return null;const r=el.getBoundingClientRect(),s=getComputedStyle(el);if(s.display==='none'||s.visibility==='hidden'||Number(s.opacity)===0||r.width<2||r.height<2||r.right<=0||r.bottom<=0||r.left>=innerWidth||r.top>=innerHeight)return null;return r;}
  // Alt spacing also supports partially overlapping tracks and nested/inline
  // elements. Keep the comparison visible whenever two rendered boxes have a
  // measurable edge distance.
  function redline(aElement,bElement){
    if(!aElement?.isConnected||!bElement?.isConnected||aElement===bElement)return;
    const a=visibleRect(aElement),b=visibleRect(bElement);if(!a||!b)return;
    const horizontal=b.left>=a.right?{x:a.right,n:b.left-a.right}:a.left>=b.right?{x:b.right,n:a.left-b.right}:null;
    const vertical=b.top>=a.bottom?{y:a.bottom,n:b.top-a.bottom}:a.top>=b.bottom?{y:b.bottom,n:a.top-b.bottom}:null;
    if(horizontal&&horizontal.n>0){const y=Math.max(a.top,b.top)+Math.min(a.height,b.height)/2;addMeasure(horizontal.x,y,horizontal.n,'h',horizontal.n);}
    if(vertical&&vertical.n>0){const x=Math.max(a.left,b.left)+Math.min(a.width,b.width)/2;addMeasure(x,vertical.y,vertical.n,'v',vertical.n);}
    if(!horizontal&&!vertical){
      const dx=Math.max(a.left-b.right,b.left-a.right,0),dy=Math.max(a.top-b.bottom,b.top-a.bottom,0);
      if(dx>0){const x=b.left>=a.right?a.right:b.right,y=Math.max(a.top,b.top)+Math.min(a.height,b.height)/2;addMeasure(x,y,dx,'h',dx);}
      if(dy>0){const y=b.top>=a.bottom?a.bottom:b.bottom,x=Math.max(a.left,b.left)+Math.min(a.width,b.width)/2;addMeasure(x,y,dy,'v',dy);}
    }
  };
  function nearestVisibleSibling(element){
    const source=visibleRect(element);if(!source)return null;
    let branch=element,parent=element.parentElement;
    while(parent&&parent!==document.body){
      let best=null;
      for(const node of parent.children){
        if(node===branch||node.contains(element))continue;
        const r=visibleRect(node);if(!r)continue;
        const vertical=overlap(source.top,source.bottom,r.top,r.bottom),horizontal=overlap(source.left,source.right,r.left,r.right);
        const gap=vertical>0?(r.right<=source.left?source.left-r.right:r.left>=source.right?r.left-source.right:Infinity):horizontal>0?(r.bottom<=source.top?source.top-r.bottom:r.top>=source.bottom?r.top-source.bottom:Infinity):Infinity;
        if(Number.isFinite(gap)&&(!best||gap<best.gap))best={element:node,gap};
      }
      if(best)return best.element;
      branch=parent;parent=parent.parentElement;
    }
    return null;
  }
  function elementNearPoint(x,y,excluded){
    let best=null,bestDistance=Infinity,bestArea=Infinity;
    for(const node of document.body.querySelectorAll('*')){
      if(node===excluded||node.contains(excluded)||excluded?.contains(node))continue;
      const r=visibleRect(node);if(!r)continue;
      const dx=x<r.left?r.left-x:x>r.right?x-r.right:0,dy=y<r.top?r.top-y:y>r.bottom?y-r.bottom:0,distance=dx*dx+dy*dy,area=r.width*r.height;
      if(distance<bestDistance-0.5||(Math.abs(distance-bestDistance)<=0.5&&area<bestArea)){best=node;bestDistance=distance;bestArea=area;}
    }
    return best;
  }
  function renderGaps(){
    if(state.mode!=='inspect'||!state.selected?.element?.isConnected)return;
    const container=state.selected.element,box=container.getBoundingClientRect(),style=getComputedStyle(container);
    if(!box.width||!box.height)return;
    const border={left:parseFloat(style.borderLeftWidth)||0,right:parseFloat(style.borderRightWidth)||0,top:parseFloat(style.borderTopWidth)||0,bottom:parseFloat(style.borderBottomWidth)||0};
    const inner={left:box.left+border.left,right:box.right-border.right,top:box.top+border.top,bottom:box.bottom-border.bottom};
    const children=[...container.children].filter(child=>!host.contains(child)).map(child=>({node:child,style:getComputedStyle(child),r:child.getBoundingClientRect()})).filter(item=>item.style.display!=='none'&&item.style.visibility!=='hidden'&&item.r.width>1&&item.r.height>1);
    if(!children.length)return;
    // The four measurements from the selected container's inner edge to its
    // closest child are its visible internal padding.
    const leftChild=children.reduce((a,b)=>a.r.left<b.r.left?a:b),rightChild=children.reduce((a,b)=>a.r.right>b.r.right?a:b),topChild=children.reduce((a,b)=>a.r.top<b.r.top?a:b),bottomChild=children.reduce((a,b)=>a.r.bottom>b.r.bottom?a:b);
    const cx=box.left+box.width/2,cy=box.top+box.height/2;
    const insetLeft=Math.max(0,leftChild.r.left-inner.left),insetRight=Math.max(0,inner.right-rightChild.r.right),insetTop=Math.max(0,topChild.r.top-inner.top),insetBottom=Math.max(0,inner.bottom-bottomChild.r.bottom);
    if(insetLeft>0)addMeasure(inner.left,cy,insetLeft,'h',insetLeft);
    if(insetRight>0)addMeasure(rightChild.r.right,cy,insetRight,'h',insetRight);
    if(insetTop>0)addMeasure(cx,inner.top,insetTop,'v',insetTop);
    if(insetBottom>0)addMeasure(cx,bottomChild.r.bottom,insetBottom,'v',insetBottom);
    // Show the nearest internal gap between visible sibling elements. The
    // direction is inferred from overlapping tracks, which works for flex,
    // grid, and ordinary block layouts.
    let horizontalGap=null,verticalGap=null;
    for(let i=0;i<children.length;i++)for(let j=i+1;j<children.length;j++){
      const a=children[i].r,b=children[j].r,verticalOverlap=overlap(a.top,a.bottom,b.top,b.bottom),horizontalOverlap=overlap(a.left,a.right,b.left,b.right);
      if(verticalOverlap>0){const d=a.right<=b.left?b.left-a.right:b.right<=a.left?a.left-b.right:-1;if(d>=0&&(!horizontalGap||d<horizontalGap.d))horizontalGap={d,a,b};}
      if(horizontalOverlap>0){const d=a.bottom<=b.top?b.top-a.bottom:b.bottom<=a.top?a.top-b.bottom:-1;if(d>=0&&(!verticalGap||d<verticalGap.d))verticalGap={d,a,b};}
    }
    if(horizontalGap&&horizontalGap.d>0){const {a,b,d}=horizontalGap;const x=a.right<=b.left?a.right:b.right;const y=Math.max(a.top,b.top)+Math.min(a.height,b.height)/2;addMeasure(x,y,d,'h',d);}
    if(verticalGap&&verticalGap.d>0){const {a,b,d}=verticalGap;const y=a.bottom<=b.top?a.bottom:b.bottom;const x=Math.max(a.left,b.left)+Math.min(a.width,b.width)/2;addMeasure(x,y,d,'v',d);}
  }
  const checkFeishu = async () => {if(state.feishuChecking)return;state.feishuChecking=true;try{state.feishuStatus=await chrome.runtime.sendMessage({type:'feishu-status'});}catch{state.feishuStatus={configured:false};}finally{state.feishuChecking=false;render();}};
  function settingsPanel(){const s=ensureNode('settings','settings');showNode(s,state.settingsOpen);if(!state.settingsOpen)return;s.style.left=`${state.toolbar.left}px`;s.style.top=`${state.toolbar.top+52}px`;s.replaceChildren();const title=document.createElement('h3');title.textContent='Inspector settings';const label=document.createElement('label');label.className='setting';label.innerHTML='<span>Show style panel</span><input type="checkbox" />';const input=label.querySelector('input');input.checked=state.showPanel;input.onchange=()=>{state.showPanel=input.checked;state.panelOpen=state.showPanel;render()};const feishu=document.createElement('div');feishu.className='feishu-setting';const dot=document.createElement('i');const status=state.feishuStatus;dot.className=`feishu-dot ${status?.configured?'ready':'warning'}`;const text=document.createElement('span');text.className='feishu-text';text.textContent=state.feishuChecking?'Checking Feishu…':status?.configured?`Feishu connected · ${status.appId}`:'Connect your Feishu app';const open=document.createElement('button');open.type='button';open.className='feishu-open';open.textContent='Configure';open.onclick=()=>chrome.runtime.sendMessage({type:'open-feishu-settings'});feishu.append(dot,text,open);s.append(title,label,feishu);if(!state.feishuStatus&&!state.feishuChecking)checkFeishu();}
  function submitToast(){const toast=ensureNode('submitToast','submit-toast');showNode(toast,!!state.submitStatus);if(!state.submitStatus)return;toast.className=`submit-toast ${state.submitStatus}`;toast.textContent=state.submitMessage;}
  function paintCaptureOverlay(){
    const root=ensureNode('captureOverlay','capture-overlay');showNode(root,state.captureSelecting);app.classList.toggle('capture-active',state.captureSelecting);if(!state.captureSelecting)return;
    if(!root.firstChild){
      root.innerHTML='<div class="capture-shade" data-shade="top"></div><div class="capture-shade" data-shade="left"></div><div class="capture-shade" data-shade="right"></div><div class="capture-shade" data-shade="bottom"></div><div class="capture-selection"><span class="capture-size"></span><i class="capture-handle" data-handle="nw"></i><i class="capture-handle" data-handle="n"></i><i class="capture-handle" data-handle="ne"></i><i class="capture-handle" data-handle="e"></i><i class="capture-handle" data-handle="se"></i><i class="capture-handle" data-handle="s"></i><i class="capture-handle" data-handle="sw"></i><i class="capture-handle" data-handle="w"></i></div><div class="capture-instructions">Drag to select an area · release to capture · Esc to cancel</div><div class="capture-controls"><button type="button" class="capture-cancel">Cancel</button></div>';
      root.querySelector('.capture-cancel').onclick=e=>{e.stopPropagation();cancelRegionCapture();};
      const confirmButton=root.querySelector('.capture-confirm');if(confirmButton)confirmButton.onclick=e=>{e.stopPropagation();confirmRegionCapture();};
      root.addEventListener('pointerdown',e=>{
        if(e.button!==0||e.target.closest('.capture-controls'))return;e.preventDefault();e.stopPropagation();
        const handle=e.target.closest('.capture-handle')?.dataset.handle,selection=e.target.closest('.capture-selection'),startX=e.clientX,startY=e.clientY,rect=state.captureRect?{...state.captureRect}:null;
        state.captureDrag={kind:handle?'resize':selection&&rect?'move':'draw',handle:handle||'',startX,startY,rect};
        if(state.captureDrag.kind==='draw')state.captureRect={left:startX,top:startY,width:0,height:0};
        root.setPointerCapture?.(e.pointerId);paintCaptureOverlay();
      });
      root.addEventListener('pointermove',e=>{
        const drag=state.captureDrag;if(!drag)return;e.preventDefault();
        if(drag.kind==='draw'){
          const next=normalizeRect(drag.startX,drag.startY,clamp(e.clientX,0,innerWidth),clamp(e.clientY,0,innerHeight));state.captureRect=next;
        }else if(drag.kind==='move'){
          state.captureRect={...drag.rect,left:clamp(drag.rect.left+e.clientX-drag.startX,0,innerWidth-drag.rect.width),top:clamp(drag.rect.top+e.clientY-drag.startY,0,innerHeight-drag.rect.height)};
        }else{
          let left=drag.rect.left,top=drag.rect.top,right=left+drag.rect.width,bottom=top+drag.rect.height;const h=drag.handle;
          if(h.includes('w'))left=clamp(e.clientX,0,right-8);if(h.includes('e'))right=clamp(e.clientX,left+8,innerWidth);if(h.includes('n'))top=clamp(e.clientY,0,bottom-8);if(h.includes('s'))bottom=clamp(e.clientY,top+8,innerHeight);state.captureRect={left,top,width:right-left,height:bottom-top};
        }
        paintCaptureOverlay();
      });
      const endDrag=e=>{const drag=state.captureDrag;if(!drag)return;state.captureDrag=null;try{root.releasePointerCapture?.(e.pointerId);}catch{}if(drag.kind==='draw'&&state.captureRect?.width>=8&&state.captureRect?.height>=8){confirmRegionCapture();return;}paintCaptureOverlay();};
      root.addEventListener('pointerup',endDrag);root.addEventListener('pointercancel',endDrag);
    }
    const rect=state.captureRect,selection=root.querySelector('.capture-selection'),controls=root.querySelector('.capture-controls'),confirm=root.querySelector('.capture-confirm'),shades=[...root.querySelectorAll('.capture-shade')];
    if(!rect){selection.hidden=true;if(controls)controls.hidden=true;Object.assign(shades[0].style,{left:'0px',top:'0px',width:`${innerWidth}px`,height:`${innerHeight}px`});for(let i=1;i<shades.length;i++)shades[i].style.cssText='display:none';return;}
    selection.hidden=false;if(controls)controls.hidden=false;setBox(selection,rect);selection.querySelector('.capture-size').textContent=`${Math.round(rect.width)} × ${Math.round(rect.height)}`;selection.querySelector('.capture-size').style.top=rect.top<34?'6px':'-27px';
    const right=rect.left+rect.width,bottom=rect.top+rect.height,areas=[[0,0,innerWidth,rect.top],[0,rect.top,rect.left,rect.height],[right,rect.top,Math.max(0,innerWidth-right),rect.height],[0,bottom,innerWidth,Math.max(0,innerHeight-bottom)]];
    shades.forEach((shade,index)=>{const [left,top,width,height]=areas[index];shade.style.cssText=`left:${left}px;top:${top}px;width:${width}px;height:${height}px`;});
    const controlWidth=88,controlHeight=40;let controlLeft=clamp(rect.left+rect.width-controlWidth,8,innerWidth-controlWidth-8),controlTop=bottom+10;if(controlTop+controlHeight>innerHeight-8)controlTop=Math.max(8,rect.top-controlHeight-10);if(controls){controls.style.left=`${controlLeft}px`;controls.style.top=`${controlTop}px`;if(confirm){confirm.disabled=state.captureBusy||rect.width<8||rect.height<8;confirm.textContent=state.captureBusy?'Capturing…':'Capture';}}
  }
  function feedbackDialog(){
    const root=ensureNode('feedback','feedback-backdrop');showNode(root,state.feedbackOpen);if(!state.feedbackOpen)return;root.classList.remove('stacked');
    root.replaceChildren();
    const dialog=document.createElement('div');dialog.className='feedback-dialog';dialog.tabIndex=-1;dialog.setAttribute('role','dialog');dialog.setAttribute('aria-modal','true');dialog.setAttribute('aria-label','Send feedback to Feishu');root.appendChild(dialog);
    const cardHead=(label,action,ariaLabel)=>{const head=document.createElement('div');head.className='feedback-head';const title=document.createElement('strong');title.textContent=label;const close=document.createElement('button');close.className='feedback-close';close.type='button';close.setAttribute('aria-label',ariaLabel);close.innerHTML=icon('close');close.onclick=action;head.append(title,close);return {head,close};};
    const close=()=>{resetFeedback();render();};
    const questionCard=document.createElement('section');questionCard.className='feedback-card feedback-question-card';const questionHead=cardHead('Question',close,'Close feedback');questionCard.appendChild(questionHead.head);
    const inputs=document.createElement('div');inputs.className=`feedback-inputs${state.feedbackEditing?' is-editing':''}`;const question=document.createElement('textarea');question.placeholder='Describe the issue...';question.value=state.feedbackQuestion;question.disabled=state.feedbackBusy;question.setAttribute('aria-label','Question');const note=document.createElement('input');note.type='text';note.placeholder='Optional note...';note.value=state.feedbackNote;note.disabled=state.feedbackBusy;note.setAttribute('aria-label','Optional note');inputs.append(question,note);questionCard.appendChild(inputs);
    const selectedDocument=state.documents?.find(item=>item.id===state.selectedDocumentId);if(selectedDocument){const connected=document.createElement('div');connected.className='feedback-connected-document';connected.title=selectedDocument.name||'Connected document';const name=document.createElement('span');name.textContent=selectedDocument.name||'Connected document';connected.appendChild(name);questionCard.appendChild(connected);}
    const status=document.createElement('div');status.className=`feedback-status${state.feedbackStatusType?` ${state.feedbackStatusType}`:''}`;status.textContent=state.feedbackMessage;questionCard.appendChild(status);
    const actions=document.createElement('div');actions.className='feedback-actions';const destinationToggle=document.createElement('button');destinationToggle.className=`feedback-destination-toggle${state.destinationOpen?' active':''}`;destinationToggle.type='button';destinationToggle.setAttribute('aria-label','Destination document');destinationToggle.setAttribute('aria-expanded',String(state.destinationOpen));destinationToggle.innerHTML=icon('scroll-text');const send=document.createElement('button');send.className='feedback-send';send.type='button';send.disabled=state.feedbackBusy||state.feedbackAdding||!!state.feedbackResultUrl;send.textContent=state.feedbackBusy?'Sending…':'Send';actions.append(destinationToggle,send);questionCard.appendChild(actions);dialog.appendChild(questionCard);
    let select=null,docUrl=null;
    if(state.destinationOpen){const destinationCard=document.createElement('section');destinationCard.className='feedback-card feedback-destination-card';const destinationHead=cardHead('Destination document',()=>{state.destinationOpen=false;render();},'Close destination document');destinationCard.appendChild(destinationHead.head);const fields=document.createElement('div');fields.className='feedback-destination-fields';const selectWrap=document.createElement('div');selectWrap.className='feedback-select-wrap';select=document.createElement('select');select.className='feedback-doc';select.setAttribute('aria-label','Select a document');const empty=document.createElement('option');empty.value='';empty.textContent='Select a document';select.appendChild(empty);for(const saved of state.documents){const option=document.createElement('option');option.value=saved.id;option.textContent=saved.name||'Untitled Feishu document';option.selected=saved.id===state.selectedDocumentId;select.appendChild(option);}select.disabled=state.feedbackBusy||state.feedbackAdding;const chevron=document.createElement('span');chevron.className='feedback-select-icon';chevron.innerHTML=icon('chevron-down');selectWrap.append(select,chevron);docUrl=document.createElement('input');docUrl.className='feedback-doc-url';docUrl.type='url';docUrl.placeholder=state.feedbackAdding?'Adding document...':'Paste the document link';docUrl.value=state.feedbackDocumentUrl;docUrl.disabled=state.feedbackBusy||state.feedbackAdding;docUrl.setAttribute('aria-label','Paste the document link');fields.append(selectWrap,docUrl);destinationCard.appendChild(fields);dialog.appendChild(destinationCard);}
    if(state.screenshotDataUrl){const shotColumn=document.createElement('div');shotColumn.className='feedback-shot-column';const shotWrap=document.createElement('div');shotWrap.className='feedback-shot-wrap';const canvas=document.createElement('canvas');canvas.className='feedback-canvas';canvas.setAttribute('aria-label','Screenshot annotation canvas');const retake=document.createElement('button');retake.className='feedback-retake';retake.type='button';retake.textContent='Retake';retake.disabled=state.feedbackBusy;retake.onclick=()=>beginRegionCapture();shotWrap.append(canvas,retake);shotColumn.appendChild(shotWrap);const annotateBar=document.createElement('div');annotateBar.className='feedback-annotate-bar';const toolButton=(tool,iconName,label)=>{const button=document.createElement('button');button.type='button';button.innerHTML=icon(iconName);button.title=label;button.setAttribute('aria-label',label);button.classList.toggle('active',state.annotationTool===tool);button.onclick=()=>{state.annotationTool=tool;state.annotationDrawing=false;state.annotationDraft=null;render();};return button;};const pen=toolButton('pen','pen','Pen');const arrow=toolButton('arrow','arrow-up-right','Arrow');const rectangle=toolButton('rectangle','square','Rectangle');const ellipse=toolButton('ellipse','circle','Ellipse');const divider=document.createElement('span');divider.className='annotation-divider';const undo=document.createElement('button');undo.type='button';undo.innerHTML=icon('undo');undo.title='Undo (Ctrl/Cmd + Z)';undo.setAttribute('aria-label','Undo last annotation');undo.disabled=!state.annotationHistory.length||state.feedbackBusy;undo.onclick=()=>{if(undoAnnotation())render();};annotateBar.append(pen,arrow,rectangle,ellipse,divider,undo);const help=document.createElement('span');help.className='annotation-help';help.textContent=state.annotationHistory.length?`${state.annotationHistory.length} annotation${state.annotationHistory.length===1?'':'s'}`:'Drag to annotate';annotateBar.appendChild(help);shotColumn.appendChild(annotateBar);root.appendChild(shotColumn);setupAnnotations(canvas).catch(error=>{state.feedbackMessage=error.message;state.feedbackStatusType='error';render();});}else{state.feedbackMessage='Screenshot unavailable. Close this dialog and try again.';state.feedbackStatusType='error';status.textContent=state.feedbackMessage;status.className='feedback-status error';}
    const shotColumn=root.querySelector('.feedback-shot-column');
    if(shotColumn){const anchor=state.feedbackAnchorRect||{left:innerWidth/2-180,top:innerHeight/2-140,width:360,height:280};const gap=12,formWidth=336,edge=12,toolbarHeight=46,formHeight=state.destinationOpen?394:230;const maxShotWidth=Math.min(560,innerWidth-edge*2),maxShotHeight=Math.min(420,Math.max(120,innerHeight-toolbarHeight-edge*2));const aspect=Math.max(.1,(anchor.width||360)/(anchor.height||280));let shotWidth=Math.min(maxShotWidth,Math.max(180,anchor.width||360)),shotHeight=shotWidth/aspect;if(shotHeight>maxShotHeight){shotHeight=maxShotHeight;shotWidth=Math.min(maxShotWidth,shotHeight*aspect);}if(shotHeight<120){shotHeight=120;shotWidth=Math.min(maxShotWidth,shotHeight*aspect);}const totalWidth=shotWidth+gap+formWidth;const canSitBeside=totalWidth<=innerWidth-edge*2&&formHeight<=innerHeight-edge*2;if(canSitBeside){const groupLeft=clamp(anchor.left,edge,innerWidth-totalWidth-edge);const shotTop=clamp(anchor.top,edge,Math.max(edge,innerHeight-shotHeight-toolbarHeight-edge));const dialogTop=clamp(anchor.top,edge,Math.max(edge,innerHeight-formHeight-edge));shotColumn.style.left=`${groupLeft}px`;shotColumn.style.top=`${shotTop}px`;shotColumn.style.width=`${shotWidth}px`;shotColumn.style.height=`${shotHeight+toolbarHeight}px`;dialog.style.left=`${groupLeft+shotWidth+gap}px`;dialog.style.top=`${dialogTop}px`;}else{root.classList.add('stacked');dialog.style.left='';dialog.style.top='';shotColumn.style.left='';shotColumn.style.top='';shotColumn.style.width='';shotColumn.style.height='';}}
    root.onclick=e=>{if(e.target===root&&!state.feedbackBusy)close();};root.onkeydown=e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='z'&&!e.shiftKey&&!(e.target instanceof HTMLInputElement)&&!(e.target instanceof HTMLTextAreaElement)){e.preventDefault();e.stopPropagation();if(undoAnnotation())render();return;}if(e.key==='Escape'){e.preventDefault();e.stopPropagation();close();}};inputs.onclick=()=>{if(!state.feedbackEditing){state.feedbackEditing=true;inputs.classList.add('is-editing');requestAnimationFrame(()=>question.focus());}};question.onfocus=note.onfocus=()=>{state.feedbackEditing=true;inputs.classList.add('is-editing');};question.oninput=()=>{state.feedbackQuestion=question.value;};note.oninput=()=>{state.feedbackNote=note.value;};destinationToggle.onclick=()=>{state.feedbackQuestion=question.value;state.feedbackNote=note.value;state.destinationOpen=!state.destinationOpen;state.feedbackMessage='';state.feedbackStatusType='';render();};
    if(select&&docUrl){select.onchange=()=>{state.selectedDocumentId=select.value;state.feedbackMessage='';state.feedbackStatusType='';saveDocumentState().catch(error=>{state.feedbackMessage=error.message;state.feedbackStatusType='error';render();});render();};docUrl.oninput=()=>{state.feedbackDocumentUrl=docUrl.value;};const addDocument=async()=>{state.feedbackDocumentUrl=docUrl.value.trim();if(!state.feedbackDocumentUrl||state.feedbackAdding)return;state.feedbackAdding=true;state.feedbackMessage='';state.feedbackStatusType='';render();try{const saved=await saveDocument(state.feedbackDocumentUrl);state.feedbackDocumentUrl='';state.feedbackMessage=`Selected: ${saved.name}`;state.feedbackStatusType='success';}catch(error){state.feedbackMessage=error.message;state.feedbackStatusType='error';}finally{state.feedbackAdding=false;render();}};docUrl.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();addDocument();}};docUrl.onpaste=()=>setTimeout(addDocument,0);}
    send.onclick=()=>{state.feedbackQuestion=question.value;state.feedbackNote=note.value;const destination=state.documents.find(item=>item.id===state.selectedDocumentId);if(!state.feedbackQuestion.trim()){state.feedbackEditing=true;state.feedbackMessage='Enter a question before sending.';state.feedbackStatusType='error';render();return;}if(!destination){state.destinationOpen=true;state.feedbackMessage='Select or paste a Feishu document link.';state.feedbackStatusType='error';render();return;}if(!state.screenshotDataUrl){state.feedbackMessage='Capture a screenshot first.';state.feedbackStatusType='error';render();return;}const payload={question:state.feedbackQuestion.trim(),note:state.feedbackNote.trim(),documentId:destination.id,documentUrl:destination.url,pageTitle:window.document.title||'Untitled page',pageUrl:location.href,viewport:`${innerWidth} × ${innerHeight}`,screenshotDataUrl:state.screenshotDataUrl};resetFeedback();submitFeedbackInBackground(payload);beginRegionCapture();};
    dialog.onkeydown=e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='z'&&!e.shiftKey&&!(e.target instanceof HTMLInputElement)&&!(e.target instanceof HTMLTextAreaElement)){e.preventDefault();e.stopPropagation();if(undoAnnotation())render();return;}if(e.key==='Escape'){e.preventDefault();e.stopPropagation();close();return;}if(e.key==='Enter'&&!e.shiftKey&&e.target!==docUrl&&e.target!==select){e.preventDefault();e.stopPropagation();send.click();}};if(!state.feedbackEditing)requestAnimationFrame(()=>dialog.focus({preventScroll:true}));
  }
  function colorPanel(){const p=ensureNode('colorPanel','color-panel'),visible=state.mode==='eyedropper'&&(state.pickedColor||state.eyedropperError);showNode(p,!!visible);if(!visible)return;p.style.left=`${Math.min(innerWidth-236,Math.max(8,state.toolbar.left))}px`;p.style.top=`${Math.min(innerHeight-170,state.toolbar.top+52)}px`;const key=`${state.pickedColor}:${state.eyedropperError}`;if(p.dataset.key===key)return;p.dataset.key=key;if(state.eyedropperError){p.innerHTML=`<div class="color-error">${state.eyedropperError}</div>`;return;}const hex=state.pickedColor,{r,g,b}=hexToRgb(hex);p.innerHTML=`<div class="color-preview" style="background:${hex}"></div><button class="color-copy" type="button" aria-label="Copy ${hex}"><span>${hex}</span><span class="copy-note">点击复制</span></button><div class="color-rgb"><span>RGB</span><span>rgb(${r}, ${g}, ${b})</span></div>`;const copy=p.querySelector('.color-copy'),note=p.querySelector('.copy-note');copy.onclick=async e=>{e.stopPropagation();try{await navigator.clipboard.writeText(hex);note.textContent='已复制';setTimeout(()=>{if(note.isConnected)note.textContent='点击复制';},1200);}catch{note.textContent='复制失败';}};}
  const rulerCanvas = (key,className) => {if(!ui[key]){ui[key]=document.createElement('canvas');ui[key].className=className;ui[key].hidden=true;app.appendChild(ui[key]);}return ui[key];};
  const prepareRuler = (canvas,width,height) => {const dpr=Math.max(1,devicePixelRatio||1),pixelWidth=Math.round(width*dpr),pixelHeight=Math.round(height*dpr);if(canvas.width!==pixelWidth||canvas.height!==pixelHeight){canvas.width=pixelWidth;canvas.height=pixelHeight;canvas.style.width=`${width}px`;canvas.style.height=`${height}px`;}const ctx=canvas.getContext('2d');ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,width,height);ctx.fillStyle='#fafafa';ctx.fillRect(0,0,width,height);ctx.strokeStyle='#b8b8b8';ctx.fillStyle='#999';ctx.lineWidth=1;ctx.font='10px Inter,ui-sans-serif,system-ui';return ctx;};
  function renderRulers(){const visible=state.enabled&&state.mode==='guides',top=rulerCanvas('rulerTop','pixel-ruler top'),left=rulerCanvas('rulerLeft','pixel-ruler left'),corner=ensureNode('rulerCorner','ruler-corner');showNode(top,visible);showNode(left,visible);showNode(corner,visible);if(!visible)return;const topWidth=Math.max(1,innerWidth-28),leftHeight=Math.max(1,innerHeight-28),step=10,major=100;let ctx=prepareRuler(top,topWidth,28);ctx.beginPath();ctx.moveTo(0,27.5);ctx.lineTo(topWidth,27.5);const firstX=Math.ceil((scrollX+28)/step)*step;for(let value=firstX;value<=scrollX+innerWidth;value+=step){const x=Math.round(value-scrollX-28)+.5,isMajor=value%major===0,isMid=value%50===0;ctx.moveTo(x,28);ctx.lineTo(x,28-(isMajor?9:isMid?6:4));if(isMajor){const label=String(value),width=ctx.measureText(label).width;ctx.fillText(label,x-width/2,11);}}ctx.stroke();ctx=prepareRuler(left,28,leftHeight);ctx.beginPath();ctx.moveTo(27.5,0);ctx.lineTo(27.5,leftHeight);const firstY=Math.ceil((scrollY+28)/step)*step;for(let value=firstY;value<=scrollY+innerHeight;value+=step){const y=Math.round(value-scrollY-28)+.5,isMajor=value%major===0,isMid=value%50===0;ctx.moveTo(28,y);ctx.lineTo(28-(isMajor?9:isMid?6:4),y);if(isMajor){const label=String(value),width=ctx.measureText(label).width;ctx.save();ctx.translate(10,y+width/2);ctx.rotate(-Math.PI/2);ctx.fillText(label,0,0);ctx.restore();}}ctx.stroke();}
  function syncXrayClass(){if(document.body)document.body.classList.toggle('xray-mode',state.enabled&&state.mode==='xray');}
  function panel(){const t=state.mode==='typography',selected=t?state.hovered:state.selected,p=ensureNode('panel','panel');if(!selected||!state.panelOpen||!state.showPanel){showNode(p,false);return;}showNode(p,true);const {tag,rect,style,layout}=selected,tokens=selected.tokens||tokensFor(selected.element,t),panelW=272,gap=12,edge=8;let x,y;if(state.panelPosition){x=state.panelPosition.left;y=state.panelPosition.top;}else{const right=rect.left+rect.width+gap,left=rect.left-panelW-gap;x=right+panelW<=innerWidth-edge?right:left>=edge?left:Math.max(edge,innerWidth-panelW-edge);y=rect.top;if(y+300>innerHeight-edge)y=Math.max(edge,Math.min(rect.top-312,innerHeight-300-edge));}x=clamp(x,edge,Math.max(edge,innerWidth-panelW-edge));y=clamp(y,edge,Math.max(edge,innerHeight-300-edge));p.style.left=`${x}px`;p.style.top=`${y}px`;if(p._uiReviewElement===selected.element&&p.dataset.mode===state.mode)return;p._uiReviewElement=selected.element;p.dataset.mode=state.mode;p.replaceChildren();p.innerHTML=`<h3><span class="panel-title"><span class="panel-tag">${t?'Text':'Element'}</span><span class="panel-kind">${tag}</span></span><button class="panel-close" aria-label="Close details">×</button></h3>`;p.querySelector('.panel-close').onclick=e=>{e.stopPropagation();state.panelOpen=false;state.closedElement=selected.element;render()};const head=p.querySelector('h3');head.onpointerdown=e=>{if(e.target.closest('button'))return;e.preventDefault();const sx=e.clientX,sy=e.clientY,sl=x,st=y;const move=m=>{const nx=clamp(sl+m.clientX-sx,edge,Math.max(edge,innerWidth-panelW-edge)),ny=clamp(st+m.clientY-sy,edge,Math.max(edge,innerHeight-300-edge));p.style.left=`${nx}px`;p.style.top=`${ny}px`;state.panelPosition={left:nx,top:ny};};const up=()=>{removeEventListener('pointermove',move);removeEventListener('pointerup',up)};addEventListener('pointermove',move);addEventListener('pointerup',up);};if(t){section(p,'Typography');[['Font family',style.fontFamily],['Text size token',tokens.size],['Text style token',tokens.style],['Text color token',tokens.color],['Font size',style.fontSize],['Line height',style.lineHeight],['Letter spacing',style.letterSpacing],['Text align',style.textAlign],['Text color',style.color,true]].forEach(x=>row(p,x[0],x[1],x[2]));}else{section(p,'Layout');[['Size',`${Math.round(rect.width)} × ${Math.round(rect.height)}`],['Margin',style.margin],['Padding',style.padding],['Gap',style.gap]].forEach(x=>row(p,x[0],x[1]));section(p,'Style');[['Text color',style.color,true],['Font size',style.fontSize],['Font weight',style.fontWeight],['Line height',style.lineHeight],['Radius',style.borderRadius],['Background',style.backgroundColor,true],['Opacity',style.opacity]].forEach(x=>row(p,x[0],x[1],x[2]));}}
  function render(){syncXrayClass();app.style.display=state.enabled?'':'none';if(!state.enabled){app.classList.remove('capture-active');gapLayer.replaceChildren();sizeLayer.replaceChildren();renderRulers();return;}toolbar();const hoverBox=ensureNode('hoverBox','box hover-box'),hoverLabel=ensureNode('hoverLabel','label'),selectedBox=ensureNode('selectedBox','box selected-box');const canHover=state.hovered&&state.mode==='inspect'&&state.hovered.element!==state.selected?.element;showNode(hoverBox,!!canHover);showNode(hoverLabel,!!canHover);if(canHover){const {rect,tag}=state.hovered;setBox(hoverBox,rect);hoverLabel.style.transform=`translate3d(${Math.max(4,rect.left)}px,${Math.max(4,rect.top-27)}px,0)`;const label=`${tag} · ${Math.round(rect.width)} × ${Math.round(rect.height)}`;if(hoverLabel.textContent!==label)hoverLabel.textContent=label;}const canSelect=state.mode==='inspect'&&state.selected?.element?.isConnected;showNode(selectedBox,!!canSelect);if(canSelect){state.selected.rect=rectFor(state.selected.element);setBox(selectedBox,state.selected.rect);}sizeLayer.replaceChildren();if(canSelect&&state.showSize)renderSize();if(state.mode!=='inspect'||state.alt||state.measureRefresh){gapLayer.replaceChildren();if(state.mode==='inspect'&&state.alt){const compareElement=state.hovered?.element!==state.selected?.element?state.hovered?.element:nearestVisibleSibling(state.selected.element);redline(state.selected?.element,compareElement);}else if(state.mode==='inspect'&&state.measureRefresh&&canSelect)renderGaps();}state.measureRefresh=false;renderRulers();panel();colorPanel();settingsPanel();feedbackDialog();paintCaptureOverlay();submitToast();const h=ensureNode('hint','hint');showNode(h,true);const hint=state.mode==='typography'?'Text inspector active · hover text · click Aa to close':state.mode==='inspect'?(state.alt?'Alt spacing mode · hover another element':'Element inspector active · click an element to measure internal spacing · click ⌖ to close'):state.mode==='guides'?'Pixel rulers enabled':state.mode==='eyedropper'?'Eyedropper active · click the tool to pick again':state.mode==='xray'?'X-ray mode enabled via body.xray-mode · press X to close':'Page interaction restored · choose a tool to inspect';if(h.textContent!==hint)h.textContent=hint;}
  document.addEventListener('mousemove',e=>{if(!state.enabled||!['inspect','typography'].includes(state.mode)||host.contains(e.target))return;let target=state.mode==='typography'?textTarget(e.target):e.target;if(state.mode==='inspect'&&state.alt&&state.selected?.element?.isConnected){target=elementNearPoint(e.clientX,e.clientY,state.selected.element)||nearestVisibleSibling(state.selected.element)||target;}if(target===document.body||target===document.documentElement){if(!state.hovered&&!state.showSize)return;state.hovered=null;state.showSize=false;schedule();return;}if(target===state.hovered?.element)return;state.panelOpen=true;state.closedElement=null;if(state.mode==='inspect')state.showSize=target===state.selected?.element;state.hovered=info(target,false,state.mode==='typography');if(state.mode==='typography'&&target===state.closedElement)state.panelOpen=false;schedule();},{passive:true});
  // Alt is held while comparing; releasing it ends comparison immediately.
  // Inspection mode is a modal interaction layer. Stop all page gestures before
  // their target handlers receive them; the shadow-host UI remains interactive.
  const blockPageEvent = e => { if(!state.enabled||!state.mode||state.mode==='eyedropper'||state.mode==='guides'||host.contains(e.target)) return; if(e.type==='click'&&state.mode==='inspect'){state.selected=info(e.target,true);state.showSize=true;state.measureRefresh=true;state.panelOpen=true;state.closedElement=null;render();} e.preventDefault(); e.stopImmediatePropagation(); };
  ['pointerdown','pointerup','click','dblclick','auxclick','contextmenu','dragstart'].forEach(type=>document.addEventListener(type,blockPageEvent,{capture:true}));
  document.addEventListener('keydown',e=>{const k=e.key.toLowerCase();if(state.captureSelecting){if(k==='escape'){e.preventDefault();e.stopImmediatePropagation();cancelRegionCapture();}else if(k==='enter'){e.preventDefault();e.stopImmediatePropagation();confirmRegionCapture();}return;}if(host.contains(e.target))return;if(e.target instanceof HTMLInputElement||e.target instanceof HTMLTextAreaElement||e.target.isContentEditable)return;if(!state.enabled)return;if(k==='alt'&&state.mode==='inspect'){state.alt=true;schedule();return;}const modes={i:'inspect',t:'typography',r:'guides',p:'eyedropper',x:'xray'};if(modes[k]){e.preventDefault();e.stopImmediatePropagation();activateMode(modes[k]);return;}if(k==='c'){e.preventDefault();e.stopImmediatePropagation();beginRegionCapture();return;}if(k==='s'){e.preventDefault();e.stopImmediatePropagation();state.settingsOpen=!state.settingsOpen;render();return;}if(k==='escape'){e.preventDefault();e.stopImmediatePropagation();if(state.feedbackOpen){resetFeedback();render();}else if(state.mode){exitMode();render();}return;}if(state.mode&&!['eyedropper','guides'].includes(state.mode)){e.preventDefault();e.stopImmediatePropagation();}}, {capture:true});
  document.addEventListener('keyup',e=>{if(e.key==='Alt'&&state.alt){state.alt=false;state.measureRefresh=true;schedule();}}, {capture:true});
  let geometryTimer=0;
  const geometryChanged=()=>{if(!state.enabled||!state.mode)return;if(state.hovered?.element?.isConnected)state.hovered.rect=rectFor(state.hovered.element);if(state.selected?.element?.isConnected)state.selected.rect=rectFor(state.selected.element);gapLayer.hidden=true;sizeLayer.hidden=true;schedule();clearTimeout(geometryTimer);geometryTimer=setTimeout(()=>{gapLayer.hidden=false;sizeLayer.hidden=false;state.measureRefresh=true;schedule();},100);};
  addEventListener('scroll',geometryChanged,{capture:true,passive:true});
  addEventListener('resize',geometryChanged,{passive:true});
  chrome.runtime.onMessage.addListener(m=>{if(m.type==='toggle'){state.enabled=!state.enabled;if(!state.enabled){resetFeedback();exitMode();}render();}});render();
})();
